[package]
name = "presolve-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
presolve_compiler = { package = "presolve-compiler", path = "../presolve_compiler" }
presolve_parser = { package = "presolve-parser", path = "../presolve_parser" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "presolve"
path = "src/main.rs"

[lints]
workspace = true
