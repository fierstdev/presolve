[package]
name = "presolve-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "The Presolve compiler and application command-line interface."
readme = "README.md"
rust-version.workspace = true

[dependencies]
presolve_compiler = { package = "presolve-compiler", path = "../presolve_compiler", version = "0.2.0-beta.11" }
presolve_parser = { package = "presolve-parser", path = "../presolve_parser", version = "0.2.0-beta.11" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[[bin]]
name = "presolve"
path = "src/main.rs"

[lints]
workspace = true
