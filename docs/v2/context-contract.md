# Context tokens and providers contract

`context_projection` projects canonical Context token and Provider identities,
default presence, type-codec eligibility, and provider value-expression identity.
It does not accept string designators or reimplement provider selection.
