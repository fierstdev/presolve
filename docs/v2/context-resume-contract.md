# Context consumption and resume contract

`context_resume_projection` preserves canonical provider/default/unresolved
consumer outcomes and exact Context resume-slot identities. It does not perform
runtime lookup or change nearest-provider selection.
