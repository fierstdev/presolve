# Effects contract

`effect_projection` exposes canonical browser-only runtime-effect triggers and
execution functions. Scheduling, cleanup, declaration order, and resume remain
owned by existing runtime-effect products.
