File: fixtures/0011-direct-assignment/input/ResetCounter.tsx
TemplateGraph:
  templates:
    template ResetCounter
      root: <button> id=n0
      attributes:
        data-ez-on-click = event-handler(click -> this.reset)
        data-ez-bindings = bindings(this.count)
      children:
        Text("Count:")
        Binding id=n1 expression="this.count" initial=Some("5")
