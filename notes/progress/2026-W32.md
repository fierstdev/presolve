# 2026-W32

Beta.25 published successfully through GitHub Actions after its complete local
and hosted release gates. Exact registry checks confirmed beta.25 for the
framework, CLI, TypeScript authority, compiler WASM, language service, LSP,
testing, Vite integration, and creator. A fresh public
`pnpm create presolve@0.2.0-beta.25` project installed, checked, built, and
prepared its Cloudflare deployment with the canonical `app.tsx`, `app.css`,
`index.html`, routes, and public assets.

The clean-room browser proof then exposed a real computed-runtime defect in the
published starter: `this.count + 1` treated serialized numeric lexemes as
strings, rendering `01` and then `11`. The fix is compiler-owned rather than a
scaffold workaround. Shared pure-program constants now retain recursive value
kinds in Computed schema v13, Effect schema v8, and Context schema v3. The
runtime decodes those tagged values before arithmetic, access, conditional,
Context, or Effect execution. Numeric and string literals remain distinct while
State and resume serialization contracts remain unchanged.

The exact generated counter now renders `Count: 0` with `Next increment: 1`,
then clicks to `Count: 1` with `Next increment: 2`; the browser reports no
warnings or errors. The structural component/Effect lifecycle proof also passes
with the shared representation, preventing an artifact-family split.

Beta.26 is a locally verified release candidate. An uninterrupted
`pnpm release:check` passes 665 compiler tests, all CLI suites, all 59
real-browser cases, TypeScript 7.0.2 compatibility, all package tests, 91 public
documentation files, the packed clean-room scaffold, production determinism,
fixed production budgets, crate packaging, and VSIX packaging. The audited
development runtime is 316,184 bytes and remains within the frozen budgets.

Principal beta.26 SHA-256 values are:

- framework `840129d70fcd146b0e7f37807055e45626c6ab7cfefd9f2c579ccfd9dd3f9521`;
- CLI `02233a87f0da435e97c90ee405c76e948d457d8cab82405d8a056f0d04088481`;
- creator `872b0d0abacaea3f6b059242555ac4b1eb6d062cef30eb9f504db555d23e5fc2`;
- compiler WASM `5e3cf6734981153b07cd4471c03efee4a7aa50409d90b2fd608d8d5cb51b07d8`;
- language service `f38bda86ab22ae88e88cfe4c3a5eac8ec58f8ba9d1e16df45c5047f86ae75eff`;
- LSP `bfe93bbaab3c40cc3a38847eb0b70b3bbc3a06cb4ce0eb1edf866f42dc3f4be4`;
- testing `26561a6bd110bdc53764e6b4c68f79f3a0f50faa9c8b2f0a3406e55d6b99d108`;
- TypeScript authority `adc715af5167351fb4b7eb180df8ea053b20652b273baa03d874938b3ed1a289`;
- Vite integration `b5fd958aa5bc26fc38a4c9074cea95c62ee1511b71a741c449a5583b6e9e4300`;
  and
- VS Code `98098fcd279a15cabe125e419ae5bc75a243d50e212af93ef4c195b9e0bd4213`.

Full-beta completion is 92%. Hosted beta.26 verification/publication and exact
registry proof are next, followed by Presolve.dev adoption and deployment.
Canonical general-purpose Resource/package authoring and final beta hardening
remain after the website is current.
