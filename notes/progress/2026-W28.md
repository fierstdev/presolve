# Progress Log

## 1. Sprint Zero verification

Completed initial repository setup.

Verified:

- `cargo test --workspace`
- `cargo run -p ezc_cli explain fixtures/0001-source-summary/input/Counter.tsx`
- `cargo run -p ezc_cli -- explain fixtures/0001-source-summary/input/Counter.tsx --format json`

Result:

- Rust workspace builds.
- `ezc_core` unit tests pass.
- `ezc_cli` emits text explain output.
- `ezc_cli` emits JSON explain output.
- Source spans are included for decorators, class declarations, and render methods.

Current limitation:

- This is not a real TSX parser yet. It is a source-summary slice that uses lightweight text detection.


## 2. Fixture test added

Added a golden fixture test for `fixtures/0001-source-summary`.

The test reads the fixture input, runs `summarize_source`, renders `explain_text`, and compares it against `expected/explain.txt`.

Purpose:

- prevent explain output drift
- prove fixture wiring
- establish compiler-output testing discipline


## 3. JSON fixture comparison policy

Changed the JSON fixture assertion to compare parsed JSON values instead of raw strings.

Reason:

- editor formatting should not break machine-readable fixture tests
- JSON explain output is a structured data contract
- text explain output remains an exact string contract

Added `serde_json` as a dev-dependency for semantic fixture comparison.


## 4. Core module split

Split `ezc_core` into focused modules:

- `model.rs` for data structures
- `summarize.rs` for source-summary analysis
- `explain.rs` for text and JSON explain rendering
- `lib.rs` for public module wiring and tests

No behavior changed. Existing text and JSON fixture tests continue to pass.

Reason:

- keep compiler concepts separated
- preserve a clean public API through re-exports
- prepare for parser-backend spike without growing `lib.rs`


## 5. CLI explain integration tests

Added integration tests for the `ezc_cli explain` command.

The tests run the compiled CLI binary and verify:

- text explain output matches the golden fixture exactly
- JSON explain output matches the JSON fixture semantically
- the command exits successfully
- stdout is valid UTF-8

This protects the actual user-facing compiler command, not just the core library functions.


## 6. CLI parser command

Added `ezc_cli parse <file>`.

The command uses `ezc_parser::parse_file` and prints parser-adapter output for:

- diagnostics
- classes
- decorators
- properties
- methods
- JSX roots
- simple bindings

Added CLI integration tests for valid and broken TSX fixtures.

This exposes the new Oxc-backed parser adapter through the user-facing CLI while preserving the older `explain` source-summary command.


## 7. CLI parse golden fixtures

Promoted `ezc_cli parse` integration tests from substring smoke checks to full golden fixture comparisons.

Added:

- `fixtures/0001-source-summary/expected/parse.txt`
- `fixtures/0002-broken-tsx/expected/parse.txt`

This locks down the user-visible parser output for both valid and malformed TSX fixtures.


## 8. Minimal component graph

Added the first compiler-owned semantic graph above parsing.

`ezc_parser::ParsedFile` now maps into `ezc_core::ComponentGraph`.

The graph currently captures:

- component class name
- custom element name from `@component`
- route path from `@route`
- state fields from `state(...)`
- methods
- render root JSX element
- render JSX attributes
- render bindings

This is the first transition from parser facts to EdgeZero compiler semantics.


## 9. CLI component graph command

Added `ezc_cli graph <file>`.

The command parses TSX through `ezc_parser`, builds an `ezc_core::ComponentGraph`, and prints compiler-owned semantic output.

Added golden fixtures for:

- valid counter component
- broken TSX fixture

This creates the first observable path from source code to parser adapter to compiler semantic graph to user-facing CLI output.


## 10. Component graph semantic diagnostics

Added the first semantic diagnostics to `ComponentGraph`.

New diagnostics:

- `EZC1001`: component class missing `@component(...)`
- `EZC1002`: component class missing `render()`
- `EZC1003`: render binding references an unknown field
- `EZC1004`: event handler references an unknown method

Parser extraction now captures simple event handler references from JSX attributes, such as `this.increment` from `onClick={() => this.increment()}`.

Added semantic-error fixture coverage.


## 11. HTML fixture comparison policy

Adjusted the HTML CLI fixture test to compare normalized HTML instead of raw strings.

Reason:

- editors may format HTML fixtures across multiple lines
- HTML attribute layout whitespace is not the semantic contract for this slice
- the generated element and attributes are the behavior being protected

The empty broken-TSX HTML fixture remains an exact string comparison.


## 12. JSX child preservation

Extended the parser, component graph, and HTML generator to preserve simple JSX children.

Supported child forms:

- normalized JSX text
- JSX expression bindings such as `{this.count}`

Generated HTML now includes static text and binding comments instead of only an empty root element.

This moves the compiler pipeline from structural placeholder output toward real static HTML generation.


## 13. Minimal template graph

Introduced `TemplateGraph` as a distinct compiler graph between `ComponentGraph` and HTML code generation.

Pipeline is now:

- parse TSX
- build component semantics
- build template semantics
- generate static HTML

HTML codegen now consumes `TemplateGraph` instead of `ComponentGraph`.

This separates component-level meaning from DOM/template-level structure and prepares the compiler for richer template analysis.


## 14. CLI template graph command

Added `ezc_cli template <file>`.

The command prints the compiler-owned `TemplateGraph` between component semantics and generated HTML.

This gives the compiler pipeline a visible inspection point:

- `parse`: parser facts
- `graph`: component semantics
- `template`: template semantics
- `html`: generated artifact


## 15. Nested JSX preservation

Extended parser, component graph, template graph, HTML codegen, and CLI template output to preserve nested JSX elements.

`TemplateGraph` now represents a recursive element tree instead of only a flat root with text and binding children.

Added nested JSX fixture coverage for template and HTML output.


## 16. Template binding ownership

Adjusted `TemplateGraph` binding ownership so elements receive binding metadata only for bindings they directly own.

Nested bindings now attach to the nested element rather than being duplicated on ancestors.

This makes generated static HTML closer to a reactive DOM graph where dynamic update points belong to specific template nodes.


## 17. Stable template node IDs

Added deterministic template node IDs to `TemplateGraph`.

Element nodes and binding nodes now receive stable IDs in source-order traversal.

Generated HTML now includes:

- `data-ez-node` on element nodes
- `<!-- ez-binding:<id>:<expression> -->` comments for binding nodes

These IDs create the anchor layer needed for future hydration, resumability, diagnostics, and runtime update hooks.


## 18. Template manifest

Added a machine-readable template manifest generated from `TemplateGraph`.

The manifest records:

- element node IDs and tag names
- binding node IDs and expressions
- event handlers and the element node they attach to

Added `ezc_cli manifest <file>` and JSON fixture coverage.

This creates the first runtime-facing compiler artifact: HTML contains anchors, and the manifest describes what those anchors mean.


## 19. Build artifact command

Added `ezc_cli build <file> [--out dir]`.

The build command writes:

- `index.html`
- `template.manifest.json`

This creates the first output-directory artifact shape and proves that HTML anchors and the template manifest can be emitted together from one source file.


## 20. Standalone build page

Updated `ezc_cli build` to write a complete browser-openable `index.html`.

The standalone page includes:

- doctype
- HTML/head/body structure
- generated HTML fragment
- embedded inert JSON template manifest

The `html` command still emits only the fragment. The `build` command now emits a full page plus the separate `template.manifest.json` artifact.


## 21. Tiny browser runtime

Added an emitted `runtime.js` artifact to `ezc_cli build`.

The runtime currently:

- reads the embedded template manifest
- verifies element anchors from `data-ez-node`
- verifies binding anchors from `ez-binding` comments
- sets `data-ez-runtime="ready"` or `"error"` on the document root
- exposes boot state as `window.__EDGEZERO__`
- dispatches an `edgezero:ready` browser event

No state evaluation, event execution, or reactive updates are implemented yet.


## 22. Increment actions in template manifest

Completed Slice 6D-A.

The parser now recognizes the narrow `this.<field>++` expression statement shape inside class methods and records it as a parser-owned state update.

`ComponentGraph` maps those parser facts into compiler-owned action semantics:

- method name
- increment operation
- target state field

The template manifest builder now consumes both `ComponentGraph` and `TemplateGraph` so runtime-facing JSON can include compiler-emitted action records next to template nodes and events.

Current limitation: only increment update expressions are recognized. `this.count += 1` remains unsupported until the later mutation-operations slice.


## 23. Browser runtime increment actions

Completed Slice 6D-B.

The emitted browser runtime now consumes manifest actions instead of inspecting source code in the browser.

Runtime boot now:

- normalizes handler references such as `this.increment`
- indexes manifest actions by method name
- indexes element anchors from `data-ez-node`
- initializes numeric runtime state from binding initial values
- attaches click listeners for manifest event records
- executes increment actions against the in-memory state object
- updates text nodes immediately following matching binding comment anchors
- keeps component debug state under `window.__EDGEZERO__`

Added an ignored real-browser integration test that builds the nested counter, serves the build output over HTTP, opens it in local headless Chrome, clicks the generated button twice, and verifies `Count:2`, ready runtime status, no missing anchors, and updated debug state.

Current limitation: event handling is still per-node click listeners only. Delegated events are deferred to Slice 6D-D, and the browser test is an explicit ignored test until Slice 6D-E promotes browser coverage into a permanent gate.


## 24. Explicit runtime state store

Completed Slice 6D-C.

The emitted browser runtime now uses an explicit state store instead of direct ad hoc mutation variables.

The store contains:

- component runtime records
- binding callbacks keyed by component and field
- action records keyed by component and method
- element anchors keyed by template node ID

Action execution now flows through `readField`, `writeField`, and `notifyField`. Bindings register update callbacks by field, so a single state write updates every text binding subscribed to that field.

Added a double-binding counter fixture and expanded the ignored real-browser integration test to verify two `{this.count}` bindings update together after two clicks. The browser probe also checks the debug store maps exposed under `window.__EDGEZERO__.store`.

Current limitation: events are still attached directly per element. Delegated event handling remains the next runtime model change.


## 25. Delegated runtime events

Completed Slice 6D-D.

Parser, component graph, template graph, and manifest event semantics now preserve explicit event types. JSX `onClick` becomes compiler-owned event type `click`, and manifest event records now include:

- template node ID
- event type
- handler reference

Template HTML now emits event-specific marker attributes such as `data-ez-on-click`.

The runtime no longer installs per-element click listeners. It builds an event index by event type and template node ID, installs one delegated document listener per event type, walks from the browser event target up through `data-ez-node` ancestors, and dispatches through the manifest action index.

The double-binding browser probe now exercises a nested click target inside one button and a second button wired to a separate handler. Both route through delegated click dispatch and update the subscribed bindings.

Added compiler diagnostics for unsupported event types and duplicate event declarations on the same element.

Current limitation: only delegated `click` is supported; event modifiers and richer propagation semantics are still deferred.


## 26. Browser runtime e2e gate

Completed Slice 6D-E.

The real-browser runtime probe is now a permanent test instead of an ignored manual check. The suite remains implemented as a Rust integration harness so it can reuse the compiler fixture build path without adding a JavaScript e2e dependency.

The browser gate now:

- builds the double-binding counter fixture deterministically
- serves the generated page through an in-test static server
- launches real Chrome through `EDGEZERO_CHROME` or standard local paths
- verifies standalone boot, manifest anchors, initial counter rendering, two increments, nested delegated click dispatch, shared binding updates, and debug store shape
- fails on unexpected `[EdgeZero]` runtime console errors

Added local entry points:

- `pnpm test:e2e`
- `pnpm test:e2e:headed`
- `just e2e`
- `just e2e-headed`

Added `e2e/browser/README.md` to document the permanent gate and `.github/workflows/ci.yml` to run both the Rust workspace tests and the explicit browser e2e job with Chrome installed in CI.

Current limitation: the headed aliases run the same deterministic Chrome dump-DOM harness as the default e2e command. A truly interactive headed runner remains deferred until a DevTools or Playwright-based harness is worth the dependency.


## 27. String state literals

Completed Slice 6E-A.

State initializers now recognize string literals in addition to numeric literals. The parser records the semantic string value rather than the source-quoted spelling, so:

```tsx
name = state("Austin & <Zero>");
```

is carried forward as `Austin & <Zero>`.

The existing template value path now supports string bindings:

- component state fields preserve the unquoted string value
- template binding nodes carry that value as their initial value
- static HTML escapes the rendered text as `Austin &amp; &lt;Zero&gt;`
- manifest JSON serializes the value as a JSON string
- browser runtime initialization uses the manifest value directly instead of coercing every initial value with `Number(...)`

Added `fixtures/0006-string-state` plus parser, core, CLI fixture, and real-browser coverage. The browser e2e suite now boots both the delegated counter fixture and the string-state fixture, verifying `window.__EDGEZERO__` preserves the string state value.

Current limitation: initial values are still represented by the temporary string-capable scalar path. The compiler-owned typed serializable value model remains deferred to Slice 6E-D.


## 28. Boolean state literals

Completed Slice 6E-B.

State initializers now recognize boolean literals:

```tsx
enabled = state(true);
disabled = state(false);
```

The initial-value path now uses small typed scalar enums instead of plain `Option<String>`:

- parser: `ParsedStateInitialValue`
- compiler/component graph: `StateInitialValue`
- template binding nodes: typed `initial_value`
- manifest binding nodes: typed `initial_value`

Boolean state values render as `true` / `false` in static HTML and serialize as JSON booleans in the manifest, while existing numeric and string manifest behavior remains stable.

Added `fixtures/0007-boolean-state` plus parser, core, CLI fixture, and real-browser coverage. The browser e2e suite now boots the delegated counter fixture, string-state fixture, and boolean-state fixture, verifying `window.__EDGEZERO__` preserves booleans as strict `true` and `false`.

Current limitation: the typed scalar enum is intentionally narrow and should be extended/renamed into the roadmap's `SerializableValue` shape in Slice 6E-D before arrays and objects.


## 29. Null state literals

Completed Slice 6E-C.

State initializers now recognize null literals:

```tsx
selection = state(null);
```

Null values are preserved through the typed scalar path:

- parser: `ParsedStateInitialValue::Null`
- compiler/component graph: `StateInitialValue::Null`
- template binding nodes: typed null initial value
- manifest binding nodes: JSON `null`

Static HTML renders null bindings as empty text, and the browser runtime now formats null binding updates as empty text instead of `"null"`. Runtime initialization also preserves explicit null in `window.__EDGEZERO__` debug state.

Added `fixtures/0008-null-state` plus parser, core, CLI fixture, and real-browser coverage. The browser e2e suite now boots the delegated counter fixture, string-state fixture, boolean-state fixture, and null-state fixture.

Current limitation: the scalar value type is still named around initial state. Slice 6E-D should stabilize it as the compiler-owned `SerializableValue` model before arrays and objects.


## 30. GitHub Actions Chrome sandbox repair

After pushing through Slice 6E-B, GitHub Actions failed both the `Rust workspace` and `Browser e2e` jobs in `crates/ezc_cli/tests/runtime_browser.rs`.

The failure was Chrome startup, not compiler/runtime behavior:

```text
No usable sandbox!
```

The browser harness now routes Chrome launch arguments through one helper and adds `--no-sandbox` plus `--disable-dev-shm-usage` only when `CI` is present. Local Chrome runs keep their previous arguments.

Verified the normal local path and the CI argument path with:

- `cargo fmt --all --check`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `cargo test --workspace`
- `pnpm test:e2e`

Current limitation: this fix is validated locally by exercising the CI argument path, but the GitHub runner itself still needs a push to confirm the hosted environment.


## 31. Typed serializable value model

Completed Slice 6E-D.

The temporary initial-state scalar type names are now stabilized around the roadmap's serializable value model:

- parser: `ParsedSerializableValue`
- compiler/component graph: `SerializableValue`
- template binding nodes: typed `initial_value`
- manifest binding nodes: typed `initial_value`

The compiler-owned `SerializableValue` model currently covers:

```rust
SerializableValue::Null
SerializableValue::Boolean(bool)
SerializableValue::Number(String)
SerializableValue::String(String)
```

No fixture output changed in this slice. The existing null, boolean, number, and string fixtures continue to verify parser extraction, static HTML rendering, manifest JSON semantics, and real-browser initialization.

Current limitation: the serializable value model is still primitive-only. Arrays and objects remain intentionally deferred, and Slice 6F starts mutation operation expansion before broadening values further.


## 32. Verification gate cleanup

Resolved the two stale verification exceptions in `AGENT_HANDOFF.md`.

`cargo clippy --workspace --all-targets -- -D warnings` now passes. The cleanup covered:

- replacing the SWC parser spike's local `Arc<SourceMap>` with `Rc<SourceMap>`
- adding `#[must_use]` to public pure compiler/codegen helpers
- adding the manifest JSON panic documentation requested by clippy
- reducing oversized CLI/component graph helper functions by extracting smaller print/diagnostic helpers
- tightening minor pedantic style warnings in source summary and CLI output formatting

Installed `just` in the local shell via `cargo install just --locked` and verified the repository recipe directly with `just e2e`.

One failed `just e2e` attempt happened while it was run in parallel with `cargo test --workspace`; both commands launched the browser suite at the same time and contended on the same Chrome profile `SingletonLock` files. Running the browser gate sequentially passes.

Verified:

- `just e2e`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `pnpm test:e2e`


## 33. Decrement mutation operation

Completed Slice 6F-A.

The compiler/runtime action pipeline now recognizes decrement update expressions:

```tsx
this.count--;
```

Added operation variants across the pipeline:

- parser: `ParsedStateOperation::Decrement`
- compiler/component graph: `StateOperation::Decrement`
- template manifest: JSON operation `"decrement"`
- browser runtime: numeric decrement execution through the existing delegated action path

Added `fixtures/0009-decrement-counter` with expected HTML, template, and manifest outputs. The browser e2e suite now includes a real Chrome probe that starts at `Count:2`, clicks the decrement button, and verifies both rendered text and `window.__EDGEZERO__` debug state update to `1`.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: add-assign, subtract-assign, direct assignment, boolean toggle, and multi-step action plans remain deferred to later 6F sub-slices.


## 34. Add-assign and subtract-assign literal operations

Completed Slice 6F-B.

The compiler/runtime action pipeline now recognizes assignment update expressions with literal operands:

```tsx
this.count += 2;
this.count -= 3;
```

Added typed operand variants across the pipeline:

- parser: `ParsedStateOperation::AddAssign(ParsedSerializableValue)` and `ParsedStateOperation::SubtractAssign(ParsedSerializableValue)`
- compiler/component graph: `StateOperation::AddAssign(SerializableValue)` and `StateOperation::SubtractAssign(SerializableValue)`
- template manifest: JSON operations `"add_assign"` and `"subtract_assign"` with typed `operand`
- browser runtime: numeric operand execution through the existing delegated action path

Added `fixtures/0010-add-subtract-assign` with expected HTML, template, and manifest outputs. The existing `fixtures/0001-source-summary` counter now also emits an `add_assign` action because it already used `this.count += 1`; this is now supported behavior rather than an ignored update.

The browser e2e suite now includes a real Chrome probe that starts at `4`, clicks the add button to reach `6`, clicks the subtract button to reach `3`, and verifies both rendered bindings plus `window.__EDGEZERO__` debug state.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: direct assignment, boolean toggle, and multi-step action plans remain deferred to later 6F sub-slices.


## 35. Direct literal assignment

Completed Slice 6F-C.

The compiler/runtime action pipeline now recognizes direct assignment expressions with literal values:

```tsx
this.count = 0;
```

Added typed assignment variants across the pipeline:

- parser: `ParsedStateOperation::Assign(ParsedSerializableValue)`
- compiler/component graph: `StateOperation::Assign(SerializableValue)`
- template manifest: JSON operation `"assign"` with typed `operand`
- browser runtime: direct writes through the existing delegated action path

Added `fixtures/0011-direct-assignment` with expected HTML, template, and manifest outputs. The browser e2e suite now includes a real Chrome probe that starts at `Count:5`, clicks reset, and verifies rendered text updates to `Count:0`.

Current runtime detail: assigned numeric literals are written from the manifest operand as-is. Because the current primitive value model serializes `SerializableValue::Number(String)` as a JSON string, the debug state for `this.count = 0` is `"0"` while rendered text remains `0`. Changing numeric manifest semantics remains a later value-model decision.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: boolean toggle and multi-step action plans remain deferred to later 6F sub-slices.


## 36. Boolean toggle pattern

Completed Slice 6F-D.

The compiler/runtime action pipeline now recognizes the explicit boolean toggle pattern:

```tsx
this.enabled = !this.enabled;
```

Added closed toggle variants across the pipeline:

- parser: `ParsedStateOperation::Toggle`
- compiler/component graph: `StateOperation::Toggle`
- template manifest: JSON operation `"toggle"` with no operand
- browser runtime: boolean-only toggle execution through the existing delegated action path

Added `fixtures/0012-boolean-toggle` with expected HTML, template, and manifest outputs. The browser e2e suite now includes a real Chrome probe that starts at `Enabled:false`, clicks to `Enabled:true`, clicks again to `Enabled:false`, and verifies `window.__EDGEZERO__` debug state stays boolean.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: multi-step action plans remain deferred to Slice 6F-E.


## 37. Multi-step action plans

Completed Slice 6F-E.

One component method can now contain multiple supported state updates, and the browser runtime executes every step in source order:

```tsx
this.count += 2;
this.count--;
this.count = 8;
this.count++;
this.enabled = !this.enabled;
```

The manifest action shape remains flat. Repeated `method` entries now represent an ordered action plan for that method, and runtime registration groups those entries into arrays instead of overwriting earlier steps.

Added `fixtures/0013-multi-step-action` with expected HTML, template, and manifest outputs. The browser e2e suite includes a real Chrome probe that starts at `Count:1Enabled:false`, clicks once, and verifies ordered execution reaches `Count:9Enabled:true` with debug state `{ count: 9, enabled: true }`.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser multi_step_action_runs_all_steps_in_a_real_browser -- --nocapture`

Current limitation: action steps remain limited to the closed 6F operation set; manifest/runtime contract versioning is deferred to Slice 6G.


## 38. Runtime contract versioning and diagnostics

Completed Slice 6G.

The compiler/runtime boundary is now explicit and versioned:

- template manifests serialize top-level `"schema_version": 1`
- browser runtime exposes `window.__EDGEZERO__.runtime_version`
- browser runtime exposes `window.__EDGEZERO__.supported_schema_version`
- browser runtime exposes `window.__EDGEZERO__.diagnostics`
- fatal boot failures set `data-ez-runtime="error"` and still expose diagnostics

Runtime boot now rejects missing, invalid, or unsupported manifests with stable diagnostic codes:

- `EZR_MISSING_MANIFEST`
- `EZR_INVALID_MANIFEST_JSON`
- `EZR_UNSUPPORTED_SCHEMA`

The runtime also reports stable nonfatal/runtime operation diagnostics for missing anchors, unresolved events/actions, and invalid state operations. Compatibility rules are documented in `docs/runtime-contract.md`.

Added browser coverage that intentionally boots pages with no manifest, malformed JSON, and a future schema version. Each probe verifies `data-ez-runtime="error"`, fatal diagnostics, runtime version exposure, and supported schema exposure.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`

Current limitation: schema compatibility is exact-match only; there is no manifest migration layer yet.


## 39. Preserve static JSX attributes

Completed Slice 7A.

JSX attributes are now parsed as structured values instead of display strings:

- boolean presence attributes
- string literal attributes
- expression attributes
- spread attributes
- unsupported complex JSX attribute values

The compiler graph carries those structured render attributes forward. `TemplateGraph` now preserves ordinary string and boolean static attributes in source order, before compiler-owned `data-ez-*` attributes. Static HTML generation emits escaped string values and boolean presence attributes without `=""`.

Added duplicate/static attribute diagnostics:

- `EZC1007` duplicate attribute on one element
- `EZC1008` expression attribute value unsupported in 7A
- `EZC1009` spread attribute unsupported in 7A
- `EZC1010` complex JSX attribute value unsupported in 7A

Added `fixtures/0014-static-attributes` covering string attributes, boolean attributes, ARIA attributes, and escaping quotes, ampersands, and angle brackets.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`

Current limitation: dynamic attribute bindings remain deferred to Slice 7B; `className` and `htmlFor` normalization policy remains undecided.


## 40. Dynamic attribute bindings

Completed Slice 7B.

Supported JSX expression attributes of the form:

```tsx
<button disabled={this.disabled} title={this.label}>
```

The compiler now lowers supported state-backed expression attributes into template binding nodes with stable IDs, initial primitive state values, and manifest metadata identifying the binding target:

```json
{ "kind": "binding", "target": "attribute", "attribute": "disabled" }
```

Static HTML generation applies the initial binding value. Boolean attributes such as `disabled` are omitted when false and emitted as presence attributes when true; string attributes render escaped initial text.

The browser runtime skips comment-anchor validation for attribute binding nodes and registers binding callbacks against the existing component state store. Updates apply in place to the already-rendered element:

- boolean attributes toggle attribute presence
- property-backed attributes such as `disabled` also update the DOM property
- string attributes are set or removed from the element

Added `fixtures/0015-dynamic-attributes` covering `disabled={this.disabled}`, `title={this.label}`, a click action that toggles/assigns state, and a text binding sharing the same state field. The browser e2e probe verifies initial attribute hydration, click-driven disabled/title/text updates, manifest target metadata, and debug state.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser dynamic_attributes_update_in_a_real_browser -- --nocapture`

Current limitation: dynamic attributes are limited to `this.<stateField>` primitive state bindings; arbitrary expressions, method calls, spreads, arrays, objects, and `className`/`htmlFor` normalization remain deferred.


## 41. Source spans on template nodes and edges

Completed Slice 7C.

Template semantics are now traceable back to source:

- parser JSX text children carry spans anchored to the first rendered text character
- parser JSX binding children carry expression-container spans
- parser event handlers carry source attribute spans
- component graph render elements, attributes, events, text children, and binding children preserve those spans
- template graph elements, source-owned attributes, text children, and binding children preserve those spans
- generated `data-ez-bindings` attributes are explicitly unspanned and shown as `@ generated`

Runtime manifests intentionally remain unchanged. Source metadata is exposed through development output instead:

- `parse` child output now shows `@ line:column span=start..end`
- `graph` child output now shows `@ line:column span=start..end`
- `template` output now shows `@ file:line:column span=start..end` for every source-owned template node/edge

Added focused coverage for nested element and binding spans in `fixtures/0004-nested-jsx`, plus parser assertions for event/text/binding spans in the counter and nested JSX fixtures. Regenerated template fixtures now record source locations across existing template cases.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: source spans are not included in runtime manifest JSON; adding optional development manifest metadata remains a future decision.


## 42. JSX fragments

Completed Slice 7D.

JSX fragments are now represented explicitly through the compiler pipeline:

- parser roots can be `ParsedJsxNode::Fragment`
- parser children can be `ParsedJsxChild::Fragment`
- component graph carries `RenderFragment`
- template graph carries root fragments and nested `TemplateChild::Fragment` nodes with stable compiler IDs and source spans

Static HTML generation treats fragments as transparent containers. Fragment children are emitted as siblings without an artificial wrapper element. Runtime manifests also treat fragments as transparent and collect only element, binding, and event records from their children.

Added `fixtures/0016-fragments` with a root fragment and nested fragment:

```tsx
<>
  <h1>Title</h1>
  <>
    <p>Status: {this.label}</p>
    <span>Done</span>
  </>
</>
```

The fixture verifies wrapperless sibling HTML, template fragment IDs/spans, and manifest output that omits fragment nodes. Browser coverage verifies the built page boots, renders `h1`, `p`, and `span` as direct sibling content, preserves element node IDs, hydrates the binding text, and emits no fragment wrapper DOM nodes.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser fragments_preserve_sibling_identity_in_a_real_browser -- --nocapture`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: fragment IDs are compiler/template-only and are not serialized into runtime manifests until a range-anchor/runtime identity use case requires them.


## 43. Conditional rendering

Completed Slice 7E.

Conditional rendering now flows through the parser, component graph, template graph, static HTML, manifest, and browser runtime.

Supported forms:

```tsx
{this.enabled ? <span>On</span> : <span>Off</span>}
{this.enabled && <span>On</span>}
```

The compiler now emits a conditional template child with:

- a conditional node ID
- stable start/end boundary IDs
- the `this.<stateField>` condition expression
- serializable initial condition value
- true and false branch template children

Static HTML renders the initial branch from state and wraps it in `ez-conditional-start` / `ez-conditional-end` comments. Runtime manifests serialize a conditional node with branch HTML snippets. The runtime collects conditional anchors at boot, initializes the backing boolean state from the manifest, registers a binding callback for the condition field, and replaces only the DOM nodes between the two comments when the state toggles.

Logical-and shorthand reuses the same model with an empty false branch, so the false update leaves the anchored range empty and the next true update restores the branch HTML.

Added fixtures:

- `fixtures/0017-conditional-rendering`
- `fixtures/0018-logical-and-conditional`

Added browser coverage:

- ternary condition switches from `On` to `Off` and back without a full component rerender
- logical-and condition switches from `On` to an empty branch and back
- branch element indexes refresh after replacement

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `pnpm test:e2e`
- `just e2e`

Current limitation: conditional branch snippets are replaced as static HTML. Bindings, events, and nested dynamic behavior inside swapped-in branches are not re-registered yet.


## 44. Keyed list semantic model

Completed Slice 7F-A.

The compiler now represents the constrained keyed-list form through parser, component, and template semantics:

```tsx
{this.items.map((item, index) => <li key={item.id}>{index}: {item.label}</li>)}
```

Each list node preserves:

- iterable dependency (`this.items`)
- item variable (`item`)
- optional index variable (`index`)
- key expression (`item.id`)
- source span and item template

`key` is now recognized as structural list metadata rather than an unsupported ordinary JSX attribute. The parent template element records the iterable as a binding dependency, while the list item template remains inspectable through the `parse`, `graph`, and `template` commands.

Added `fixtures/0019-keyed-list-semantics` with golden parser, component-graph, and template-graph output. Core coverage also proves that the primitive-only value model does not render list items or manifest records yet: the initial HTML is the empty parent `<ul>` and the manifest contains only that parent element.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: keyed lists accept only an expression-bodied `.map` callback with one or two identifier parameters and a JSX element carrying `key={...}`. Runtime manifest records, reconciliation, and key diagnostics remain deferred to 7F-C and 7F-D.


## 45. Static initial keyed-list rendering

Completed Slice 7F-B.

`ParsedSerializableValue` and `SerializableValue` now support recursive array literals composed of serializable values. A keyed list can therefore render its initial state at compile time:

```tsx
labels = state(["North", "South"]);

render() {
  return (
    <ol>
      {this.labels.map((label, index) => <li key={label}>{index}: {label}</li>)}
    </ol>
  );
}
```

Static HTML expands the item template once per initial array item. Within that template, the exact item variable and optional index variable resolve to their initial values. Generated element and binding IDs are suffixed with the deterministic array index, such as `n2:0` and `n2:1`, so expanded instances do not duplicate compiler node IDs.

Added `fixtures/0020-static-keyed-list` with golden HTML and template output. Parser coverage verifies the array state model; core coverage verifies array preservation, item/index expansion, deterministic IDs, and the intentional manifest boundary.

The runtime manifest still contains only the static parent element. No list state, list anchors, or reconciliation behavior is emitted yet, so browser behavior remains unchanged in this slice.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: static initial list binding evaluation supports only the item identifier and optional index identifier. Object array entries, member expressions such as `item.id`, and all dynamic keyed updates remain deferred to 7F-C and later.


## 46. Keyed list reconciliation

Completed Slice 7F-C.

List templates now emit compiler-owned DOM range anchors:

```html
<!-- ez-list-start:n5:this.labels -->
<li data-ez-node="n7:North">...</li>
<!-- ez-list-end:n6 -->
```

The template manifest records each list's iterable, initial array, item/index variables, key expression, start/end IDs, item-root template ID, and placeholder HTML for inserting a new item root. Static instances use a deterministic `templateNodeId:key` DOM ID, so the runtime can build its initial key-to-element map from the rendered page.

When a recognized array assignment changes the list state, the browser runtime reconciles the list range by:

- retaining existing roots for unchanged keys
- inserting roots for new keys from the compiler-provided template
- moving retained roots into the new key order with `insertBefore`
- removing roots for keys absent from the new array

Added `fixtures/0021-keyed-list-reconciliation` and browser coverage that begins with `North` and `South`, reorders them while inserting `East`, then trims to `East` alone. The probe verifies that moved `North` and `South` roots retain object identity, `East` is inserted, stale roots are removed, and `East` remains the same DOM element through deletion of the other items.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: runtime reconciliation supports one item root element, direct primitive item keys or index keys, and direct item/index text substitution. Key diagnostics, object/member-expression key evaluation, list-item event hydration, and nested dynamic list behavior remain deferred to 7F-D and later.


## 47. Key diagnostics

Completed Slice 7F-D.

The parser now retains a valid `.map()` JSX callback as a list node even when its item root has no `key` attribute. This gives semantic analysis enough information to explain why a list cannot use retained-node reconciliation instead of degrading the expression to a generic binding.

The component graph now emits:

- `EZC1011` for a missing `key={...}` attribute
- `EZC1012` for an iteration-index key
- `EZC1013` for unsupported expressions, including member keys such as `item.id`
- `EZC1014` for duplicate statically-known primitive initial keys

For the current primitive-list runtime contract, the only supported form is a direct item key such as `key={label}`. The explicit boundary avoids silent index-identity fallback and establishes the diagnostic contract before recursive object values introduce supported member keys.

Added `fixtures/0022-keyed-list-diagnostics`, which covers all four failure modes through the `ezc_cli graph` command. The prior `item.id` semantic fixture now intentionally emits `EZC1013`.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: duplicate diagnostics cover only compile-time-known primitive initial arrays. Object values, member-expression keys, nested dynamic list behavior, and list-item event hydration remain future work.


## 48. Recursive object serializable values

Completed Slice 7F-E.

`ParsedSerializableValue` and `SerializableValue` now include recursive ordered object maps. The parser accepts static identifier, string, and numeric object keys whose values are recursively serializable primitives, arrays, or objects. This applies to both `state({...})` initial values and literal assignment operands.

The component graph preserves object values, and the template manifest serializes them as normal JSON objects in binding initial values and action operands. This establishes object data as part of the compiler/runtime artifact contract before member-expression evaluation begins.

Added `fixtures/0023-recursive-object-values`, containing nested object and array state plus an object assignment action. Parser coverage validates the recursive parser model; core coverage validates component and manifest preservation; the CLI manifest fixture protects the emitted JSON contract.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: object values are preserved as data, but JSX/list member expressions such as `item.id` are not evaluated yet. Object spreads, computed keys, shorthand references, methods, accessors, and arbitrary property expressions are intentionally unsupported.


## 49. Member-expression evaluation

Completed Slice 7F-F.

The compiler now resolves non-empty dot-member paths rooted at a list item variable against compile-time serializable object values. Static list templates can render `item.label` and nested paths such as `item.details.region`; member-derived keys such as `item.id` now produce the same stable initial DOM identity contract as direct primitive keys.

Key diagnostics now distinguish unsupported expressions from member paths that cannot produce a primitive key. `EZC1015` reports a missing or non-primitive initial member value, including the initial item index. Empty object lists remain valid so dynamic object data can use the member-key form in the next runtime slice.

Added `fixtures/0024-static-object-keyed-list` with nested member bindings and `item.id` keys. Core and CLI coverage verify static HTML, key-scoped IDs, and the manifest contract. The key diagnostics fixture now covers an explicit missing object member.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: member paths resolve only for static compiler rendering. Runtime insertion still substitutes only direct item/index text, and runtime reconciliation still falls back to index identity for object keys until 7F-G.


## 50. Object keyed-list reconciliation

Completed Slice 7F-G.

The browser runtime now mirrors the compiler's constrained dot-member evaluator for list item values. It derives object keys such as `item.id` when locating initial DOM roots and when reconciling array assignments. This preserves object-row identity across reordering and deletion instead of falling back to index identity.

When inserting a new object row from a list manifest template, the runtime walks its binding comments and materializes member text such as `item.label` and `item.details.region` before insertion. This keeps compiler-generated templates sufficient for new object roots without introducing a client-side template language.

Added `fixtures/0025-object-keyed-list-reconciliation` plus a browser probe that begins with North and South objects, reorders them while inserting East, then trims to East. It verifies retained North/South identity, inserted East member text, retained East identity, removal of stale rows, manifest member-key metadata, and runtime debug state.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: list reconciliation retains roots by key, but it does not refresh member text on a retained row when that row's object payload changes. Dynamic item attributes, events, and nested dynamic behavior remain deferred to 7F-H.


## 51. Dynamic list-item behavior

Completed Slice 7F-H and the 7F language-foundation sequence.

Keyed list instances now carry their current item/index payload while preserving the compiler-generated root element for unchanged keys. On an array assignment, retained rows refresh direct item/index bindings and member paths such as `item.label` and `item.details.region`. Scoped binding comments now have compiler-owned end anchors, so text updates cannot overwrite the static text that follows them.

List template attributes may use the same constrained item/index/member expressions. The compiler emits element-local binding metadata, and the runtime applies updates through its existing attribute update path. Initial and runtime-inserted list roots also discover delegated click action metadata; removed roots unregister that metadata before leaving the DOM.

Added `fixtures/0026-dynamic-list-item-behavior`. Its browser probe verifies initial list actions, retained root identity, refreshed member text and attributes, inserted item attributes, and click actions for both inserted and retained items.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: list refresh supports only direct item/index variables and non-empty dot-member paths for text and attributes. Nested dynamic conditionals and their event/binding hydration remain outside this slice.


## 52. Global semantic IDs

Completed ASM-1, the first Application Semantic Model transition slice.

Added the typed `SemanticId` value and assigned readable component-scoped identities to component graph entities and rendered templates. A valid component uses its `@component(...)` name as the global root, producing IDs such as:

```text
component:x-counter
component:x-counter/state:count
component:x-counter/method:increment
component:x-counter/action:increment:0
component:x-counter/template:render
```

Invalid components without `@component(...)` use their class name only as a deterministic fallback for diagnostics and later validation. The compiler does not yet publish semantic IDs in HTML, runtime manifests, or CLI output, so this foundation does not alter existing backend contracts.

Core tests now verify typed derivation, fallback behavior, graph assignment, and stability when two component declarations are reordered. This proves the IDs are not tied to template allocation order, which remains backend-local.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: only named component-level entities and rendered templates have semantic IDs. Ownership edges, nested template semantic IDs, cross references, provenance, validation, and `ez asm` inspection remain separate ASM slices.


## 53. Semantic ownership

Completed ASM-2, establishing direct ownership for the semantic entities introduced in ASM-1.

Added the typed `SemanticOwner` relationship so the compiler now represents a minimal semantic hierarchy:

```text
application
  component:x-counter
    state:count
    method:increment
      action:increment:0
    template:render
```

Components are application roots. State fields, methods, and rendered templates carry their owning component ID. Action steps carry the ID of their defining method. The typed application-root case prevents child entities from silently using an absent owner while preserving the absence of a synthetic application semantic ID before the ASM shell exists.

The ownership data remains compiler metadata only. No HTML, manifest, runtime, or CLI artifact changed. Nested template nodes still lack semantic IDs, so their ownership is deliberately not inferred from backend-local `n*` values.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: ownership is carried by current graph structs rather than a centralized ASM relation table. Cross-reference resolution, source provenance, validation, and semantic query APIs remain subsequent ASM slices.


## 54. Cross-reference resolution

Completed ASM-3, adding the first resolved semantic relation graph.

`ComponentGraph.references` now records directed edges using a relation kind plus source and target `SemanticId` values. The initial resolved relationship set is deliberately small and concrete:

```text
component:x-counter/action:increment:0
  --ActionState-->
component:x-counter/state:count

component:x-counter/event:click:0
  --EventMethod-->
component:x-counter/method:increment
```

Event handlers now receive deterministic component-scoped IDs in render traversal order and are owned by the rendered template. Action references resolve only when their target state field exists; event references resolve only when their `this.<method>` handler target exists. Existing unresolved-reference diagnostics remain unchanged, and unresolved attempts are not materialized as partial edges.

The relation graph remains compiler metadata only. HTML, manifests, runtime behavior, and existing CLI output remain unchanged.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: only action-to-state and event-to-method relations are represented. There is no source provenance, centralized relation index, reverse query, validation pass, or CLI inspection yet.


## 55. Source provenance

Completed ASM-4, adding canonical source provenance for the current semantic graph.

`ComponentGraph.provenance` is now a `SemanticId`-keyed registry of `SourceProvenance { path, span }`. It records the source origin of components, state fields, methods, action steps, rendered templates, and event handlers. Template nodes consume the same registry entry rather than introducing a separate source-location model.

The parser now preserves the AST span of every recognized state update. Action provenance therefore identifies the update or assignment expression itself. Resolved `ActionState` and `EventMethod` references carry the provenance of their source action or event handler, while target provenance remains available from the registry.

The compiler continues to keep provenance metadata out of HTML, template manifests, runtime artifacts, and CLI output. This makes source tracing available to future ASM tooling without an immediate backend schema change.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_parser`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: provenance is per parsed file and covers only currently semanticized entities and resolved references. Multi-file aggregation, source remapping, generated-code origins, queries, validation, and CLI inspection remain later ASM work.


## 56. ApplicationSemanticModel shell

Completed ASM-5, establishing the application-level semantic entry point.

`ApplicationSemanticModel` now assembles the current compiler outputs into one model: components, rendered templates, diagnostics, a centralized ownership map, resolved references, and the semantic provenance registry. It is built from the existing component and template graph builders, so the compiler's HTML, manifest, runtime, and CLI behavior remains unchanged.

The ownership map includes component roots, state fields, methods, action steps, event handlers, and rendered templates. The ASM shell therefore gives future compiler passes and developer tools one stable semantic input without forcing the existing backends to migrate early.

Verified:

- `cargo fmt --all --check`
- `cargo test -p ezc_core`
- `cargo test -p ezc_cli --test explain`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `CI=true cargo test -p ezc_cli --test runtime_browser -- --nocapture`
- `pnpm test:e2e`
- `just e2e`

Current limitation: the ASM represents one parsed file and remains a data shell. Query APIs, validation, multi-file aggregation, source remapping, and CLI inspection are separate follow-on slices.


## 57. Compiler query API

Completed ASM-6. `ApplicationSemanticModel` now provides read-only entity, component, template, ownership, provenance, and directed reference queries. Results borrow model data and preserve deterministic construction order. Existing compiler backends remain unchanged.

Verified: `cargo fmt --all --check`, `cargo test -p ezc_core`, and `cargo clippy --workspace --all-targets -- -D warnings`.

Next: ASM-7 validation.


## 58. ASM validation

Completed ASM-7. `validate_application_semantic_model` now checks entity ownership, provenance completeness, owner targets, reference endpoints, and reference-source provenance. It returns deterministic `EZASM1001` through `EZASM1006` diagnostics without changing compiler backend behavior.

Verified: `cargo fmt --all --check`, `cargo test -p ezc_core`, and `cargo clippy --workspace --all-targets -- -D warnings`.

Next: ASM-8 `ez asm` CLI.


## 59. ASM CLI

Completed ASM-8. `ezc asm <file>` now reports deterministic application-semantic counts and validation results from the ASM shell.

Verified: `cargo fmt --all --check`, `cargo test -p ezc_cli --test explain`, and `cargo clippy --workspace --all-targets -- -D warnings`.


## 60. Dependency analysis

Completed Era III-A. Added a reusable `AnalysisPass` interface and `DependencyAnalysisPass`, which materializes deterministic forward dependencies and reverse dependents from resolved ASM references.

Verified: `cargo fmt --all --check`, `cargo test -p ezc_core`, and `cargo clippy --workspace --all-targets -- -D warnings`.


## 61. Constant evaluation

Completed Era III-B. `ConstantEvaluationPass` exposes known serializable initial state and literal action operands by semantic ID. It does not interpret arbitrary JavaScript.

Verified: formatting, core tests, and clippy.


## 62. Dead semantic analysis

Completed Era III-C. `DeadSemanticAnalysisPass` treats `render` and event-target methods as live, reporting remaining methods and their actions as conservative unreferenced candidates.

Verified: formatting, core tests, and clippy.


## 63. Optimization planning

Completed Era III-D. `OptimizationPlanningPass` turns dead candidates into non-mutating recommendations with source provenance. No compiler transformation runs in this slice.

Verified: formatting, core tests, and clippy.


## 64. Explainability

Completed Era III-E. `ExplainabilityPass` reports deterministic totals for components, dependencies, constants, optimization recommendations, and validation diagnostics.

Verified: formatting, core tests, and clippy.


## 65. Resume plan

Completed Era IV-A. `ResumePlan` inventories each component's state and delegated event semantic IDs from the ASM.

Verified: formatting, core tests, and clippy.


## 66. Resume manifest

Completed Era IV-B. `ResumeManifest` schema v1 serializes the resume plan's component, state, and event references as a compiler artifact.

Verified: formatting, core tests, and clippy.


## 67. Serializable instances

Completed Era IV-C. `SerializableInstance` carries a component semantic ID and its known serializable initial state values.

Verified: formatting, core tests, and clippy.


## 68. Resume diagnostics

Completed Era IV-D. Resume validation reports missing component instances and planned state slots absent from an instance payload.

Verified: formatting, core tests, and clippy.


## 69. Resume boot plan

Completed Era IV-E. `ResumeBootPlan` combines manifest, serializable instances, and diagnostics to determine zero-replay eligibility without changing runtime boot.

Verified: formatting, core tests, and clippy.


## 70. Lazy action chunks

Completed Era IV-F. Each delegated event now has a compiler plan containing the action IDs for its handler method.

Verified: formatting, core tests, and clippy.


## 71. Resume explainability

Completed Era IV-G. `explain_resume` emits deterministic component, instance, chunk, eligibility, and diagnostic totals.

Verified: formatting, core tests, and clippy.


## 72. Route graph

Completed Era V-A. `RouteGraph` maps declared route paths to component semantic IDs as metadata only.

Verified: formatting, core tests, and clippy.


## 73. Module graph

Completed Era V-B. `ModuleGraph` groups component semantic IDs by source provenance path, ready for multi-file ASM aggregation.

Verified: formatting, core tests, and clippy.


## 74. Layout graph foundation

Completed Era V-C. `LayoutGraph` maps every declared route to its component semantic ID and an explicit empty layout chain. Layout declarations are not yet a language feature, so this establishes the consumer contract without inventing syntax.

Verified: formatting, core tests, and clippy.


## 75. Compilation unit frontend

Completed C1-A, beginning the compiler-first roadmap revision. `CompilationUnit` is now the deterministic application input for `ezco`: it accepts already parsed files or parses an in-memory set of sources, then sorts them by path before semantic construction.

`build_application_semantic_model_for_unit` aggregates the existing component graph, template graph, diagnostics, references, provenance, and ownership data across every file in the unit. The established single-file builder remains available as a compatibility wrapper.

This is deliberately a frontend boundary, not a claim of module semantics. Imports, exports, module edges, duplicate identities, and symbols are not yet modeled. The previous module graph remains a provenance grouping of components, while C1-B will introduce actual import/export extraction and module-edge resolution.

The roadmap is now compiler-first: complete the multi-file frontend, symbol resolution, typed semantic lowering, and canonical multi-file ASM before resuming additional application-platform graph work.

Verified: `cargo fmt --all --check`, `cargo test -p ezc_core`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace`.


## 76. Import/export extraction and module edges

Completed C1-B. The parser now preserves import declarations, import specifiers, exports, re-exports, default exports, and export-all declarations as structured frontend data on each `ParsedFile`. Exported component classes remain visible to existing component lowering.

`ModuleGraph` no longer infers modules by grouping component provenance. It now derives module nodes and import/re-export edges directly from a `CompilationUnit`. Relative specifiers resolve only against paths already present in that unit, including TypeScript/JavaScript extension and `index` candidates. Package specifiers are explicitly external; missing relative targets remain unresolved.

This establishes a real file-relationship layer without prematurely resolving imported names or package semantics. C2-A will add local module symbol tables before cross-module binding resolution.

Verified: parser tests, core tests, formatting, clippy, and the workspace suite.


## 77. Local module symbol tables

Completed C2-A. `SymbolTable` now indexes every component graph built from a `CompilationUnit` on a per-module basis. It records component class names plus class-qualified state and method names, each linked to its existing semantic ID, owner, and source provenance.

The table deliberately excludes imports, export aliases, and package symbols. It therefore gives future compiler passes a stable local-declaration lookup without conflating a source name with a cross-module binding. Duplicate local keys produce deterministic `EZSYM1001` diagnostics.

Next is C2-B: resolve named, default, and namespace imports together with local export aliases only across already resolved relative module edges.

Verified: core tests, formatting, clippy, and the workspace suite.


## 78. Relative module binding resolution

Completed C2-B. `BindingTable` consumes the `CompilationUnit`, C2-A local symbols, and C1-B module graph to resolve local export names along with named, default, and namespace imports whose source is a relative module already resolved in the unit.

Resolved import targets retain the existing `ModuleSymbol` identity and provenance. Namespace imports expose an explicit deterministic export map. Missing exports and unresolved relative imports emit `EZBIND1003` and `EZBIND1002`; external package imports remain deliberately unbound.

Re-export chains are not flattened here. C2-C will resolve `export { ... } from` and `export * from` chains before introducing module-identity collision diagnostics.

Verified: core tests, formatting, clippy, and the workspace suite.


## 79. Re-export chains and identity collisions

Completed C2-C. `BindingTable` now flattens relative `export { ... } from` and `export * from` chains with a bounded fixed-point pass over the `CompilationUnit`. Named, default, and namespace imports can therefore consume a re-exported local semantic symbol without reparsing or rebuilding another model.

The binding layer also detects component semantic IDs that collide across distinct modules, reporting deterministic `EZBIND1008` diagnostics. This exposes the remaining limitation of the legacy component-scoped ID format rather than silently collapsing application entities.

Namespace re-exports and external package re-exports remain intentionally unresolved. C2-D will migrate component semantic IDs to include stable module identity before deeper typed semantic lowering begins.

Verified: core tests, formatting, clippy, and the workspace suite.


## 80. Module-qualified semantic identities

Completed C2-D. Canonical compiler products now derive component roots from normalized source-module paths, producing readable IDs such as `module:src/Counter.tsx/component:x-counter`. Their state, method, action, event, and template descendants inherit the same module-qualified root.

`ApplicationSemanticModel`, `SymbolTable`, and `BindingTable` use the module-qualified graph, so identical `@component` tags in separate source files no longer collide. The original `build_component_graph` remains as a legacy backend compatibility entry point, keeping current generated HTML and runtime-manifest schemas unchanged while compiler metadata becomes globally unambiguous.

Next is C3-A: lower template descendants and bindings into typed semantic entities with provenance, extending the canonical ASM beyond component-level roots.

Verified: core tests, formatting, clippy, and the workspace suite.


## 81. Template semantic entities

Completed C3-A. The canonical ASM now lowers authored template descendants into typed `TemplateSemanticEntity` records. Elements, fragments, text, bindings, attributes, event attributes, conditionals, and lists receive module-qualified semantic IDs, direct template ownership, and source provenance.

These entities use deterministic semantic traversal paths rather than backend-local `n*` anchor IDs. Existing HTML and template-manifest generation remains unchanged, so runtime artifacts keep their current contracts while compiler consumers gain a richer authoritative model.

Next is C3-B: resolve template bindings, dynamic attributes, and conditional dependencies to their owning component state semantic IDs.

Verified: core tests, formatting, clippy, and the workspace suite.


## 82. Template state dependencies

Completed C3-B. The canonical ASM now resolves direct `this.<stateField>` reads from authored text bindings, dynamic attributes, and conditional tests to the owning component state field. Each relationship is a `TemplateState` semantic reference whose source is the typed template entity and whose provenance points to the authored source expression.

This is intentionally a direct-member resolver, not an expression evaluator. Member access such as `this.settings.enabled`, computed expressions, unresolved field names, and keyed-list iterables remain absent from the relation graph. HTML, manifests, and browser runtime behavior are unchanged.

Next is C3-C: resolve keyed-list iterable dependencies to component state semantic IDs.

Verified: core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 83. Keyed-list iterable dependencies

Completed C3-C. Typed keyed-list entities with a direct `this.<stateField>` iterable now emit the same source-provenanced `TemplateState` relation used by bindings, dynamic attributes, and conditionals. The reference points to the owning component's canonical state field, making list invalidation visible to ASM consumers.

List item scope is intentionally excluded: item/index variables, key expressions, item-member reads, and nested item-template behavior do not resolve through the component state table. HTML, manifests, and browser list reconciliation remain unchanged.

Next is C3-D: resolve template event attributes to component method semantic IDs.

Verified: core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 84. Template event-method references

Completed C3-D. Typed template event attributes with a direct `this.<method>` handler now emit source-provenanced `EventMethod` references to the resolved method on their owning component. The reference source is the canonical `EventAttribute` entity, allowing compiler consumers to trace directly from authored template event semantics.

The existing render-handler `EventMethod` reference remains intact for backend compatibility, so both sources intentionally target the same method. Event parsing, dispatch, manifests, and browser runtime behavior are unchanged.

Next is C4-A: add deterministic JSON output to `ezc asm` for canonical ASM inspection.

Verified: core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 85. ASM JSON inspection

Completed C4-A. `ezc asm <file> --format json` now emits a deterministic schema-versioned inspection document. Schema version 1 contains canonical semantic entities with ownership and source provenance, resolved references, compiler diagnostics, and ASM validation diagnostics. Entity and relation arrays have explicit deterministic ordering.

The existing text summary remains the default. The JSON document is intentionally an inspection contract rather than a raw serialization of compiler structs or backend artifacts, so its schema can remain stable while internals evolve.

Next is C4-B: extend `ezc asm` to inspect a multi-file `CompilationUnit` while preserving C4-A's schema and deterministic ordering.

Verified: core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 86. Multi-file ASM inspection

Completed C4-B. `ezc asm` now accepts one or more explicit source files and builds the canonical ASM from a deterministic `CompilationUnit`. Inputs may be supplied in any order; the compiler's sorted unit ordering determines semantic construction and output ordering.

Single-file text and JSON output remain compatible with C4-A. For multi-file JSON, schema version 1 retains the primary `file` field and adds an ordered `files` array so existing consumers remain compatible while new consumers can inspect the full unit input.

Next is C5-A: retain explicit TypeScript state-field type annotations in parser summaries without inference or type checking.

Verified: core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 87. State type annotation parsing

Completed C5-A. Parser summaries now retain explicit TypeScript annotations on properties recognized as `state(...)` declarations. Each `ParsedTypeAnnotation` carries the authored type text without the leading colon and a source span beginning at the colon, preserving exact source provenance for later semantic lowering.

This is syntax retention only. Non-state property annotations, type inference, type-import resolution, compatibility checks, diagnostics, manifests, and runtime behavior are intentionally unchanged.

Next is C5-B: lower declared state-field type annotations into canonical component and ASM data.

Verified: parser and core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 88. Declared state type lowering

Completed C5-B. `StateField` now carries optional `DeclaredStateType` metadata built from the parser annotation. It preserves the authored type text and full source provenance, making explicit state types available through both `ComponentGraph` and `ApplicationSemanticModel` without changing the state identity or runtime model.

Declared types are descriptive metadata only. They do not participate in type inference, validation, references, manifests, or browser runtime behavior.

Next is C5-C: expose declared state types through the versioned ASM JSON inspection document.

Verified: parser and core tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 89. ASM declared type inspection

Completed C5-C. `ezc asm --format json` now emits optional `declared_type` metadata for typed state entities. The field includes the raw declared text plus full annotation provenance, allowing developer tools to inspect state type declarations through the same canonical document as entities and references.

Untyped state entities remain unchanged in the JSON output, preserving C4-A compatibility. The new field is descriptive only and does not introduce type classification or checking.

Next is C5-D: classify exact primitive declared state types in canonical metadata.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 90. Primitive state type classification

Completed C5-D. Canonical `DeclaredStateType` metadata now classifies exact `string`, `number`, `boolean`, and `null` declarations. All other authored type text, including the typed-state fixture's union, remains intentionally unclassified while preserving raw text and provenance.

This is classification only. It performs no type resolution, inference, validation, or runtime change.

Next is C5-E: expose primitive state type classification through ASM JSON inspection.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 91. ASM primitive type inspection

Completed C5-E. `ezc asm --format json` now projects the canonical exact-primitive declared-state classification as optional `declared_type.kind`. Supported declarations report one of `string`, `number`, `boolean`, or `null`; unclassified authored forms such as unions keep their declared text and provenance while omitting the field.

This is an inspection-only extension to schema version 1. The CLI does not interpret types itself, create an `unknown` category, validate state assignments, or change manifests and browser runtime artifacts.

Next is C5-F: validate exact primitive declared state types against statically serializable initial state values.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 92. Primitive state initializer validation

Completed C5-F. The canonical component graph now emits `EZC1016` when an exact declared primitive state type (`string`, `number`, `boolean`, or `null`) has a statically known initializer of a different primitive type. The existing ASM inspection document exposes these compiler diagnostics without a separate CLI validation path.

Validation intentionally ignores unclassified declarations, arrays, objects, missing values, actions, inferred types, imports, aliases, unions, and runtime flow. This is a narrow reliable check, not general TypeScript compatibility.

Next is C5-G: attach source provenance to `EZC1016` diagnostics and expose it through ASM JSON inspection.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 93. Primitive type diagnostic provenance

Completed C5-G. `ComponentDiagnostic` now carries optional source provenance, and `EZC1016` uses the declared type annotation span that establishes the incompatible primitive contract. `ezc asm --format json` projects that optional diagnostic `provenance` field while preserving existing diagnostic code and message output.

Other compiler and ASM validation diagnostics remain unlocated until their source evidence is modeled deliberately. No validation rules, manifests, runtime artifacts, or CLI exit behavior changed.

Next is C5-H: validate direct primitive literal state assignments in action steps.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 94. Primitive action assignment validation

Completed C5-H. The canonical component graph now emits `EZC1017` when an exact primitive declared state field receives a different primitive through `this.<field> = <literal>`. Each diagnostic uses the parser's direct action-expression span, so ASM inspection can lead developer tools to the incompatible assignment.

This check intentionally excludes unclassified fields, composite values, valid assignments, compound operators, increments, decrements, toggles, flow analysis, and runtime behavior. Existing action manifests and browser output are unchanged.

Next is C5-I: validate boolean toggle actions against exact primitive declared state types.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 95. Primitive toggle action validation

Completed C5-I. The canonical component graph now emits `EZC1018` when a recognized self-toggle action targets an exact primitive declaration other than `boolean`. The diagnostic points to the toggle expression, while valid boolean and unclassified declarations remain accepted.

This is limited to `this.<field> = !this.<field>`. Direct assignments, numeric increment/decrement and compound operators, arbitrary expressions, manifests, and runtime behavior remain unchanged.

Next is C5-J: validate increment and decrement actions against exact primitive declared state types.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 96. Primitive numeric action validation

Completed C5-J. The canonical component graph now emits `EZC1019` when a recognized increment or decrement action targets an exact primitive declaration other than `number`. Each diagnostic points to its action expression; valid number and unclassified declarations remain accepted.

This check is limited to `this.<field>++` and `this.<field>--`. Compound arithmetic, direct assignment, toggle actions, arbitrary expressions, manifests, and runtime behavior remain unchanged.

Next is C5-K: validate compound numeric action targets and literal operands.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 97. Primitive compound action validation

Completed C5-K. The canonical component graph now emits `EZC1020` for non-number exact primitive targets and `EZC1021` for non-number literal operands in recognized `+=` and `-=` actions. Each diagnostic uses the action-expression span; composite serializable operands are reported by their concrete value kind.

This closes the supported literal action forms without adding arbitrary expression evaluation, variable flow, runtime enforcement, or manifest changes.

Next is C5-L: expose source-provenanced compiler diagnostics in text `ezc asm` output.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 98. ASM text diagnostic locations

Completed C5-L. Human-readable `ezc asm` output now appends deterministic compiler diagnostic details when they exist. Source-provenanced diagnostics include the canonical path, line, column, and byte span, while zero-diagnostic output remains unchanged.

JSON inspection remains unchanged. ASM validation diagnostics are still count-only in text output and are the next focused inspection slice.

Next is C5-M: expose ASM validation diagnostics in text `ezc asm` output.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 99. ASM text validation diagnostics

Completed C5-M. Human-readable `ezc asm` now also formats sorted ASM validation diagnostics when they exist, completing the text inspection surface for compiler and ASM integrity findings. Empty validation output remains omitted, preserving ordinary command output.

Source-driven ASM construction is normally validation-clean, so this defensive formatter is covered directly without adding an artificial invalid CLI source path. JSON output and validation rules are unchanged.

Next is C6-A: add an explicit `ezc check` command for compiler and ASM validation diagnostics.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 100. Compiler check command

Completed C6-A. `ezc check <file> [file...]` now builds the canonical `CompilationUnit` and ASM, reports parser/compiler/ASM validation diagnostics, and exits nonzero when any diagnostic exists.

Next is C6-B: add deterministic JSON output to `ezc check`.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 101. JSON compiler check output

Completed C6-B. `ezc check --format json` now emits a deterministic schema-versioned document containing sorted parser, compiler, and ASM validation diagnostics plus input files. It preserves C6-A text output and nonzero failure behavior.

Next is C6-C: add check summary counts and category filtering.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 102. Check summaries and category filters

Completed C6-C. `ezc check` now includes deterministic parser/compiler/validation summary counts. Repeatable `--category parser|compiler|validation` filters displayed diagnostic detail in text and JSON, while exit status still reflects all canonical diagnostics.

Next is C6-D: add check diagnostic severity policy.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 103. Check diagnostic severity policy

Completed C6-D. `ezc check --fail-on error|warning|info` now controls the parser severity threshold that fails a check, defaulting to `error`. Compiler and ASM validation diagnostics remain hard failures regardless of this parser policy.

Next is C6-E: add check policy documentation and project defaults.

Verified: CLI/core/parser tests, formatting, strict workspace clippy, and the workspace suite including browser runtime tests.


## 104. Check policy documentation and project defaults

Completed C6-E. The root documentation and `ezc_cli` usage now expose the complete `check` policy surface: `--category parser|compiler|validation` and `--fail-on error|warning|info`. The documented project default is `--fail-on error`; parser warnings and informational diagnostics stay visible without failing the command, while compiler and ASM validation diagnostics always fail. The JSON contract now verifies its default `fail_on` value.

The policy remains command-scoped. EdgeZero does not yet accept a project policy file, preset, or discovery mechanism. The browser e2e entry points now use one Rust test thread because each test launches a real Chrome process; this removes host-resource contention from the documented `pnpm test:e2e` and `just e2e` checks.

Next is C6-F: add parser diagnostic label provenance to `ezc check` output without changing parser semantics, the default policy, or hard compiler/ASM failures.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 105. Check parser diagnostic label provenance

Completed C6-F. `ezc check` now preserves parser diagnostic label provenance in every visible diagnostic surface. Text output prints each label below its parser diagnostic with the canonical file path, line, column, and byte span. JSON output includes a deterministic `labels` array per parser diagnostic with `line`, `column`, `start`, and `end` fields.

This projection does not change parser behavior, category filtering, summary counts, the command-scoped `--fail-on` policy, or the hard failure behavior for compiler and ASM validation diagnostics. Label messages, source excerpts, code frames, and compiler diagnostic provenance in check JSON remain intentionally out of scope.

Next is C6-G: add optional compiler diagnostic provenance to `ezc check --format json` without changing parser labels, validation rules, filtering, or exit behavior.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 106. Check compiler diagnostic provenance

Completed C6-G. `ezc check --format json` now includes optional `provenance` on compiler diagnostics when the canonical compiler has a reliable source location. The object reuses ASM inspection coordinates exactly: `path`, `start`, `end`, `line`, and `column`. Diagnostics without a reliable source location omit the field rather than emitting null or a synthetic location.

This completes the C6 check-output provenance series without changing parser labels, category filtering, summary counts, parser severity policy, or compiler/ASM failure behavior. Source remapping, code frames, and provenance for ASM validation diagnostics remain out of scope.

Next: select the next compiler roadmap slice after C6, preserving the established check JSON contract unless that slice explicitly evolves it.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 107. ASM ownership traversal queries

Completed C7-A. `ApplicationSemanticModel` now exposes deterministic `application_roots()` and `children_of()` queries. Both return semantic IDs in the canonical ownership-map order, allowing future explain, editor, and inspector consumers to traverse the compiler-owned application hierarchy without reconstructing it from raw model fields.

The slice intentionally exposes one ownership level at a time. It adds no CLI syntax, model mutation, transitive walk, filtering, or source lookup.

Next is C7-B: add deterministic transitive ownership traversal while preserving C7-A direct-query ordering and ownership semantics.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 108. ASM transitive ownership traversal

Completed C7-B. `ApplicationSemanticModel::descendants_of()` now walks a semantic subtree in deterministic depth-first pre-order. Each direct child retains C7-A semantic-ID order and appears before its own descendants, giving explain, editor, and inspector consumers a stable parent-before-child traversal.

The query is read-only and returns semantic IDs only. It adds no depth metadata, filtering, source lookup, CLI syntax, or semantic-model mutation.

Next is C7-C: add deterministic semantic entity-kind queries for tooling filters while preserving ownership traversal semantics.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 109. ASM semantic entity-kind queries

Completed C7-C. `SemanticEntityKind` now provides a typed classification for canonical ASM entities, and `ApplicationSemanticModel::entities_of_kind()` returns matching semantic IDs in deterministic ownership-map order. This gives tooling a stable filter without independent scans of component, template, and relation collections.

The slice intentionally supports only broad canonical entity categories. Template subkind filters, composite predicates, source lookup, CLI arguments, and model mutation remain out of scope.

Next is C7-D: add deterministic source-provenance lookup queries while preserving C7 ownership and entity-kind ordering.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 110. ASM source-provenance lookup queries

Completed C7-D. `ApplicationSemanticModel::entities_in_file()` now returns every semantic ID with provenance in an exact source path, while `entities_at()` returns every entity whose canonical half-open source span covers a byte offset. Both retain deterministic semantic-ID ordering from the provenance map.

The lookup surface intentionally accepts compiler paths and byte offsets only. Path normalization, line/column ranges, source remapping, ranking, editor protocols, CLI syntax, and model mutation remain out of scope.

Next is C7-E: add deterministic semantic reference-kind queries while preserving C7 ownership, entity-kind, and provenance semantics.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 111. ASM semantic reference-kind queries

Completed C7-E. `ApplicationSemanticModel::references_of_kind()` now returns canonical semantic relations for one `SemanticReferenceKind`, sorted by source and target semantic IDs. Tooling can filter action/state, event/method, and template/state relations without independently scanning or ordering the ASM relation list.

The query is read-only and intentionally supports one relation kind at a time. Composite predicates, endpoint filters, provenance lookup, CLI syntax, and model mutation remain out of scope.

Next is C7-F: add deterministic semantic reference provenance lookup while preserving C7 ownership, entity-kind, and reference-kind filtering.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 112. ASM semantic reference provenance queries

Completed C7-F. `ApplicationSemanticModel::references_in_file()` and `references_at()` now locate canonical semantic relations by exact source path and half-open byte span. Results are sorted by source and target semantic IDs, giving tooling stable relation lookups independent of construction order.

This completes the C7 query foundation without adding line/column input, path normalization, source remapping, composite predicates, CLI syntax, or model mutation.

Next: select the next compiler roadmap slice after C7, preserving deterministic read-only query ordering unless that slice explicitly evolves it.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 113. ASM entity inspection

Completed C8-A. `ezc_cli asm <file> --entity <semantic-id> [--format text|json]` now inspects one canonical semantic entity. The inspection reports the entity’s kind, owner, source provenance, direct children, descendant count, incoming/outgoing references, and compiler diagnostics whose source provenance overlaps the selected entity. Unknown IDs fail explicitly.

The legacy `explain` command remains unchanged. C8-A intentionally requires an existing semantic ID, discoverable through full ASM JSON; source-position selection is next.

Next is C8-B: add source-path and byte-offset entity selection using C7-D provenance queries, while preserving the C8-A inspection document.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 114. ASM entity inspection by source offset

Completed C8-B. `ezc_cli asm <file> --source <path> --offset <byte> [--format text|json]` now selects a canonical semantic entity directly from source provenance. The command chooses the uniquely narrowest entity span covering the offset, then returns the C8-A inspection document. No match, equal-width ambiguity, invalid offsets, incomplete source selectors, and conflicts with `--entity` fail explicitly.

The selector intentionally accepts exact compiler paths and byte offsets. Line/column input, path normalization, source remapping, candidate-choice UX, and legacy `explain` migration remain out of scope.

Next is C8-C: add child and relation filtering to ASM entity inspection while preserving C8-A/B inspection and selection semantics.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 115. ASM entity inspection filters

Completed C8-C. Selected `ezc_cli asm` inspection now supports `--child-kind` and `--reference-kind` filters. Child filtering accepts canonical broad entity kinds, while relation filtering accepts canonical action/state, event/method, and template/state relation kinds. Both filters work with semantic-ID and source-offset entity selection, affect only direct children and incoming/outgoing relation lists, and reject unselected full-ASM inspection.

The inspection document, selected entity, descendant count, ownership, provenance, and diagnostics remain unchanged. C8-C deliberately accepts one filter of each category; composite predicates, descendant and diagnostic filters, line/column selection, path normalization, and source remapping remain out of scope.

Next is C8-D: add an explicit entity-inspection mode to `ezc explain` while preserving the default legacy source-summary output and the C8 inspection contract.

Verified: formatting; parser/core/CLI tests; non-CI and CI browser suites; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 116. ASM entity inspection in explain

Completed C8-D / Phase A1. `ezc_cli explain <file>` now enters explicit entity-inspection mode when supplied with an ASM selector or entity filter. It delegates to the same canonical inspection runner as `ezc_cli asm`, so semantic-ID selection, source-byte selection, child/reference filtering, diagnostics, deterministic ordering, text output, and schema-versioned JSON remain one read-only compiler behavior.

The existing `ezc_cli explain <file> [--format text|json]` source-summary path is unchanged, including its established golden text and JSON fixture contracts. Filters without an entity selector fail through the canonical inspection validation instead of being treated as legacy explain options.

Next is Phase A2: extend semantic navigation with explicit parents, children, and references while preserving the canonical ASM ownership and relation ordering.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 117. Canonical semantic navigation

Completed Phase A2. `ApplicationSemanticModel` now exposes `parent_of` for direct ownership navigation and `ancestors_of` for deterministic nearest-first traversal through the application root. The traversal is cycle-safe defensively, while ordinary compiler-built ASM ownership remains acyclic.

Selected `ezc_cli asm` and explicit entity-inspection `ezc_cli explain` now include ordered parent IDs alongside their existing direct children and incoming/outgoing references. This projects all navigation through the canonical ASM and does not add parser behavior, backend graph semantics, manifest changes, runtime intelligence, or model mutation.

Next is Phase A3: define a stable semantic graph export from the canonical ASM without serializing backend-owned graph details.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 118. Stable semantic graph export

Completed Phase A3. `SemanticGraph` is now a schema-versioned, backend-independent artifact built directly from `ApplicationSemanticModel`. It exports ordered application roots, typed semantic nodes with source provenance, parent-to-child ownership edges, and resolved action/state, event/method, and template/state edges.

`ezc_cli asm <file> [file...] --format graph` emits deterministic pretty JSON for this artifact. The application root remains a dedicated roots collection rather than an invented synthetic semantic node. The existing `ezc_cli graph` ComponentGraph command remains unchanged for compatibility; the new export deliberately excludes parser facts, diagnostics, backend-local IDs, manifests, and browser runtime artifacts.

Next is Phase A4: audit duplicated semantic information and migrate ownership consumption into the ASM while preserving existing backend/runtime contracts.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 119. Canonical ASM ownership consumption

Completed Phase A4. The ASM now structurally derives ownership for components, state fields, methods, actions, event handlers, and templates instead of copying their legacy owner fields. Template semantic entities remain the one transitional ownership input because their template-containment relation is established during lowering.

Canonical template entity lookup, template state/event dependency lowering, and dead semantic action analysis now consume `ApplicationSemanticModel::ownership`. Regression tests deliberately corrupt legacy owner fields after lowering and verify that canonical ownership behavior remains stable. Existing ComponentGraph, TemplateSemanticEntity construction, and SymbolTable owner fields remain compatibility/frontend data; no runtime, manifest, parser, or backend artifact contract changed.

Next is Phase A5: convert compiler passes toward immutable transformations over canonical ASM products while preserving deterministic outputs and existing backend/runtime contracts.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 120. Immutable ASM pass foundation

Completed Phase A5. `ImmutableAsmPass` now defines the canonical read-only transformation boundary from `ApplicationSemanticModel` to a newly constructed compiler product. Dependency, constant evaluation, dead-semantic, optimization-planning, and explainability passes now implement `transform`; their established `AnalysisPass::analyze` entry point remains a compatibility wrapper.

Pass composition now calls immutable transforms directly. Core regression coverage proves that every migrated pass returns the same deterministic result through both entry points and leaves the source ASM unchanged. The strict clippy pass also documented the panic invariants on the A3 semantic graph API.

Next is Phase B1: introduce a compiler-owned arithmetic-expression model and canonical semantic lowering, beginning with narrow numeric arithmetic and deterministic diagnostics rather than general JavaScript evaluation.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 121. Constant arithmetic state initializers

Completed Phase B1. The parser now retains a typed tree for constant numeric arithmetic used inside `state(...)` initializers, and canonical lowering converts it into the compiler-owned `ArithmeticExpression` model. Numeric literals, parentheses, and `+`, `-`, `*`, `/`, and `%` evaluate deterministically before the existing backend paths consume the initial serializable value. The authored expression remains available in ASM JSON and ComponentGraph inspection output.

Invalid numeric literals, division or remainder by zero, and non-finite results now produce source-provenanced `EZC1022` diagnostics. The new `0032-arithmetic-state-initializer` fixture locks parse, graph, and emitted HTML behavior, while parser/core/CLI tests cover tree retention, lowering, evaluation, inspection, and invalid arithmetic.

This deliberately does not become a JavaScript evaluator: state reads, local variables, calls, coercions, action expressions, comparisons, logical operators, control flow, and expression typing remain unsupported. The existing runtime receives only the compiler-computed initial value and needs no added arithmetic intelligence.

Next is Phase B2: add compiler-owned constant comparison expressions to state initializers while preserving B1 arithmetic semantics and deterministic diagnostics.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 122. Constant comparison state initializers

Completed Phase B2. Constant `state(...)` expressions now use one compiler-owned `ConstantExpression` model rather than parallel arithmetic-only metadata. B1 arithmetic remains intact, and a comparison may combine numeric literals and arithmetic with `===`, `!==`, `<`, `<=`, `>`, or `>=`. Canonical lowering evaluates the expression to a boolean before existing HTML, manifest, and browser-runtime paths consume the initial state. ASM JSON and ComponentGraph inspection retain the deterministic authored expression through the generic `initial_expression` field.

Invalid arithmetic within a comparison reports source-provenanced `EZC1023`, while standalone arithmetic retains `EZC1022`. The `0033-constant-comparison-state-initializer` fixture locks parser, graph, and static HTML behavior; parser/core/CLI tests cover comparison-tree retention, all six operators, boolean lowering, inspection, and diagnostic provenance.

This remains a deliberate compiler language, not JavaScript compatibility: comparisons are numeric-only and static, and state reads, local variables, calls, coercion, string/boolean comparisons, logical operations, control flow, expression typing, and action expressions remain unsupported.

Next is Phase B3: add compiler-owned constant logical expressions to state initializers while preserving B1/B2 semantics, boolean results, and deterministic diagnostics.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 123. Constant logical state initializers

Completed Phase B3. The canonical `ConstantExpression` model now represents boolean literals and nested logical expressions. `state(...)` initializers may compose B2 numeric comparisons and boolean literals with `&&` or `||`; canonical lowering evaluates only the reachable branch using deterministic compiler-time short-circuit semantics, then passes the resulting boolean through the existing backend and runtime paths. ASM JSON and ComponentGraph inspection retain the authored logical expression through `initial_expression`.

Reached invalid arithmetic within a logical expression produces source-provenanced `EZC1024`, while an invalid expression in a short-circuited branch intentionally produces no diagnostic. The `0034-constant-logical-state-initializer` fixture locks parser, graph, and static HTML output; parser/core/CLI tests cover logical-tree retention, AND/OR evaluation, short-circuit behavior, inspection, and invalid reachable branches.

This remains a deliberate compiler language rather than JavaScript compatibility: logical operands must be boolean literals, B2 comparisons, or nested supported logical expressions. Truthiness, coercion, unary negation, state reads, local variables, calls, nullish coalescing, control flow, expression typing, and action expressions remain unsupported.

Next is Phase B4: add compiler-owned constant nullish-coalescing expressions to state initializers while preserving B1-B3 semantics and deterministic diagnostics.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 124. Constant nullish-coalescing state initializers

Completed Phase B4. Constant `state(...)` expressions now support `??` across supported primitive literals and B1-B3 expressions. Canonical lowering evaluates the left side first and evaluates the right only when the left result is `null`, then sends the selected static value through existing backends. Reached invalid arithmetic in a nullish expression reports `EZC1025`; a non-null left operand short-circuits an invalid right branch.

The `0035-constant-nullish-state-initializer` fixture locks parser, graph, static HTML, and ASM inspection behavior. Nullish coalescing remains static-only: undefined, state reads, calls, coercion, unary operators, control flow, expression typing, and action expressions remain unsupported.

Next is Phase B5: add compiler-owned unary expressions to state initializers while preserving B1-B4 semantics and deterministic diagnostics.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 125. Constant unary state initializers

Completed Phase B5. The canonical constant-expression model now supports unary `!`, unary `+`, and unary `-`. Boolean negation accepts supported boolean expressions, while numeric sign operators accept supported numeric constant expressions; lowering evaluates the result before existing backend emission and inspection preserves the authored unary form.

The `0036-constant-unary-state-initializer` fixture locks parse, graph, and HTML output. Parser/core/CLI coverage verifies boolean negation and numeric sign lowering; no runtime expression evaluator was introduced.

Next is Phase B6: introduce compiler-owned local variables with deterministic, constrained semantics.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 126. Method-local constants

Completed Phase B6. Supported serializable local declarations in component methods are now retained by the parser and canonically lowered onto their owning methods with source spans. ASM JSON exposes deterministic `local_variables` metadata for method entities. Locals remain lexical compiler metadata only: they do not yet resolve render bindings, execute in actions, or require runtime support.

The `0037-method-local-constants` fixture and ASM CLI coverage lock the inspection behavior. Next is Phase B7: constrained method parameters.

Verified: formatting; parser/core/CLI tests; browser runtime tests; strict workspace clippy; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.


## 127. Constrained method parameters

Completed Phase B7. Direct identifier parameters on component methods are now retained in authored order by the parser and canonically lowered onto their owning methods. The canonical ASM inspection document exposes each parameter's name and source provenance through method `parameters` metadata, so compiler services can inspect declarations without reparsing or relying on runtime behavior.

This exists to establish compiler-owned method declaration semantics before local reference resolution. B7 deliberately does not execute parameters, create closures, bind them into templates or actions, infer or check types, or support destructuring, default values, or rest parameters. Unsupported parameter forms are not lowered.

The `0038-constrained-method-parameters` fixture and parser, core, and CLI tests lock deterministic identifier-only lowering, authored ordering, and ASM provenance.

Next is Phase B8: constrained local-variable resolution.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## 128. Local variable resolution

Completed Phase B8. Supported method-local constants now have stable method-child semantic IDs, ownership, and provenance in the canonical ASM. Exact, uniquely declared identifiers from normal `render()` template bindings and dynamic attributes resolve through deterministic `template-local` edges. Their known serializable values are used by compiler lowering to seed initial static HTML; no browser-runtime evaluator was added.

This exists to give compiler services and later optimization passes a canonical declaration-to-use relationship instead of reinterpreting source text. B8 deliberately excludes list-item callback scopes, duplicate local names, member access, arbitrary expressions, calls, closures, action references, runtime updates, and type semantics. Unsupported forms remain unresolved rather than guessed.

The `0039-method-local-resolution` fixture plus core and CLI tests lock ASM entity ownership, relation targets, semantic-graph export, deterministic inspection, and static HTML initialization.

Next is Phase B9: an immutable constant-folding pass.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## 129. Immutable constant folding pass

Completed Phase B9. The parser and component-graph lowering now retain supported state initializer expression trees without evaluating them. `ConstantFoldingPass` is an immutable, idempotent ASM transformation that produces a new model containing folded serializable state values, preserved authored expressions, source-provenanced existing diagnostics, and refreshed template values. CLI graph, template, HTML, manifest, and build products consume this folded compiler product.

This exists to make evaluation a canonical compiler optimization rather than a parser/frontend side effect, so later compiler services and optimizations can reuse one result. B9 folds only the existing supported state initializer language. It does not introduce a unified expression graph, local/action expression evaluation, general template evaluation, runtime evaluation, flow analysis, or type propagation.

The `0040-immutable-constant-folding` fixture plus core and CLI tests lock raw-versus-folded separation, pass immutability/idempotence, canonical HTML consumption, and the pre-existing diagnostic contracts.

Next is Phase B10: canonical expression graph.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## 130. Canonical expression graph

Completed Phase B10. Supported state initializer expressions now lower into one ASM-owned graph with stable root and child semantic IDs, typed operator/literal nodes, ownership, and diagnostic spans. Constant folding and ASM inspection consume that graph, so expression topology is no longer independently interpreted by each semantic consumer.

The graph currently covers only the existing supported state initializer language. Legacy field-local trees remain lowering compatibility data while consumers use the canonical graph. Path-aware canonical provenance for every node, non-state expression owners, general template/action expressions, and type propagation remain out of scope.

The `0041-canonical-expression-graph` fixture plus core and CLI tests lock graph node construction, stable roots, canonical rendering, and folding consumption.

Next is Phase B11: expression provenance.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## 131. Expression provenance

Completed Phase B11. Every canonical state-initializer expression node now owns `SourceProvenance`: the source file path and the exact span of its authored expression. Constant-folding diagnostics reuse the graph root provenance directly, so expression diagnostics and future tooling share one canonical location.

The graph remains limited to the currently supported state initializer language. Provenance is not yet available for general template, action, or JavaScript expressions because those expressions are not graph nodes in this slice.

Core graph coverage verifies each node has the owning source path and a span contained by its root expression, while folding coverage verifies diagnostics reuse the canonical root provenance; no fixture was required because B11 changes canonical ASM metadata rather than a backend artifact.

Next is Phase B12: expression queries.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## 132. Expression queries

Completed Phase B12, finishing Phase B. The canonical ASM now exposes deterministic read-only expression graph queries for node and root lookup, owner-scoped enumeration, direct dependencies and reverse dependencies, source provenance, state-field ownership, and source-path/offset selection.

This exists so compiler services, language tooling, and later optimization passes can navigate the same graph used by folding and diagnostics without reparsing legacy expression trees. B12 intentionally provides direct graph navigation only: no mutation, transitive closure, query language, or non-state expression support was introduced.

Core ASM coverage locks deterministic lookup, topology, ownership, provenance, and source selection. No fixture was required because B12 adds compiler query APIs without changing backend products.

Phase B is complete. The next roadmap phase is Phase C: semantic type and knowledge system; do not begin it automatically.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## Roadmap update. Phase C - Semantic Type and Knowledge System

This supersedes the earlier coarse Phase C outline. Phase C is a staged C1-C35 compiler foundation. Do not begin C1 automatically; complete exactly one slice at a time with the established deterministic ASM, documentation, and full-verification contracts.

1. C1 - Canonical semantic type model: introduce the compiler-owned `SemanticType` representation for unknown, never, null, booleans, numbers, strings, arrays, objects, and unions.
2. C2 - Type identities and provenance: retain origin, declared-versus-inferred status, source provenance, and canonical identities where appropriate.
3. C3 - Primitive and literal types: support primitive and string, numeric, and boolean literal types.
4. C4 - Array and tuple types: support homogeneous arrays and tuple-shaped types without blocking later tuple semantics.
5. C5 - Object structural types: support compiler-owned typed object properties required for application state and list members.
6. C6 - Union and nullable types: support unions such as `string | null` and discriminating literal unions.
7. C7 - Type aliases: preserve alias source identity while resolving aliases to canonical semantic types.
8. C8 - Imported type resolution: resolve named imports and re-exports through existing module and binding infrastructure before external packages.
9. C9 - State type inference: infer state types, with an explicit empty-collection policy such as `Array<unknown>` unless annotated.
10. C10 - State initializer compatibility: replace primitive-only checks with canonical assignability for annotated state initializers.
11. C11 - Expression type propagation: assign semantic types to canonical expression nodes.
12. C12 - Operator typing: define explicit operand and result contracts for arithmetic, comparison, logical, unary, and nullish operators.
13. C13 - Local variable typing: infer and validate method-local constants as canonical declarations.
14. C14 - Method parameter typing: lower parameter annotations into typed semantic entities.
15. C15 - Return type semantics: support explicit and inferred method result types for later computed, loader, action, and helper semantics.
16. C16 - Assignment compatibility: validate direct assignments through centralized assignability.
17. C17 - Compound mutation typing: validate compound assignments, increments, decrements, and toggles through semantic types.
18. C18 - Template binding typing: validate text interpolation and renderability.
19. C19 - Attribute and property typing: introduce compiler-owned DOM attribute/property contracts.
20. C20 - Conditional typing: require canonical boolean conditions unless a later explicit slice adopts truthiness.
21. C21 - List iterable typing: validate array-like list iterables and infer item/index template-scope types.
22. C22 - Member access typing: resolve nested object member access and report missing or invalid properties deterministically.
23. C23 - Computed value type foundation: establish typed derived getter contracts before computed runtime behavior.
24. C24 - Action signature foundation: define typed action inputs and outputs before action runtime expansion.
25. C25 - Resource type foundation: define `Resource<T>` data/error/pending, serializability, and execution-boundary semantics.
26. C26 - Serialization compatibility: determine which semantic types can cross compiler/runtime boundaries.
27. C27 - Server/client boundary compatibility: validate type movement across execution boundaries before backend generation.
28. C28 - Type normalization: canonicalize equivalent types deterministically.
29. C29 - Assignability engine: centralize `is_assignable(source, target)` rather than scattering compatibility rules.
30. C30 - Type query APIs: expose entity, expression, declaration, usage, serializability, and assignability queries from the ASM.
31. C31 - Type inspection output: extend `ezc asm` and `ezc explain` with semantic types, provenance, and inference source.
32. C32 - Type diagnostics: establish stable diagnostic families for unknown types, operators, assignments, members, conditions, iterables, renderability, and serialization.
33. C33 - Type-system validation pass: validate type references, identities, aliases, recursive cycles, and ownership consistency.
34. C34 - Type-system fixture suite: cover valid and invalid cross-module state, expression, action, template, array, object, union, and serialization combinations.
35. C35 - Phase C stability audit: remove duplicated legacy type logic, verify immutable canonical consumption, document unsupported semantics, and freeze the initial type-inspection contract.

Phase C is complete only when meaningful values, expressions, declarations, and execution boundaries have canonical queryable, validatable, serializable, and explainable semantic types. Later resources, computed values, actions, forms, resumability, optimization, and IDE tooling must consume this canonical layer rather than establish parallel type assumptions.

Next when explicitly requested: C1 - canonical semantic type model.


## 133. Canonical semantic type model

Completed Phase C1. Added the compiler-owned `SemanticType` algebra with unknown, never, null, boolean, number, string, array, object, and union forms. `ApplicationSemanticModel` now contains the canonical semantic type assignment model, establishing the shared location that later declaration lowering, inference, and expression typing will populate.

This exists so Phase C evolves one semantic type language rather than treating raw TypeScript strings or existing primitive checks as the compiler's canonical type system. C1 intentionally leaves the assignment model empty and preserves declared state-type text as legacy input metadata; it adds no inference, annotation lowering, compatibility decisions, diagnostics, identities, provenance, literals, tuples, aliases, imports, or query API.

Core coverage locks every C1 type form and verifies that ASM construction retains legacy declared metadata without populating canonical assignments. No fixture was required because the slice adds semantic foundation only and changes no backend product.

Next is C2: type identities and provenance. Do not begin it automatically.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `pnpm test:e2e`; and `just e2e`.


## 134. Type identities and provenance

Completed Phase C2. Canonical type assignments now have a deterministic `SemanticTypeId`, typed subject, semantic origin, declared-or-inferred status, and `SourceProvenance`. The assignment container remains empty until later source-lowering and inference slices populate it.

This metadata exists so aliases, imported declarations, and inferred expressions can retain stable, explainable attribution instead of treating a raw annotation string as a type identity. C2 adds no primitive lowering, aliases, imports, inference, compatibility, diagnostics, or type queries.

Core coverage locks deterministic identity plus origin, declared status, and provenance retention. Browser e2e commands were omitted because C2 changes only compiler-internal type metadata and does not affect parsing, code generation, or runtime artifacts.

Next is C3: primitive and literal types.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 135. Primitive and literal types

Completed Phase C3. Exact state annotations now lower into canonical primitive types (`string`, `number`, `boolean`, and `null`) and literal types for strings, numbers, and booleans. Each assignment retains the C2 identity, subject, declared status, and annotation provenance.

This establishes literal semantics for later discriminated unions, routes, configuration, and action inputs without inferring types from values or interpreting unsupported TypeScript syntax. Arrays, tuples, objects, unions, aliases, imports, inference, compatibility, and diagnostics remain later slices.

Core coverage locks primitive and literal state-annotation lowering plus the retained assignment metadata. Browser e2e commands were omitted because C3 changes only canonical compiler metadata and does not affect generated runtime artifacts.

Next is C4: array and tuple types.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 136. Array and tuple types

Completed Phase C4. Canonical semantic types now include ordered tuple forms and lower supported `T[]` and `[T, U]` state annotations. Primitive element types retain C3 meaning, while unresolved array element names lower to `unknown` until aliases and imports receive canonical resolution.

This establishes collection shape without coupling it to raw TypeScript names or early structural-object behavior. C4 adds no object-type lowering, union parsing, aliases, imports, inference, assignability, or diagnostics.

Core coverage locks primitive arrays, unresolved-name arrays, and ordered tuple lowering. Browser e2e commands were omitted because C4 changes only compiler-internal type metadata and does not affect generated runtime artifacts.

Next is C5: object structural types.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 137. Object structural types

Completed Phase C5. Semicolon-separated structural object state annotations now lower into canonical object-property maps with deterministic names and canonical property types.

This establishes object shapes before list-item and member-access semantics depend on them. C5 does not create property identities/provenance, resolve aliases, validate member reads, infer objects, or add compatibility diagnostics.

Core coverage locks structural object property lowering. Browser e2e commands were omitted because C5 changes only compiler-internal type metadata and does not affect generated runtime artifacts.

Next is C6: union and nullable types.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 138. Union and nullable types

Completed Phase C6. Top-level union annotations now lower into canonical `Union` members, preserving authored order pending C28 normalization. This includes literal unions and nullable forms such as `string | null` and `{ id: string } | null`.

This gives filters, loading states, and route values a durable canonical representation without yet adding alias resolution, import resolution, assignability, inference, normalization, or diagnostics.

Core coverage locks literal-union and object-or-null lowering. Browser e2e commands were omitted because C6 changes only compiler-internal type metadata and does not affect generated runtime artifacts.

Next is C7: type aliases.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 139. Type aliases

Completed Phase C7. The parser retains local `type` aliases, and the semantic type model gives each supported alias a module-qualified canonical identity and source provenance. State annotations that name a local alias resolve to its canonical semantic type while preserving alias origin.

This separates alias source identity from semantic type meaning before imported resolution and tooling depend on both. C7 supports direct local aliases with currently supported underlying types only; nested aliases, exports, imports, generics, and cycles remain later work.

Parser and core coverage lock local primitive and literal-union aliases plus alias-origin assignments. Browser e2e commands were omitted because C7 changes only parser and compiler semantic metadata, not generated runtime artifacts.

Next is C8: imported type resolution.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_parser`; and `cargo test -p ezc_core`.


## 140. Imported type resolution

Completed Phase C8. Type aliases are now compiler module symbols, so unit-level canonical type construction resolves supported named alias imports through local relative modules and named re-export chains. Resolved state assignments retain the defining alias's canonical identity.

This reuses existing deterministic module, symbol, and binding infrastructure instead of introducing an independent type import resolver. External packages, namespace/default imports, generic aliases, and cycles remain out of scope.

Parser and core coverage lock named imported alias resolution through a re-export. Browser e2e commands were omitted because C8 changes only parser/frontend semantic metadata and does not affect generated runtime artifacts.

Next is C9: state type inference.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_parser`; and `cargo test -p ezc_core`.


## 141. State type inference

Completed Phase C9. Unannotated direct serializable state initializers now receive canonical inferred semantic types. Primitive values widen to primitive types, non-empty arrays infer element types, objects infer structural properties, and empty arrays use the explicit `Array<unknown>` policy.

This establishes useful `state(...)` contracts without inferring from arbitrary expressions or changing runtime behavior. Constant-expression propagation, full compatibility, and type diagnostics remain later slices.

Core coverage locks inferred number, empty/non-empty array, and object state types with inferred status. Browser e2e commands were omitted because C9 changes only compiler-internal type metadata and does not affect generated runtime artifacts.

Next is C10: state initializer compatibility.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 142. State initializer compatibility

Completed Phase C10. Declared state initializers now use canonical semantic assignability across primitive, literal, array, tuple, object, union, and nullable forms. Existing incompatibilities retain the stable `EZC1016` family while valid empty arrays, literal unions, and nullable object values are accepted.

This generalizes the earlier primitive-only check without expanding action, template, or general-expression semantics. The final general assignability engine remains C29.

Core coverage locks representative valid and invalid state compatibility cases. Browser e2e commands were omitted because C10 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C11: expression type propagation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 143. Expression type propagation

Completed Phase C11. Every node in the existing canonical state-initializer expression graph now receives an inferred semantic type assignment with the node's stable identity, owner, and source provenance. Literals retain literal types; arithmetic and numeric unary expressions are `number`; comparisons and logical/unary-not expressions are `boolean`; and nullish coalescing preserves a common type or forms a union.

This makes expression results available through the Application Semantic Model without asking later consumers to reinterpret authored syntax or duplicate graph traversal. C11 intentionally propagates result shapes only: C12 will define operand validity, and state reads, locals, templates, actions, arbitrary JavaScript expressions, and type diagnostics remain unsupported.

Core coverage locks canonical root and recursive node types for arithmetic and comparison expressions. Browser e2e commands were omitted because C11 changes only compiler-internal semantic metadata and has no generated runtime artifact impact.

Next is C12: operator typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 144. Operator typing

Completed Phase C12. The compiler now exposes one semantic operator typing relation for arithmetic, comparison, logical, unary, and nullish-coalescing operators. Arithmetic and comparisons require number-like operands; logical and unary-not require boolean-like operands; unary plus/minus require number-like operands; and nullish coalescing removes `null` from the left result before combining it with the fallback.

This replaces implicit JavaScript-coercion assumptions with explicit compiler semantics shared by expression propagation and later diagnostics. C12 returns `unknown` for graph nodes with invalid operands but deliberately introduces no new diagnostic family before C32. String coercion, truthiness, calls, state reads, local values, templates, actions, and arbitrary JavaScript expressions remain out of scope.

Core coverage locks valid result types, nullish behavior, and invalid arithmetic/logical combinations. Browser e2e commands were omitted because C12 changes only compiler-internal semantic metadata and has no generated runtime artifact impact.

Next is C13: local variable typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 145. Local variable typing

Completed Phase C13. Existing compiler-owned method-local declaration entities now receive inferred canonical semantic types from their lowered serializable values. The assignments retain each local's stable semantic identity, method ownership, inferred status, and source provenance, while existing template-local references continue to point to the same entity.

This gives local declarations a queryable semantic contract without duplicating the binding or reference graph. C13 is intentionally limited to the currently lowered serializable `const` forms; local expressions such as state reads, annotations, flow, mutation, destructuring, and arbitrary JavaScript initializers require later expression-language support.

Core coverage locks a render-local's canonical type, status, provenance, ownership, and template reference edges. Browser e2e commands were omitted because C13 changes only compiler-internal semantic metadata and has no generated runtime artifact impact.

Next is C14: method parameter typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 146. Method parameter typing

Completed Phase C14. Method parameter annotations are now retained by the parser and lowered into stable method-owned parameter entities. Each supported annotation receives a declared canonical semantic type assignment with authored provenance, and parameter entities participate in ASM ownership, semantic graph output, and CLI entity inspection.

This gives actions, computed values, and tooling a canonical typed parameter contract rather than parser-only method metadata. C14 supports current identifier parameters and supported annotations only; defaults, destructuring, rest, optionality semantics, parameter flow, return validation, and call-site validation remain later work. The CLI fixture assertions were also reconciled with C10's canonical array/union initializer compatibility diagnostics.

Parser, core, CLI, and browser-integration coverage pass. Browser e2e command aliases were omitted because C14 changes compiler/parser/inspection metadata and the CLI browser suite already exercises generated runtime behavior without modifying it.

Next is C15: return type semantics.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_parser`; `cargo test -p ezc_core`; and `cargo test -p ezc_cli`.


## 147. Return type semantics

Completed Phase C15. Method return annotations are retained and lower to declared canonical semantic types. When no annotation exists, the compiler infers a method return type from currently supported top-level serializable return values, preserving method identity and provenance through the ASM assignment model.

This establishes a stable method result contract for later computed, action, loader, and tooling phases. C15 intentionally excludes JSX, state/local expressions, async promises, branches, throws, implicit returns, and return compatibility diagnostics.

Core coverage locks declared and inferred method return types. Browser e2e commands were omitted because C15 changes only parser/compiler semantic metadata and has no generated runtime artifact impact.

Next is C16: assignment compatibility.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_parser`; and `cargo test -p ezc_core`.


## 148. Assignment compatibility

Completed Phase C16. Direct `this.state = literal` actions now validate in the immutable ASM folding pass against canonical declared semantic types. The existing `EZC1017` diagnostic family is retained while primitive, literal-union, nullable, array, tuple, and object compatibility uses the shared compiler-owned relation.

This removes the legacy frontend primitive-only direct-assignment check so state initialization and direct mutation share canonical semantics. C16 is limited to existing direct serializable assignments; compound mutations, arbitrary expressions, parameter/local values, and final general assignability remain later work.

Core coverage locks canonical direct-action diagnostics, including literal-union and collection incompatibilities. Browser e2e commands were omitted because C16 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C17: compound mutation typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 149. Compound mutation typing

Completed Phase C17. Toggle, increment/decrement, and `+=`/`-=` validation now runs in the immutable ASM pass against canonical semantic types. The stable `EZC1018` through `EZC1021` families remain, while boolean/number target checks and operand checks recognize canonical unions and collection value shapes.

This removes duplicated primitive mutation rules from frontend lowering and keeps mutation semantics with the canonical state-assignment relation. C17 is limited to the current literal mutation language; arbitrary expressions, coercion, parameter/local values, and broader operation semantics remain later work.

Core coverage locks canonical compound-mutation target and operand diagnostics. Browser e2e commands were omitted because C17 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C18: template binding typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 150. Template binding typing

Completed Phase C18. Resolved direct template text-binding entities now receive inferred canonical types from their state/local targets. The immutable ASM pass validates renderability: primitive, literal, null, unknown, and fully renderable union values are accepted, while direct objects, arrays, tuples, and `never` values report `EZC1027`.

This gives templates typed semantic entities instead of string-only binding metadata. C18 deliberately covers text interpolation only; DOM attribute/property contracts, list-item scope, member access, and arbitrary template expressions remain later work.

Core coverage locks a typed text-binding entity and a non-renderable object diagnostic. Browser e2e commands were omitted because C18 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C19: attribute and property typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 151. Attribute and property typing

Completed Phase C19. Dynamic attribute-binding entities now retain their authored attribute name and inferred canonical type. The compiler-owned DOM contract distinguishes `disabled` and `value` properties from the `href` attribute, requiring `boolean`, `string | number`, and `string` respectively; incompatible direct bindings report `EZC1028`.

This establishes deterministic DOM binding semantics without relying on browser coercion or runtime behavior. C19 covers only these initial direct bindings; element-specific contracts, event payloads, spreads, style objects, list scope, and arbitrary expressions remain later work.

Core coverage locks property/attribute distinction, valid `value={number}`, and invalid `disabled`/`href` diagnostics. Browser e2e commands were omitted because C19 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C20: conditional typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 152. Conditional typing

Completed Phase C20. Direct template conditional entities now inherit canonical types from their resolved state bindings. EdgeZero conditions are boolean-only: boolean/literal-boolean/unknown values are accepted, while number and other non-boolean semantic types report `EZC1029`.

This gives conditional compilation predictable semantics without JavaScript truthiness. C20 remains limited to direct resolved conditions; composite expressions, list-item scope, member access, and custom coercion remain later work.

Core coverage locks a typed condition entity, a valid boolean condition, and an invalid number condition. Browser e2e commands were omitted because C20 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C21: list iterable typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 153. List iterable typing

Completed Phase C21. Direct state-backed template lists now carry canonical iterable types, while each list records a typed item scope and optional numeric index scope. Arrays and tuples are accepted as iterable; direct non-array-like state values report `EZC1030`.

This makes list callback variables compiler-owned semantic scope data rather than strings recovered from source. C21 is intentionally limited to direct state-backed array/tuple lists; member access validation, arbitrary iterables, callbacks, and control flow remain later work.

Core coverage locks object item inference, numeric index inference, and a non-array iterable diagnostic. Browser e2e commands were omitted because C21 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C22: member access typing.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 154. Member access typing

Completed Phase C22. Supported list-item member paths now resolve through canonical object types. Successful paths such as `todo.details.region` receive a concrete binding type; failed paths become canonical failed-member records and report `EZC1031`.

This lets templates and tooling query member semantics rather than infer them from strings. C22 is limited to uniquely named list-item roots and dot-member object paths; state/local member reads, unions, optionality, indexes, calls, and arbitrary expressions remain later work.

Core coverage locks nested object-member type inference and missing-member diagnostics. Browser e2e commands were omitted because C22 changes compiler diagnostics and semantic metadata only, not generated runtime artifacts.

Next is C23: computed value type foundation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 155. Computed value type foundation

Completed Phase C23. The parser now retains method decorator and getter metadata. A getter marked `@computed()` receives a canonical computed-value type record based on its existing method return contract, such as `@computed() get remainingCount(): number` becoming a typed `number` computed value.

This establishes the semantic result contract required before a future computed dependency/runtime phase. C23 provides no evaluation, dependency tracking, caching, template reads, async values, or runtime behavior.

Parser and core coverage lock a declared typed computed getter contract. Browser e2e commands were omitted because C23 changes parser/compiler semantic metadata only, not generated runtime artifacts.

Next is C24: action signature foundation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_parser`; and `cargo test -p ezc_core`.


## 156. Action signature foundation

Completed Phase C24. The parser now retains async method metadata, and `@action()` methods receive canonical action signature records. Each record links the existing typed parameter entities to the method's canonical return type and preserves async status.

This creates queryable action input/output contracts before forms or server-action runtime behavior exist. C24 does not resolve generic promises, validate inputs, invoke actions, define transport, or implement server/client execution.

Parser and core coverage lock an async action with typed input and alias-resolved output. Browser e2e commands were omitted because C24 changes parser/compiler semantic metadata only, not generated runtime artifacts.

Next is C25: resource type foundation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_parser`; and `cargo test -p ezc_core`.


## 157. Resource type foundation

Completed Phase C25. The canonical semantic type algebra now includes `Resource`, whose contract explicitly carries typed data and error values, pending state, serializability intent, and a client/server/shared execution boundary.

This gives later resource, resumability, and backend phases one compiler-owned resource contract before any resource declaration or runtime exists. C25 does not parse resources, execute loaders, propagate errors, or enforce serialization/boundaries.

Core representation coverage locks a shared serializable resource type. Browser e2e commands were omitted because C25 changes compiler semantic metadata only, not generated runtime artifacts.

Next is C26: serialization compatibility.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 158. Serialization compatibility

Completed Phase C26. The semantic type system now exposes structural serialization compatibility. Primitives, arrays, tuples, objects, and unions are evaluated recursively; unknown and never are incompatible until proven otherwise; resources require their serializable flag and serializable data/error contracts.

This provides resumability and backend phases a deterministic compiler query without adding source rejection or runtime serialization behavior. Boundary-specific validation remains C27.

Core coverage locks primitive, structural object, unknown, and resource compatibility outcomes. Browser e2e commands were omitted because C26 changes compiler semantic metadata only, not generated runtime artifacts.

Next is C27: server/client boundary compatibility.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 159. Server/client boundary compatibility

Completed Phase C27. The semantic type system now decides whether a value may cross client/server execution boundaries. Serializable ordinary values may cross; resource values must be both serializable and marked shared, while client-only/server-only resources are rejected for opposite-boundary crossings.

This gives backend planning and resumability a deterministic semantic boundary check without source annotations, diagnostics, or code generation changes.

Core coverage locks a server-to-client string crossing and a rejected client-only resource crossing. Browser e2e commands were omitted because C27 changes compiler semantic metadata only, not generated runtime artifacts.

Next is C28: type normalization.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 160. Type normalization

Completed Phase C28. Semantic types now normalize deterministically: nested unions flatten, duplicate members and `never` are removed, singleton unions collapse, and remaining union members sort canonically. ASM type assignments, aliases, list scopes, member accesses, computed contracts, and action signatures all expose normalized types.

This stabilizes equality, caching, inspection, and later assignability. Alias identities and provenance remain distinct from normalized semantic meaning.

Core coverage locks flattening, deduplication, never elimination, and canonical union ordering. Browser e2e commands were omitted because C28 changes compiler semantic metadata only, not generated runtime artifacts.

Next is C29: assignability engine.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 161. Assignability engine

Completed Phase C29. `is_assignable(source, target)` is now the canonical normalized semantic compatibility engine. It centralizes compatibility across primitives, literals, tuples, arrays, objects, unions, and resources; existing compiler validation now calls it. The C10 state-initializer API remains as a forwarding compatibility wrapper.

This removes scattered compatibility rules before further queries and diagnostics depend on them. Generic variance, functions, conditional types, and other unmodeled language forms remain outside the current algebra.

Core coverage locks literal-union, tuple-array, and incompatible primitive compatibility. Browser e2e commands were omitted because C29 changes compiler semantic metadata and validation only, not generated runtime artifacts.

Next is C30: type query APIs.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 162. Type query APIs

Completed Phase C30. Application Semantic Model queries now expose an entity's canonical type, expression-node type, declared type assignments, all type usages, serialization compatibility, and assignability between semantic IDs.

This gives IDE, language-service, inspection, and optimization consumers one read-only semantic type interface. Rich CLI output and composite queries remain later work.

Core coverage locks every query family against declared and inferred state. Browser e2e commands were omitted because C30 changes compiler semantic metadata APIs only, not generated runtime artifacts.

Next is C31: type inspection output.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 163. Type inspection output

Completed Phase C31. `ezc asm` now reports the number of semantic type assignments and exposes each assignment-backed entity's canonical type text, declared/inferred status, semantic origin, and source provenance in JSON and selected-entity text output. Entity-scoped `ezc explain` follows the same ASM inspection route and exposes the same type contract.

This gives compiler tooling a stable inspection surface over canonical types without coupling consumers to Rust debug representation. The slice also preserves `unknown` through list-item member access, avoiding false missing-member diagnostics when an empty collection has no known element shape; existing CLI diagnostic expectations now match the already-canonical assignment and compound-mutation validation behavior.

Inspection currently covers entities with canonical type assignments. It does not yet provide a standalone alias/type-declaration browser, richer type navigation, or a source-summary type inference mode.

Next is C32: type diagnostics.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_core`; and `cargo test -p ezc_cli -- --quiet` (including 17 browser-backed CLI runtime tests). `pnpm test:e2e` and `just e2e` were omitted because C31 changes compiler inspection and diagnostics only; the full CLI suite already exercises its browser runtime contract.


## 164. Type diagnostics

Completed Phase C32. Canonical type diagnostics now have exported stable codes and families. Existing detailed diagnostics for invalid operators, incompatible assignments, non-renderable values, invalid DOM bindings, invalid conditions, non-iterable lists, and missing members use this vocabulary; unresolved declared state types now report `EZC1032` with declaration provenance. `EZC1033` reserves the non-serializable-state family for the upcoming source-level boundary/resource semantics.

This gives tooling and future validation passes deterministic diagnostic categorization without parsing message text or maintaining scattered code strings. Existing detailed codes remain unchanged, preserving compiler contracts.

The current compiler cannot yet emit a non-serializable-state diagnostic because resource and execution-boundary declarations are not lowered from source. Generic/external type resolution and source-level serialization enforcement remain later work.

Next is C33: type-system validation pass.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_core`; and `cargo test -p ezc_cli --test explain -- --quiet`. Browser/e2e commands were omitted because C32 changes compiler diagnostics and semantic metadata only, not runtime generation.


## 165. Type-system validation pass

Completed Phase C33. ASM validation now enforces canonical semantic-type integrity with `EZASM1101` through `EZASM1106`: assignment map-key/subject consistency, canonical type IDs, typed-subject ownership, assignment provenance, resolved assignment origins, and canonical type-alias identities.

This protects all later type consumers from malformed ASM data while keeping validation deterministic and separate from lowering. The existing `EZC1032` diagnostic reports unresolved declared types during lowering; validation checks the canonical model that remains.

Semantic types are value-owned, making recursive type cycles unrepresentable in the current model. Source-level generic/external alias resolution and serialization-boundary validation remain future work.

Next is C34: type-system fixture suite.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_core`; and `cargo test -p ezc_cli --test explain -- --quiet`. Browser/e2e commands were omitted because C33 validates canonical compiler data only.


## 166. Type-system fixture suite

Completed Phase C34. Added fixture 0042, a deterministic two-module semantic type application covering imported object and literal-union aliases, structural object arrays, inferred booleans, typed action input/output, a boolean template condition, typed list item/index scope, and a missing-member diagnostic.

This locks the public ASM inspection contract across the Phase C type model instead of testing each feature only in isolated units. The fixture intentionally retains a missing member to verify deterministic invalid-access reporting.

The current fixture does not cover source-level resource declarations or serializability-boundary diagnostics because those declarations are not implemented yet.

Next is C35: Phase C stability audit.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test -p ezc_core`; and focused `cargo test -p ezc_cli asm_command_covers_the_semantic_type_system_fixture_across_modules -- --exact --quiet`. Browser/e2e commands were omitted because C34 adds compiler fixtures and CLI inspection coverage only.


## 167. Phase C stability audit

Completed Phase C35. Removed the legacy component-graph primitive initializer validator, leaving immutable canonical ASM folding as the single source of semantic type compatibility diagnostics. Phase C inspection, diagnostics, validation, and fixtures now consume canonical semantic types.

Unsupported semantics remain explicit: generic/external type resolution, source-level resource declarations, serialization boundary enforcement, and richer alias navigation are deferred. The initial `ezc asm` and entity-scoped `ezc explain` type inspection contract is frozen around canonical type text, status, origin, and provenance.

Next is the next roadmap phase.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test --workspace` (including 17 browser runtime tests). `pnpm test:e2e` and `just e2e` were omitted as 100% irrelevant to this compiler-only audit; browser behavior was already exercised by the full workspace suite.


## 168. Canonical IR model

Completed Phase D1-A. Added compiler-owned `IntermediateRepresentation`, `IrModule`, `IrFunction`, `IrBlock`, and `IrInstruction` types. Functions, blocks, and instructions retain source provenance; the only instruction form is an inert `Nop` because no lowering occurs in this slice.

This establishes one backend-neutral representation before component, method, state, or template lowering starts. No backend behavior, optimization, control flow, or code generation changes were introduced.

Next is D1-B: lower components into IR modules.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D1-A adds only compiler data structures.


## 169. Component module lowering

Completed Phase D1-B. Components now lower into deterministic source-path IR modules through `lower_components_to_ir`; each module records component IDs while retaining no functions or executable instructions.

This makes the IR compiler-owned application structure without introducing method, storage, template, optimization, or backend lowering.

Next is D1-C: lower methods into IR functions with provenance.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D1-B changes only compiler IR structure.


## 170. Method function lowering

Completed Phase D1-C. Component methods now lower into empty IR functions under their owning source module, preserving canonical method IDs, names, and source provenance.

No blocks, instructions, state storage, templates, or backend generation are lowered yet.

Next is D1-D: state fields into explicit storage operations.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D1-C changes compiler IR structure only.


## 171. State storage lowering

Completed Phase D1-D. State fields now lower into module-level `InitializeStorage` IR instructions, preserving each canonical state ID and authored provenance without inventing a synthetic method or control flow.

No reads, writes, template entrypoints, DOM behavior, or backend generation are lowered yet.

Next is D1-E: template entrypoints into IR.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D1-D changes compiler IR structure only.


## 172. Template entrypoint lowering

Completed Phase D1-E. Render templates now lower into canonical IR entrypoints linking template semantic IDs, render-method IDs, and source provenance. No DOM instructions or backend output are generated.

This completes D1's structural lowering foundation. Next is D2-A: basic blocks.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D1-E changes compiler IR structure only.


## 173. Canonical CFG basic blocks

Completed Phase D2-A. Each lowered IR function now owns one empty entry `IrBlock` and records its stable `IrBlockId`. Entry-block IDs derive from canonical, module-qualified method IDs, so functions from similarly named components in different modules cannot collide.

This establishes the first compiler-owned CFG nodes without lowering statements, adding terminators, or defining any edge semantics. The entry region carries the existing method provenance and is deliberately instruction-free until a later lowering slice supplies executable operations.

Next is D2-B: branch edges.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-A changes canonical compiler IR only.


## 174. Canonical CFG branch edges

Completed Phase D2-B. `IrFunction` now owns explicit `IrBranchEdge` records, each connecting canonical source and destination `IrBlockId` values, selecting a `True` or `False` branch arm, and retaining source provenance. The public `IrBlockId::for_function` constructor provides deterministic IDs for future non-entry block construction.

This is structural CFG connectivity only: no parser control-flow constructs or condition values are lowered yet, and no terminator or backend behavior changes. It gives the upcoming loop and analysis slices a compiler-owned representation that is independent of backend code generation.

Next is D2-C: loop representation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-B changes canonical compiler IR only.


## 175. Canonical CFG loop regions

Completed Phase D2-C. `IrFunction` now owns explicit natural-loop regions. Each `IrLoop` has a stable function-qualified `IrLoopId`, one header block, ordered body and latch blocks, ordered exits, and source provenance. The loop body explicitly includes the header and latches, preserving a deterministic topology for later control-flow analyses.

This slice establishes loop representation only. No source loops are lowered, natural-loop invariants are not validated, and the existing branch-edge shape is not interpreted or rewritten. No backend behavior changes.

Next is D2-D: dominators.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-C changes canonical compiler IR only.


## 176. Canonical CFG dominators

Completed Phase D2-D. Added immutable `compute_dominators` analysis for a canonical `IrFunction`. It derives deterministic `IrDominatorTree` mappings from the function's entry block and declared branch edges. Unreachable blocks naturally dominate only themselves; reachable merge blocks retain precisely the blocks shared by all incoming paths.

The analysis is compiler-owned and leaves IR unchanged. It currently consumes only structural conditional branch edges, with no CFG validation, terminator lowering, loop-back/unconditional edge support, or convenience query API.

Next is D2-E: post-dominators.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-D changes canonical compiler analysis only.


## 177. Canonical CFG post-dominators

Completed Phase D2-E. Added immutable `compute_post_dominators` analysis for a canonical `IrFunction`. It derives deterministic `IrPostDominatorTree` mappings from the reverse branch graph, treating blocks with no declared branch successors as CFG exits. Conditional paths that merge therefore share the merge block as a post-dominator.

The analysis is compiler-owned and leaves IR unchanged. It does not yet model explicit terminators, non-terminating paths, loop-back/unconditional edges, CFG validation, or higher-level queries.

Next is D2-F: CFG queries.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-E changes canonical compiler analysis only.


## 178. Canonical CFG queries

Completed Phase D2-F. `IrFunction` now provides read-only block lookup, deterministic predecessor/successor discovery, and exit detection. `IrDominatorTree` and `IrPostDominatorTree` provide direct block-set lookup plus `dominates` and `post_dominates` predicates. These APIs expose the same canonical branch topology consumed by the immutable analyses.

This completes D2's structural CFG and analysis foundation. The next D3-A definition/use slice exposes an architectural dependency: current instructions have no result values or operands. A compiler-owned IR value/operand identity model must be chosen before definition/use chains are introduced; using `SemanticId` would incorrectly merge authored semantic entities with transient IR values.

Next is D3-A after resolving the canonical IR value/operand identity design.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-F changes canonical compiler query APIs only.


## 179. Canonical IR identity domains

Completed Phase D2-G1. Added separate typed identity domains for lowered instructions (`IrInstructionId`), transient function values (`IrValueId`), and storage slots (`IrStorageId`). Constructors create readable deterministic IDs from an IR block, canonical function, or semantic storage origin respectively.

These types deliberately do not alter current lowering behavior. They establish the identity boundary required before operands, value definitions, and data-flow analysis can be added: `SemanticId` names authored meaning, while the new IR IDs name lowered program instances.

Next is D2-G2: IR operands and constants.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-G1 adds compiler data types only.


## 180. Canonical IR operands and constants

Completed Phase D2-G2. Added a closed `IrOperand` model for transient value references, inline primitive constants, and storage references. `IrConstant` currently supports null, booleans, numeric text, and strings. `SemanticId` is intentionally not an executable operand variant.

Inline constants do not create transient values until a future instruction needs to materialize them. Function/template operands and aggregate constants remain deferred until a concrete lowering requirement exists.

Next is D2-G3: instruction result contract.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-G2 adds compiler data types only.


## 181. Canonical IR instruction results

Completed Phase D2-G3. `IrInstruction` now records a distinct `IrInstructionId`, optional `IrValueId` result, source provenance, optional semantic origin, and operation kind. The operation model now reserves explicit storage load/store plus unary and binary operation forms while preserving the existing `Nop` and `InitializeStorage` behavior.

Instruction and value identity remain separate: a load can produce a transient value, whereas storage initialization, stores, and no-ops produce none. Value records and validation are deferred to the next slices.

Next is D2-G4: canonical value registry.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-G3 changes canonical compiler IR only.


## 182. Canonical IR value registry

Completed Phase D2-G4. `IrFunction` now owns a deterministic `BTreeMap<IrValueId, IrValue>` with direct lookup. Each value records its canonical definition (`Instruction`, `Parameter`, or future `BlockParameter`), semantic type, source provenance, and optional semantic origin.

Current source lowering creates empty value registries; no source operation produces an IR value yet. This reserves block parameters without introducing phi instructions and gives D3 a settled registry contract once validation and real lowering arrive.

Next is D2-G5: storage registry.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-G4 changes canonical compiler IR only.


## 183. Canonical IR storage registry

Completed Phase D2-G5. State fields now lower into explicit `IrStorage` records with distinct storage identity, semantic origin, canonical type, initial serializable value, and provenance. Module initialization instructions now reference `IrStorageId` rather than directly naming a semantic state field.

This preserves the memory-like state model: storage is neither an authored semantic entity nor a transient SSA-like value. Method load/store lowering and integrity validation remain next.

Next is D2-G6: IR value validation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-G5 changes canonical compiler IR only.


## 184. Canonical IR value validation

Completed Phase D2-G6. Added `validate_intermediate_representation`, a deterministic integrity query that checks global instruction uniqueness, instruction-result/value-registry reciprocity, value and storage operands, storage references, and instruction/parameter/block definition ownership. Lowered IR validates cleanly; deliberately unregistered instruction results are rejected with a stable diagnostic.

This completes D2-G's identity and operand foundation. D3 can now consume a validated compiler-owned value model without reusing semantic identities.

Next is D3-A: definition/use chains.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`. Browser/e2e commands were omitted because D2-G6 changes canonical compiler IR only.


## 185. Canonical definition/use chains

Completed Phase D3-A. Added immutable `analyze_definition_uses`, which derives canonical value definitions from the function registry and records each value-consuming instruction operand with its exact operand index. Repeated operands are preserved as distinct use sites.

Next is D3-B: use-definition chains.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 186. Canonical use-definition chains

Completed Phase D3-B. Added immutable `analyze_use_definitions`, resolving every value-consuming operand to its registered canonical definition while preserving the exact instruction and operand index.

Next is D3-C: liveness.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 187. Canonical liveness analysis

Completed Phase D3-C. Added immutable block live-in/live-out analysis using canonical value operands, instruction results, and CFG successors. The fixed-point relation preserves deterministic block and value ordering.

Next is D3-D: reachability.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 188. Canonical reachability analysis

Completed Phase D3-D. Added immutable entry reachability analysis that deterministically partitions canonical blocks into reachable and unreachable sets from the function entry and branch edges.

Next is D3-E: constant propagation.

Verified: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test -p ezc_core`.


## 189. Canonical constant propagation

Completed Phase D3-E. Added immutable propagation for inline primitive constants through current unary and numeric binary IR operations.

Next is D3-F: dead assignment detection.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 190. Canonical dead assignment detection

Completed Phase D3-F. Added immutable detection of unused results from load, unary, and binary operations. Storage writes remain excluded because they are effectful.

This completes D3's current data-flow foundation. Next is D4-A: pass manager.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 191. Canonical optimization pass manager

Completed Phase D4-A. Added ordered compiler-owned pass registration and execution. Every pass receives an immutable IR input and returns a new IR representation.

Next is D4-B: immutable optimization pipeline.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 192. Immutable optimization pipeline

Completed Phase D4-B. Added an immutable ordered optimization pipeline that owns its pass list and returns a fresh IR representation for every run.

Next is D4-C: constant folding on IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 193. Canonical IR constant folding

Completed Phase D4-C. Added an immutable constant-folding pass that rewrites propagated unary and numeric binary results to explicit IR constant instructions without changing their result IDs or provenance.

Next is D4-D: dead code elimination.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 194. Canonical IR dead code elimination

Completed Phase D4-D. Added an immutable fixed-point elimination pass for unused pure-result instructions. Storage initialization and storage writes remain preserved as effectful operations.

Next is D4-E: copy propagation.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 195. Canonical IR copy propagation

Completed Phase D4-E. Added copy operations and an immutable copy-propagation pass that substitutes transitively known value operands while preserving IR instruction and result identity.

Next is D4-F: common subexpression elimination.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 196. Canonical IR common subexpression elimination

Completed Phase D4-F. Added immutable reuse of repeated current pure unary and binary expressions by converting later equivalent instructions into copies of the first result.

Next is D4-G: instruction simplification.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 197. Canonical IR instruction simplification

Completed Phase D4-G. Added immutable simplification of copies sourced from known constants into direct constant instructions.

Next is D4-H: CFG cleanup.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 198. Canonical CFG cleanup

Completed Phase D4-H. Added immutable removal of unreachable blocks and coordinated pruning of their branch edges, loop records, and instruction-defined value records.

Next is D4-I: optimization reports.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 199. Canonical optimization reports

Completed Phase D4-I. Immutable pipelines now return ordered per-pass reports with before/after block, instruction, and value metrics alongside the optimized IR.

This completes D4. Next is D5-A: DOM node IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 200. Canonical DOM node IR

Completed Phase D5-A. Added backend-neutral DOM node identities plus structural element and fragment node forms with source provenance.

Next is D5-B: text IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 201. Canonical DOM text IR

Completed Phase D5-B. Added compiler-owned DOM text records with node ownership and source provenance.

Next is D5-C: binding IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 202. Canonical DOM binding IR

Completed Phase D5-C. Added compiler-owned value-to-DOM-node binding records with canonical value identity and provenance.

Next is D5-D: attribute IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 203. Canonical DOM attribute IR

Completed Phase D5-D. Added compiler-owned DOM attributes with static or canonical value-bound content and source provenance.

Next is D5-E: event IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 204. Canonical DOM event IR

Completed Phase D5-E. Added compiler-owned DOM event records that link a node, event name, authored handler identity, and provenance.

Next is D5-F: conditional IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 205. Canonical DOM conditional IR

Completed Phase D5-F. Added compiler-owned conditional DOM output with canonical condition values, true/optional-false nodes, and provenance.

Next is D5-G: list IR.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 206. Canonical DOM list IR

Completed Phase D5-G. Added compiler-owned repeated DOM output with iterable, item/index value identities, body node, and provenance.

Next is D5-H: DOM IR inspection.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 207. Canonical DOM IR inspection

Completed Phase D5-H. Added deterministic DOM node inspection indexed by canonical node identity.

This completes D5. Next is D6-A: reactive graph.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 208. Canonical reactive graph

Completed Phase D6-A. Added a compiler-owned reactive graph container with provenanced nodes and directed edges.

Next is D6-B: dependency nodes.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 209. Canonical reactive dependency nodes

Completed Phase D6-B. Reactive nodes now classify state, computed, action, and template dependencies while retaining provenance.

Next is D6-C: dependency edges.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 210. Canonical reactive dependency edges

Completed Phase D6-C. Reactive edges now distinguish read dependencies from invalidation dependencies and retain provenance.

Next is D6-D: computed dependency tracking.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 211. Computed dependency tracking

Completed Phase D6-D. Added direct read-dependency lookup for classified computed reactive nodes.

Next is D6-E: action dependency tracking.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 212. Action dependency tracking

Completed Phase D6-E. Added direct read-dependency lookup for classified action reactive nodes.

Next is D6-F: invalidation graph.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 213. Canonical invalidation graph

Completed Phase D6-F. Added direct invalidation-edge lookup from any reactive source node.

Next is D6-G: dependency queries.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 214. Canonical dependency queries

Completed Phase D6-G. Added read-only incoming dependency and outgoing dependent queries over canonical reactive graph edges.

This completes D6. Next is D7-A: update scheduler.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 215. Canonical update scheduler

Completed Phase D7-A. Added a compiler-owned update scheduler foundation that owns canonical reactive dependency topology.

Next is D7-B: dependency ordering.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 216. Canonical dependency ordering

Completed Phase D7-B. Added deterministic topological ordering of acyclic reactive dependencies.

Next is D7-C: batch planning.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 217. Canonical update batch planning

Completed Phase D7-C. Added deterministic topological update batches for independently ready reactive nodes.

Next is D7-D: cycle detection.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 218. Canonical scheduler cycle detection

Completed Phase D7-D. Added deterministic identification of nodes excluded from topological ordering because of dependency cycles.

Next is D7-E: scheduler inspection.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 219. Canonical scheduler inspection

Completed Phase D7-E. Added one read-only scheduler inspection result containing dependency order, deterministic update batches, and cyclic nodes.

This completes Phase D. Phase E has not been started.

Verified: `cargo fmt --all --check`; `cargo clippy -p ezc_core --lib -- -D warnings`; and `cargo test -p ezc_core intermediate_representation::tests`.


## 220. Canonical computed entities

Completed Phase E1. `@computed()` getters now lower to distinct compiler-owned ASM computed entities. Each entity has a stable component-qualified computed ID, component ownership, a link to its existing getter method, canonical method provenance, memoized cache policy, unclassified purity, and a client execution boundary. The canonical semantic-graph export now includes the computed node and ownership edge.

Decision: Computed values are represented separately from methods, rather than overloading the existing method semantic entity or the Phase C typed-result record. The method remains the authored declaration, while the computed entity is the canonical derived-value subject for later dependency, IR, scheduler, runtime, and inspection slices.

Current limitations: E1 does not lower getter bodies, resolve `this` reads, infer or validate computed types beyond the existing Phase C result contract, classify purity, populate reactive graph edges, generate computed IR, or emit runtime metadata/behavior. Purity is explicitly `Unclassified` until E5 validates it.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core computed`; `cargo test -p ezc_core semantic_graph::tests::exports_first_class_computed_nodes`; `cargo check -p ezc_cli`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo clippy -p ezc_cli --bin ezc_cli -- -D warnings`.

Next is E2: lower only the roadmap's supported computed getter expressions into the canonical expression graph. Do not begin reference resolution, type validation, purity analysis, reactive graph population, IR lowering, or runtime work.


## 221. Computed expression lowering

Completed Phase E2. The parser retains one direct supported return expression from each `@computed()` getter, and core lowering places that expression beneath the getter's stable computed semantic ID in the canonical expression graph. The lowered topology preserves literals, unresolved `this.<name>` reads, static member access, arithmetic, comparison, logical, nullish-coalescing, and supported unary operations with canonical source provenance.

Decision: `this.<name>` is represented as an unresolved expression node rather than being assigned a semantic target during lowering. This keeps the expression graph compiler-owned and source-faithful while reserving all Computed → State and Computed → Computed relation construction for E3. Computed expression nodes intentionally receive no semantic type assignment in E2; E4 owns computed typing.

Current limitations: A computed getter must contain exactly one direct supported return statement to lower. Local variables, branches, calls, dynamic or optional member access, mutations, evaluation, diagnostics, reference resolution, type checking, purity, reactive graph edges, IR, and runtime artifacts remain unsupported in this slice.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser`; `cargo test -p ezc_core expression_graph::tests`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_parser --all-targets -- -D warnings`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E3: resolve only direct computed `this` reads to canonical State and Computed semantic references. Do not add typing, purity diagnostics, reactive graph population, IR lowering, or runtime behavior.


## 222. Computed reference resolution

Completed Phase E3. Direct `this.<name>` bases in computed expression graphs now resolve to canonical Computed → State or Computed → Computed references within their owning component. Static member access resolves its base read, so `this.profile.hidden` records the dependency on `profile`. Repeated reads of the same target collapse to one deterministic semantic relation; unresolved names remain relation-free.

Decision: References are entity-to-entity edges from the computed semantic entity, not read-node edges. This gives later reactive and scheduling slices stable dependency topology, while the expression graph retains the individual read nodes and their exact spans. Reference provenance is the computed entity provenance, matching the existing ASM invariant for semantic references.

Current limitations: E3 resolves only direct `this` bases within one component. It emits no unresolved-read diagnostics, does not resolve locals, imported symbols, dynamic/optional member access, methods/actions, or template bindings, and does not add computed types, purity, reactive graph edges, IR, or runtime behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core computed`; `cargo test -p ezc_core semantic_graph::tests::exports_first_class_computed_nodes`; `cargo check -p ezc_cli`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo clippy -p ezc_cli --bin ezc_cli -- -D warnings`.

Next is E4: establish canonical computed typing, declared return validation, serializability, and execution-boundary compatibility. Do not begin purity diagnostics, reactive graph population, IR lowering, or runtime behavior.


## 223. Computed typing

Completed Phase E4. Computed expression nodes and first-class computed entities now receive canonical inferred types. Inference follows E3 state/computed references recursively, resolves supported static object members, and handles literals plus supported arithmetic, comparison, logical, nullish, and unary expressions. Computed type records are keyed by computed entity ID and retain the declaring method, inferred type, declared return type and compatibility, serializability, execution boundary, boundary compatibility, and provenance.

Decision: A computed entity's type represents its inferred expression semantics, while its method's declaration remains a separately retained authored contract. This allows `get invalid(): number { return "wrong"; }` to retain inferred string semantics and a deterministic incompatible-declaration record without prematurely issuing an E19 diagnostic.

Current limitations: Only E2-supported expressions infer types. Unresolved reads, unsupported expressions, and type-inference recursion through a dependency cycle produce `unknown`; cycle diagnostics remain E8. The current computed boundary is client-only, so boundary compatibility is evaluated against the client output surface. Cross-boundary declarations, type diagnostics, purity, reactive graph edges, IR, and runtime behavior remain later work.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core computed`; `cargo test -p ezc_core semantic_type::tests`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E5: reject unsupported computed behavior with deterministic purity diagnostics. Do not add reactive graph population, IR lowering, or runtime behavior.


## 224. Computed purity

Completed Phase E5. Computed values are now classified as `Pure` or `Impure`. The compiler retains ordered purity violations with authored provenance and emits stable `EZC1034` diagnostics for every rejected category: state mutation, action invocation, effects, async execution, resource access, arbitrary calls, and nondeterministic operations.

Decision: Purity facts belong to the computed semantic entity, not a runtime check. Existing parser method-body facts retain direct calls while component semantics identify action/effect/resource targets, allowing the ASM to reject a getter before future reactive or runtime consumers observe it. `EZC1034` is the stable purity-violation code; E19 will broaden diagnostics to the remaining computed failure categories.

Current limitations: E5 detects direct retained call facts and existing state-update/async metadata. It does not build a whole-program call graph, reason about nested control-flow effects, recognize resource declarations outside retained calls, or emit unresolved-read/type/cycle/serialization diagnostics. Reactive graph, scheduler, IR, and runtime behavior remain later work.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_parser --all-targets -- -D warnings`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E6: populate canonical reactive read and invalidation edges from computed declarations. Do not begin transitive analysis, cycle detection, scheduler planning, IR lowering, or runtime behavior.


## 225. Computed reactive graph

Completed Phase E6. The ASM now exposes a deterministic compiler-owned reactive graph containing canonical state and computed nodes. Every E3 Computed -> State or Computed -> Computed reference projects to one direct `Reads` edge from the computed value to its dependency and one reverse `Invalidates` edge from that dependency to the computed value. Nodes and edges preserve canonical IDs and relation provenance, and duplicate relations collapse deterministically.

Decision: The reactive graph is derived solely from existing compiler semantic products rather than discovered at runtime or reconstructed by scheduler consumers. This makes the E3 reference relation the single canonical input to reactive topology, while retaining direct-edge provenance and ordering for inspection.

Current limitations: E6 contains direct state/computed edges only. It performs no transitive dependency/dependent analysis, cycle detection, update scheduling, action/template relation construction, computed IR lowering, or runtime behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core reactive_graph`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E7: compute deterministic transitive dependency and dependent graphs from the canonical reactive graph. Do not diagnose cycles or plan updates yet.


## 226. Computed transitive dependency analysis

Completed Phase E7. The ASM now retains immutable transitive reactive analysis for every canonical state and computed node. The dependency map follows direct `Reads` edges through all reachable computed values, while the dependent map follows direct `Invalidates` edges through all reachable computed values. Both maps include every reactive node and emit stable semantic-ID ordering.

Decision: Transitive topology is a compiler-owned analysis derived from the E6 graph, rather than a runtime walk or a mutation of direct-edge records. E6 remains the canonical source for direct relation provenance; E7 provides deterministic closure metadata for future cycle, scheduler, runtime, and inspection consumers.

Current limitations: E7 computes reachability only. It emits no cycle diagnostics, selects no evaluation order, creates no update batches, adds no action/template edges, and does not lower computed IR or runtime behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core transitive_reactive`; `cargo test -p ezc_core reactive_graph`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E8: detect computed dependency cycles with deterministic diagnostics. Do not plan evaluation/update batches, lower computed IR, or add runtime behavior.


## 227. Computed dependency cycle detection

Completed Phase E8. The compiler now identifies computed dependency cycles as deterministic strongly connected groups over direct computed `Reads` edges. The ASM retains sorted cycle membership, including self-cycles, and emits one stable `EZC1035` diagnostic per group with the first semantic ID's authored provenance.

Decision: Cycle analysis consumes only canonical E6 direct read topology, not invalidation edges, transitive closure maps, source-name discovery, or runtime behavior. This preserves an explainable compiler-owned graph product for E9 planning and later inspection while preventing duplicate diagnostics for one strongly connected group.

Current limitations: E8 detects and reports cycle groups only. It does not reject graph construction, select an evaluation order, create update batches, lower computed IR, produce runtime metadata, or execute computed values.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core dependency_cycles`; `cargo test -p ezc_core transitive_reactive`; `cargo test -p ezc_core reactive_graph`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E9: generate deterministic evaluation order and update batches using the Phase D scheduler. Do not lower computed IR or add runtime behavior.


## 228. Computed evaluation planning

Completed Phase E9. The ASM now retains a compiler-owned computed evaluation plan with deterministic semantic-ID evaluation order, scheduler-produced update batches, and explicitly unplanned nodes. The plan projects only computed-to-computed `Invalidates` edges into the existing Phase D scheduler; independent computed values batch together, and computed dependents follow their prerequisites.

Decision: State nodes remain external invalidation triggers, while the direct E6 graph remains the source of all dependency topology. This avoids treating each paired `Reads`/`Invalidates` relation as a scheduling cycle and reuses the immutable Phase D scheduler rather than introducing a second planning algorithm.

Current limitations: E9 plans only computed-to-computed evaluation. It does not execute getters, allocate caches or dirty flags, recover a plan for scheduler-unplanned nodes, lower computed IR, emit runtime metadata, or perform runtime behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core evaluation_order`; `cargo test -p ezc_core dependency_cycles`; `cargo test -p ezc_core transitive_reactive`; `cargo test -p ezc_core reactive_graph`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E10: lower computed evaluation into canonical IR functions. Do not run optimization or add runtime registry/metadata/behavior.


## 229. Computed IR lowering

Completed Phase E10. Pure computed values that E9 can schedule now lower into canonical IR functions keyed by their computed semantic IDs. Each module records an `IrComputedEvaluation` linking the computed entity, its function, and its produced result value. Lowering consumes canonical expression graph nodes, semantic references, types, and storage IDs to generate typed, provenanced constant, state/computed load, member-access, arithmetic, comparison, logical, nullish, and unary instructions.

Decision: Computed IR functions are distinct from the authored getter-method functions: their function identity is the computed entity ID, and their result is recorded explicitly rather than introducing a terminator before the existing IR control-flow contract evolves. The E10 lowering uses only compiler-owned E2--E9 products and validates that computed result records and computed loads have canonical IR targets.

Current limitations: E10 lowers only pure, scheduled values with fully resolved E2 expression roots. Existing E5 purity and E8 cycle diagnostics exclude invalid evaluations; unresolved reads remain outside IR lowering without a new diagnostic until E19. There is no optimization, execution, cache/dirty-slot allocation, runtime registry, runtime metadata, or runtime behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core lowers_planned_computed_evaluations`; `cargo test -p ezc_core intermediate_representation::tests`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E11: run the immutable optimization pipeline over computed IR. Do not add runtime registry/metadata/behavior.


## 230. Computed IR optimization

Completed Phase E11. Computed IR now runs through a fixed immutable pipeline of common-subexpression elimination, copy propagation, constant folding, instruction simplification, dead-code elimination, and CFG cleanup. `optimize_computed_ir` returns a separate optimized IR report with stable per-pass metrics; E10 lowering is unchanged. Computed evaluation result values remain explicit roots, so dead intermediates can be removed without invalidating evaluation records.

Decision: Optimization consumes canonical E10 IR and returns a new compiler-owned product instead of mutating lowering in place. The existing pipeline supplies the pass contracts, while E11 treats `IrComputedEvaluation.result` as semantically live so a generated evaluation function remains addressable after optimization.

Current limitations: E11 optimizes only the current canonical IR forms. It does not execute computed functions, allocate caches or dirty flags, add runtime records/metadata, plan runtime invalidation, or optimize unsupported/unlowered computed semantics.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core optimizes_computed_ir`; `cargo test -p ezc_core lowers_planned_computed_evaluations`; `cargo test -p ezc_core intermediate_representation::tests`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E12: introduce compiler-owned runtime computed records for cache slots, dirty flags, dependencies, evaluation functions, and serializability. Do not execute them or add runtime invalidation behavior.


## 231. Runtime computed registry

Completed Phase E12. The compiler now builds a deterministic runtime computed registry keyed by computed semantic ID. Every E10-lowered computed value receives a stable cache-slot identity, a stable dirty-flag identity initialized to true, direct canonical dependencies, its evaluation-function ID, E4 serialization compatibility, and authored provenance.

Decision: Runtime computed records derive only from canonical ASM and IR products; they do not inspect source text at runtime or discover dependencies dynamically. Registry membership is restricted to values with a lowered evaluation function, so E5-rejected, E8-unplanned, and unresolved values cannot produce partial runtime records.

Current limitations: E12 is metadata-only. It does not execute evaluation functions, allocate or mutate cache values, read/write dirty flags, emit runtime artifacts, bind templates, or perform invalidation/batching behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core runtime_computed`; `cargo test -p ezc_core lowers_planned_computed_evaluations`; `cargo test -p ezc_core optimizes_computed_ir`; `cargo test -p ezc_core computed`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo check -p ezc_cli`.

Next is E13: allow template bindings to resolve directly to computed semantic entities. Do not generate runtime artifacts or execute/invalidate computed values.


## 232. Template computed bindings

Completed Phase E13. Exact direct `this.<computed>` uses in template bindings, dynamic attributes, conditionals, and keyed-list iterable positions now resolve to deterministic `template-computed` references targeting first-class computed semantic entities. Those template entities inherit the target computed type, semantic graph export carries a typed `template-computed` edge, and selected-entity ASM inspection can filter the canonical relation.

Decision: Template computed resolution mirrors direct template state resolution and keeps `template-computed` as a distinct reference kind. State keeps precedence for an otherwise ambiguous same-named declaration, so E13 does not alter established state binding behavior. All targets, types, provenance, graph edges, and CLI inspection labels derive from canonical ASM metadata; no runtime lookup is introduced.

Current limitations: E13 resolves only exact direct `this.<name>` forms in existing supported template positions. Member access, calls, mixed expressions, template evaluation, static HTML substitution, generated runtime artifacts, cache execution, and invalidation/batching behavior remain unsupported in this slice.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core resolves_template_bindings_to_canonical_computed_entities`; `cargo test -p ezc_cli asm_command_filters_template_computed_references`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo clippy -p ezc_cli --all-targets -- -D warnings`.

Next is E14: emit compiler-generated runtime metadata for computed evaluation. Do not permit runtime dependency discovery or execute/invalidate computed values.


## 233. Computed runtime artifact generation

Completed Phase E14. The compiler now serializes a versioned `computed.runtime.json` build artifact from the E12 runtime registry and the E9 evaluation plan. Each emitted evaluation carries its canonical computed ID, cache-slot ID, dirty-flag ID and initial value, direct compiler-resolved dependencies, evaluation-function ID, and serialization contract. The artifact also records deterministic evaluation order and update batches, restricted to records that actually have lowered E10 evaluation functions.

Decision: Artifact generation is a pure projection of canonical compiler products: the registry supplies evaluation records and dependencies, while the plan supplies ordering and batches. `ezc_cli build` writes this artifact alongside the existing page, template manifest, and runtime stub, but the runtime stub does not load or execute it yet. No source parsing, runtime dependency discovery, cache mutation, or evaluation logic was introduced.

Current limitations: E14 emits metadata only. The current browser runtime does not consume `computed.runtime.json`; no getter executes, no cached value is stored or read, no template output changes, and no invalidation or batching behavior runs. Cyclic, impure, unresolved, and otherwise unlowered values remain absent through the existing E5/E8/E10/E12 filtering.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core runtime_computed`; `cargo test -p ezc_cli build_command_writes_compiler_generated_computed_runtime_metadata`; `cargo test -p ezc_cli build_command_writes_page_manifest_and_runtime_artifacts`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo clippy -p ezc_cli --all-targets -- -D warnings`.

Next is E15: implement cached computed execution driven entirely by compiler-generated dependency plans. Do not add batched invalidation or runtime dependency discovery.


## 234. Cached computed runtime execution

Completed Phase E15. `computed.runtime.json` now uses schema v2 to carry compiler-lowered E10 instruction programs and canonical state-storage initial values alongside the E12 records and E9 schedule. Built pages inline the generated artifact; the browser runtime validates it, executes each listed evaluation in the compiler-provided order at boot, stores the result in the named cache slot, and clears its compiler-initialized dirty state. The runtime exposes the resulting cache metadata through its existing debug state.

Decision: E15 uses a small interpreter over compiler-generated JSON instructions rather than parsing getters, resolving property names, or recursively discovering dependencies at runtime. Computed-to-computed loads consume values already produced by the E9 order. The schema bump makes the added execution payload explicit and leaves the template-manifest schema unchanged.

Current limitations: Execution occurs once during boot. Template binding output is not yet refreshed from computed caches, state writes do not mark values dirty, actions do not invoke evaluation, and the emitted update batches are not consumed. E16 owns exactly one post-action evaluation batch; serialization and resumability remain E17.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core runtime_computed_artifact`; `cargo test -p ezc_core runtime_codegen`; `cargo test -p ezc_cli build_command_writes_compiler_generated_computed_runtime_metadata`; `RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser computed_values_execute_once_from_compiler_generated_runtime_programs -- --nocapture`; `RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser double_binding_counter_increments_in_a_real_browser -- --nocapture`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo clippy -p ezc_cli --all-targets -- -D warnings`.

Next is E16: ensure multi-step actions trigger one computed evaluation batch. Do not add serialization/resumability integration or runtime dependency discovery.


## 235. Computed batched invalidation

Completed Phase E16. `computed.runtime.json` now uses schema v3 and includes a compiler-generated mapping from every runtime state storage to its transitive lowerable computed dependents. Runtime writes update the corresponding compiler-named storage, mark only those emitted computed values dirty, and defer the E9 update-batch flush until the enclosing action has completed. A two-step action therefore refreshes chained computed caches once from final state rather than once per state mutation.

Decision: E16 consumes E7 transitive dependents and E9 update batches as artifact metadata; the runtime does not scan evaluation dependencies, resolve getters, or construct reverse edges. The runtime exposes `computed_update_runs` solely as deterministic debug evidence of completed flushes, while every cache remains keyed by its compiler-provided slot.

Current limitations: Individual state bindings still update per action step, but computed cache execution flushes only after the action loop. Computed template bindings are not refreshed from the cache yet, browser events are not coalesced across dispatches, and serialization/resumability remain E17. No runtime dependency discovery was introduced.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core runtime_computed_artifact`; `cargo test -p ezc_cli build_command_writes_compiler_generated_computed_runtime_metadata`; `RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser multi_step_actions_flush_one_compiler_generated_computed_batch -- --nocapture`; `RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser double_binding_counter_increments_in_a_real_browser -- --nocapture`; `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `cargo clippy -p ezc_cli --all-targets -- -D warnings`.

Next is E17: integrate computed values with resumability and serialization planning. Do not extend inspection APIs, diagnostics, or runtime dependency discovery.


## 236. Serializable computed resumability planning

Completed Phase E17. Canonical resume plans and serialized resume manifests now include a computed-cache section for every value that has both an E12 runtime registry record and E4 `Serializable` compatibility. Each entry retains the computed semantic ID, stable cache-slot ID, stable dirty-flag ID, and compiler-provided initial dirty state. Unresolved, unlowered, impure, cyclic, and non-serializable values remain absent rather than gaining partial resume metadata.

Decision: E17 projects existing compiler products into resumability rather than evaluating getters or inferring cache eligibility in a runtime layer. The E12 registry establishes lowerability and identity while E4 serialization compatibility establishes boundary eligibility. Existing state-instance payloads and diagnostics are unchanged.

Current limitations: The plan identifies caches that a future resume snapshot may carry, but no live cache value is persisted or restored by the browser runtime yet. Computed template output is still not refreshed from cached values, and E17 does not add inspection output, new diagnostics, runtime dependency discovery, or a fixture-suite expansion.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core resume`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is E18: extend ASM and explain output with computed type, dependencies, dependents, evaluation order, purity, serializability, and IR identity. Do not add new diagnostics, runtime discovery, or fixture-suite expansion.


## 237. Computed inspection and queries

Completed Phase E18. ASM inspection schema v2 now adds a computed-only record to full ASM documents and selected entity documents. The record exposes the canonical computed type; E7 transitive dependencies and dependents; zero-based E9 evaluation-order and update-batch positions; E5 purity; E4 serializability; and E10 IR function identity. Selector-driven `ezc_cli explain` continues to route through the same deterministic ASM inspection path as `ezc_cli asm`, and text output presents the same computed facts.

Decision: The CLI projects existing immutable compiler products rather than re-analyzing getters or using runtime discovery. E10 lowering provides the IR identity, and values absent from E9 scheduling or E10 lowering explicitly render `null` schedule/function fields instead of receiving guessed metadata. The schema version advances to v2 because both full and selected inspection documents gain the new field.

Current limitations: E18 only reports established metadata. It adds no diagnostics, supports no new getter/body semantics, makes no runtime changes, performs no runtime dependency discovery, and adds no fixture-suite expansion. Computed template output still consumes the existing runtime path rather than an inspection surface.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_cli asm_and_explain_inspect_canonical_computed_metadata`; `cargo test -p ezc_cli asm_command_emits_deterministic_json_inspection`; `cargo test -p ezc_cli asm_command_inspects_one_semantic_entity`; and `cargo clippy -p ezc_cli --all-targets -- -D warnings`.

Next is E19: introduce stable compiler diagnostics for invalid computed declarations, unsupported bodies, unresolved reads, purity violations, type mismatches, dependency cycles, and serialization violations. Do not begin the fixture suite, stability audit, or runtime extensions.


## 238. Computed diagnostics

Completed Phase E19. Computed diagnostics now have a stable compiler-owned `EZC1034`--`EZC1040` catalog. Existing E5 purity violations and E8 dependency cycles retain their established codes. The ASM now additionally diagnoses invalid non-getter `@computed()` declarations, unsupported getter bodies, unresolved `this.<member>` reads, incompatible declared return types, and non-serializable computed results. Every record carries canonical source provenance and is emitted deterministically from immutable compiler products.

Decision: A central `ComputedDiagnosticCode` maps the stable codes, while diagnostics are projected after semantic typing from the existing component metadata, canonical expression graph, purity classification, cycle analysis, and computed type records. This preserves one compiler authority for diagnostics and prevents CLI or runtime rediscovery.

Current limitations: E19 diagnoses only forms already represented by current parser and ASM lowering. It does not support additional getter body forms, change runtime execution or validation, infer dependencies dynamically, or add the broad fixture matrix. The E20 fixture suite remains the next slice.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core reports_stable_diagnostics_for_computed_semantics`; `cargo test -p ezc_core computed`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is E20: add comprehensive compiler and browser fixtures for arithmetic, chains, diamonds, batching, template bindings, cycles, compile-time folding, serialization, and multi-file cases. Do not begin the stability audit or runtime extensions.


## 239. Computed fixture suite

Completed Phase E20. The Phase E fixture suite now spans template bindings, runtime chains, batched invalidation, arithmetic, diamond dependencies, cycles, immutable constant folding, structural serialization, and multi-file semantic identity. New fixture inputs `0046`--`0051` provide the arithmetic, diamond, cycle, folding, serialization, and two-module cases; existing `0043`--`0045` continue to cover template bindings, chain execution, and batching.

Decision: Compiler fixture assertions consume the canonical ASM, diagnostics, optimized IR, resume plan, and multi-file inspection output. Real-browser coverage is limited to runtime execution paths: the existing chain and batching fixtures plus a new diamond probe that verifies both prerequisites and the dependent refresh once after an action. No fixture invokes runtime discovery or depends on ad-hoc source interpretation.

Current limitations: The fixtures validate currently supported computed syntax and compiler products only. Computed template bindings remain statically rendered by the existing runtime path rather than dynamically consuming cache changes, and E20 adds no language semantics, runtime behavior, or fixture-generated diagnostics beyond E19.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_cli computed_fixture_suite`; `cargo test -p ezc_cli asm_command_filters_template_computed_references`; `RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser computed -- --nocapture`; and `cargo clippy -p ezc_cli --all-targets -- -D warnings`.

Next is E21: perform the Phase E stability audit. Remove duplicate logic; verify canonical ASM ownership, reactive graph and scheduler consumption, and IR validation; freeze inspection contracts; document unsupported semantics; and update the handoff and Progress Log. Do not add new runtime behavior or roadmap features.


## 240. Phase E stability audit

Completed Phase E21 and Phase E. The audit removed duplicate computed-diagnostic assembly and centralized the ASM inspection schema-v2 constant. It also corrected ASM validation to recognize canonical expression-graph nodes as valid semantic-type subjects through their own source provenance, eliminating false validation diagnostics for computed expression assignments. A dedicated audit test verifies normal and component-graph ASM construction, computed ownership, reactive read/invalidation projection, scheduler-plan derivation, and canonical IR validation against the diamond fixture.

Decision: Expression graph nodes remain first-class compiler products but not generic semantic entities. Type validation therefore accepts either an ASM entity's provenance or an expression node's canonical provenance, preserving the established inspection and ownership boundaries. Computed diagnostics now assemble through one shared helper in both construction paths, and inspection schema v2 is frozen behind a single CLI constant.

Current limitations: Computed support remains intentionally limited to the documented getter expressions and purity contract. The runtime still performs no dependency discovery; computed template bindings do not refresh from cache changes; and cache values are not persisted or restored. Any incompatible inspection change requires a new schema version and a new roadmap slice.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser`; `cargo test -p ezc_core`; `cargo test -p ezc_cli`; `cargo clippy --workspace --all-targets -- -D warnings`; `RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser -- --nocapture`; `pnpm test:e2e`; `just e2e`; and `RUST_TEST_THREADS=1 cargo test --workspace`.

Phase E is complete. Await a new explicit roadmap slice before adding compiler, runtime, or inspection behavior.


## 241. Canonical effect entities

Completed Phase F1. `@effect()` methods now produce first-class compiler-owned `Effect` entities with a stable `component/effect:name` identity, direct component ownership, an authored method link, source provenance, a client execution boundary, and the fixed language policy `AfterInitialRenderAndCompletedActionBatch`. The canonical ASM, semantic graph export, and generic CLI entity-kind handling all project the same entity rather than interpreting decorators independently.

Decision: Effects are terminal reactive consumers, but F1 records only their semantic identity and timing contract. This keeps future dependency analysis, trigger planning, scheduler placement, IR lowering, and runtime metadata attached to one compiler entity rather than to callbacks or runtime-discovered observers.

Current limitations: F1 does not lower effect statements or expressions; resolve reads; assign types; validate mutation, async, cleanup, or capabilities; add reactive graph relations; plan triggers; schedule or execute effects; emit IR/runtime metadata; or add effect-specific inspection and diagnostics. Template events remain action-only under the existing semantics.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core`; and `cargo clippy --workspace --all-targets -- -D warnings`.

Next is F2: lower supported imperative effect bodies, reusing the canonical expression graph where possible and introducing canonical statement lowering only where expressions cannot represent the body. Do not resolve references, type effects, validate effect semantics, or add reactive/runtime behavior.

Blocking decision before F2: the roadmap does not define the canonical effect-body statement vocabulary. In particular, the compiler needs an explicit contract for external member assignments such as `document.title = this.title`, capability calls, return/cleanup candidates, and the retention of unsupported statements for F5 diagnostics; it also needs to know whether assignment and call operands enter the existing expression graph in F2. This representation determines the future F3 reference, F5 diagnostic, F10 IR, and capability-metadata contracts. Per the Phase F stop condition, no F2 body AST or lowering has been invented; await guidance before continuing.


## 242. Effect body lowering

Completed Phase F2 using the supplied body-lowering contract. Every `@effect()` now owns an ordered `EffectBody`, whose stable `effect/statement:<index>` records preserve statement provenance and reference canonical expression-graph operands. The parser and component graph lower static-member assignments, direct static calls, and final bare returns; value returns and unsupported local, branch, loop, block, exception, compound-assignment, and cleanup-return forms remain explicit structured records for later diagnostics. Fixture `0052-effect-body-lowering` covers the required source forms.

Decision: Effect statements and expressions use separate identity domains. Statements are effect-owned operation records, while each value operand is lowered into the existing expression graph under the owning effect ID. This preserves one expression topology without representing an imperative body as a single synthetic expression root.

Current limitations: F2 does not resolve identifiers or `this` reads, determine whether a target is reactive state or an external host, classify calls as capabilities/actions/effects, assign types, validate unsupported behavior, create dependencies, add reactive graph edges, schedule effects, lower IR, or generate runtime artifacts. No runtime dependency discovery has been added.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser`; `cargo test -p ezc_core`; and `cargo clippy --workspace --all-targets -- -D warnings`.

Next is F3: resolve state and computed reads from the canonical effect expression operands through existing semantic references. Do not validate capabilities or effect semantics, construct reactive graph edges, schedule effects, or add runtime behavior.


## 243. Effect reference resolution

Completed Phase F3. Canonical effect expression operands now resolve direct `this.<name>` reads to existing state and computed entities through `EffectState` and `EffectComputed` semantic references. References are deduplicated per effect/target pair and retain authored operand provenance.

Current limitations: F3 does not resolve or classify capability call targets, validate state mutation or cleanup, assign types, build reactive graph edges, schedule effects, lower IR, or generate runtime behavior.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core`; and `cargo clippy --workspace --all-targets -- -D warnings`.

Next is F4: type effect expressions and validate boundary and capability compatibility without adding effect scheduling or runtime behavior.

Blocking decision before F4: the compiler has no canonical capability registry, host-member signature model, or capability boundary contracts. F4 requires those products to type `document.title = ...` and calls such as `storage.set(...)` or `analytics.track(...)`, and its choice governs F5 validation, F10 effect IR, and F12/F13 runtime metadata. Per the Phase F stop condition, no ad hoc capability allowlist or host typing has been introduced; await the initial registry/signature contract and unknown-identifier policy.


## 244. Effect typing and capability registry

Completed Phase F4 using the supplied initial capability and unknown-identifier contract. `effect_capability.rs` defines immutable registry version 1 with stable capability and operation IDs, exact case-sensitive paths, fixed/variadic signatures, client boundaries, serialization policies, and runtime-lowering identities. The complete initial registry is `document.title`, `console.{log,info,warn,error}`, `localStorage.{setItem,removeItem}`, and `sessionStorage.{setItem,removeItem}`.

The ASM now assigns canonical types to all effect expression operands and stores an immutable `EffectStatementTypeRecord` by statement identity. It records operation classification, operand and target types, signature/boundary/serialization compatibility, registry operation identity, and source provenance. Unknown external paths remain unknown rather than permissive `any`; their arguments and values still receive ordinary compiler types. Calls rooted in `this` classify separately as component action/effect/method/unresolved calls, and assignments to known reactive state retain ordinary assignment compatibility evidence for F5.

Decision: Registry v1 is the sole external-capability authority. Parser lowering, ASM typing, future validation, IR, runtime artifacts, diagnostics, and inspection must consume this one product rather than recognize globals or static paths independently. ASM validation verifies every effect type record against the canonical statement and registry.

Current limitations: F4 records facts only. It emits no effect semantic diagnostics, rejects no authored form, creates no reactive graph relations, scheduler plan, IR, optimization, runtime registry/artifact, execution, batching, resumability, or inspection schema changes. No runtime capability discovery is introduced.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F5: consume F4 statement records to validate synchronous terminal effect semantics, prohibited reactive mutation/action/effect calls, cleanup/value returns, unsupported statements, capability compatibility, and boundary/serialization failures. Do not introduce graph, scheduler, IR, runtime, or inspection behavior.


## 245. Effect semantic validation

Completed Phase F5. Every first-class effect now carries immutable compiler-owned `EffectValidation` and ordered `EffectSemanticViolation` facts. The classifier consumes only canonical F4 statement type records plus existing method metadata, and marks effects invalid for async execution, reactive state mutation, action/effect/component-method invocation, unresolved component calls or assignments, unknown external capabilities, incompatible recognized capability signature/boundary/serialization contracts, value returns, and retained unsupported statements.

Decision: F5 performs semantic rejection as entity facts rather than introducing user-facing diagnostic codes. This matches the existing computed-purity pattern while leaving Phase F18 to project one stable effect diagnostic catalog from the same immutable facts. Later graph, scheduler, IR, and runtime slices can therefore exclude invalid effects without revalidating source or inspecting runtime behavior.

Current limitations: F5 does not yet emit effect diagnostics, add reactive graph edges, compute transitive topology or triggers, schedule or lower effects, optimize, generate runtime metadata, execute effects, batch updates, resume, inspect effects, or discover dependencies at runtime.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F6: project valid effect dependencies from existing `EffectState`/`EffectComputed` references into the canonical reactive graph. Do not create trigger/scheduler/IR/runtime behavior or duplicate dependency inference.


## 246. Effect reactive graph integration

Completed Phase F6. Valid effects now appear as terminal `Effect` nodes in the existing canonical reactive graph. F3 `EffectState` and `EffectComputed` references project to ordinary `Reads` edges from each valid effect to its compiler-resolved dependencies, with inverse `Invalidates` edges from those dependencies back to the effect. Invalid F5 effects are intentionally omitted.

Decision: Effects reuse the existing graph's directionality rather than introducing a parallel effect dependency structure. They consume reactive inputs but do not produce invalidations; computed cycle detection and computed scheduling remain explicitly filtered to computed nodes.

Current limitations: F6 does not add transitive effect closure records, trigger classification, scheduling, IR, optimization, runtime metadata, execution, batching, resumability, inspection, diagnostics, or runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F7: expose deterministic transitive effect dependencies/dependents from the existing graph analysis. Do not re-infer dependencies or schedule/execute effects.


## 247. Effect transitive analysis

Completed Phase F7. `EffectReactiveAnalysis` now projects the existing canonical reactive transitive closure into valid-effect records with semantic-ID dependencies and dependents. Effects retain their terminal contract: their dependency closure includes direct and transitive state/computed inputs, while their dependent closure is empty. Invalid effects receive no analysis record.

Decision: F7 reuses the F6 graph analysis instead of walking expression nodes or maintaining a parallel effect topology. This gives F8 a deterministic compiler-owned input set for trigger derivation.

Current limitations: F7 adds no triggers, scheduler placement, IR, optimization, runtime metadata, execution, batching, resumability, inspection, diagnostics, or runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F8: derive compiler-owned action-batch trigger eligibility from effect dependency closure. Do not schedule or execute effects.


## 248. F8 trigger-identity decision

Paused before Phase F8. The existing component model lowers one authored action method into one or more individual `ComponentAction` state-write records, while the effect language contract requires exactly one execution decision per completed action batch. F8 must define whether trigger records are keyed by authored method/batch identity with dependency-change deduplication, or by individual writes with later F9/F15 reconstruction.

This choice governs F8 trigger facts, F9 scheduler placement, F12 runtime registry identity, and F15 completed-batch execution. Per the Phase F stop condition, no trigger mapping, scheduler policy, runtime metadata, or execution has been introduced. Await guidance on canonical batch identity, multi-write deduplication, and no-dependency-change behavior.


## 249. Effect trigger derivation

Completed F8 using the supplied action-batch contract. Authored `@action()` methods now own canonical `ActionBatch` records with ordered write records, deduplicated written states, method provenance, and stable batch IDs. `EffectTriggerPlan` lists every valid effect for initial rendering and derives at most one eligible trigger per `(ActionBatchId, EffectId)`, retaining matched-state evidence.

F8 intersects the batch written-state set with F7 transitive state dependencies, so direct and computed-mediated state dependencies share one rule. It does not inspect runtime values or schedule execution.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F9: integrate F8 eligibility with computed prerequisites in the existing scheduler.


## 250. F9 computed-prerequisite decision

Paused before Phase F9. F8 establishes action-batch effect eligibility, but F9 must still define the compiler-owned computed-prerequisite membership for initial rendering and each eligible action batch: every transitive computed dependency, only computed values invalidated by that batch's written states, or a shared computed flush. This governs scheduler plan membership/order, F10 lowering, and F15 runtime behavior.

Per the Phase F stop condition, no scheduler positions, computed/effect batch plans, IR, runtime metadata, or execution has been introduced. Await the prerequisite selection rule for initial and completed-action-batch schedules.


## 251. Effect scheduler integration

Completed Phase F9 using the supplied computed-prerequisite contract. `EffectExecutionPlan` now derives immutable initial and action-batch schedules solely from F7 reactive closure, F8 eligibility, E7 invalidation closure, and E9 computed batches. Initial schedules retain every executable direct/transitive computed dependency required by initially eligible effects, then place all schedulable effects after an explicit `AfterInitialRender` boundary. Action schedules retain only required computed values invalidated by that action batch, preserving E9 source-batch order and omitting unrelated members.

Decision: F9 does not create a second computed ordering algorithm. It records filtered references to existing E9 batches and uses the existing Phase D scheduler for terminal effect batches, where independent effects share one stable semantic-ID-ordered batch. Effects whose required computed values are unavailable are explicitly unplanned; direct-state-only and dependency-free effects remain schedulable.

Current limitations: F9 is compiler planning only. It adds no effect IR, optimization, runtime registry/artifact, equality-based change detection, execution, batching runtime, resumability, inspection surface, diagnostics, or runtime dependency discovery. The global computed update plan remains intact; F9 only identifies the effect-required subset for later composition.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F10: lower valid effect bodies into canonical IR from F2 statements, F4 capability facts, and F9 plan identities. Do not execute effects, emit runtime metadata, or rediscover dependencies.


## 252. F10 effect-IR contract boundary

Paused before Phase F10. The referenced `phase_f_effects_roadmap.md` file is not present in the worktree, and the retained supplied contracts cover F2 and F4 rather than the F10 instruction shape. F10 must determine how valid external-member assignments and registry-approved capability calls lower into the existing IR, including F4 operation/lowering identity, F2 operand values, terminator/result semantics, and linkage to F9 execution plans.

This decision fixes F11 optimization boundaries, F12 runtime registry identity, F13 artifact representation, and F15 execution behavior. Per the Phase F stop condition, no effect IR, optimizer change, runtime artifact, execution behavior, or runtime dependency discovery has been introduced. Await the canonical F10 IR-lowering contract.


## 253. Canonical effect IR lowering

Completed Phase F10 using the supplied effect-IR contract. Every F5-valid effect scheduled by F9 now receives at most one compiler-owned `IrFunction` keyed by the effect semantic ID and one `IrEffectExecution` record. The authored method function remains separate. F10 reuses the existing expression-to-IR path for operand values, canonical storage IDs for state reads, and canonical computed IDs for computed reads.

The shared IR now has generic, explicitly observable void `CapabilityAssign` and `CapabilityCall` instruction forms. They carry only F4 registry operation IDs, retain statement provenance and authored order, and produce no semantic result. F10 validation checks the IR structure plus the canonical F4/F5/F9 facts without rematching source capability paths. Invalid, unsupported, unplanned, or operand-unlowerable effects produce no partial function.

Current limitations: F10 creates compiler IR only. It does not optimize effects, create a runtime registry/artifact, dispatch browser capabilities, execute or batch effects, compare runtime values, add cleanup/async control flow, expose a new inspection surface, or introduce runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F11: run the existing immutable optimization pipeline over effect IR while preserving capability operations as observable ordered roots. Do not alter scheduling, generate runtime metadata, or execute effects.


## 254. Effect IR optimization

Completed Phase F11 by adding `optimize_effect_ir`, which reuses the existing immutable pass pipeline over a projection containing only F10-lowered effect functions. The optimized functions are merged back into a clone of the complete IR by semantic ID, so authored-method and computed IR remain byte-for-byte outside the slice's scope.

Pure capability operands can now fold through the shared pipeline. `CapabilityCall` and `CapabilityAssign` remain explicitly observable, void roots with their authored instruction order intact; their direct value operands continue to participate in canonical use/liveness tracking. The focused fixture proves `1 + 2` folds before a console call while console, title assignment, and the following console call preserve their exact order.

Current limitations: F11 optimizes compiler IR only. It does not alter F8 eligibility or F9 schedules, create a runtime registry/artifact, dispatch browser capabilities, execute or batch effects, compare runtime values, add cleanup/async control flow, expose a new inspection surface, or introduce runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib` (161 tests); and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F12: build deterministic compiler-owned runtime effect records from the existing F8 trigger, F9 prerequisite, and F10 optimized IR products. Metadata only; do not execute effects.


## 255. Runtime effect registry

Completed Phase F12 by adding `RuntimeEffectRegistry` and `RuntimeEffectRecord` as compiler-owned metadata products. Registry membership comes exclusively from F10/F11 `IrEffectExecution` records, so invalid, unsupported, unplanned, or otherwise unlowered effects cannot receive a runtime record.

Each record preserves the canonical effect ID, IR function identity, F1 timing policy, client execution boundary, F10 capability operation identities in authored order, and effect provenance. Initial-render records retain F9's render boundary and batch position. Completed-action records retain the F8 action-batch identity and matched-state evidence, plus the F9 batch position and prerequisite batches. Per-effect computed prerequisites are projected by intersecting existing F7 computed dependencies with F9's already scheduled requirements; no source/body expression is rewalked and no dependency is inferred anew.

Current limitations: F12 publishes metadata only. It does not serialize an artifact, interpret effect IR, dispatch browser capabilities, execute initial or completed-action effects, batch at runtime, compare runtime values, add cleanup/async behavior, expose a new inspection surface, or introduce runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib` (162 tests); and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F13: emit a versioned compiler-generated runtime effect artifact from F9 plans, F10/F11 IR, and F12 records. Do not match raw source paths or execute effects.


## 256. Runtime effect artifact

Completed Phase F13 by adding schema-v1 `RuntimeEffectArtifact` output and deterministic JSON serialization. The artifact is built from F12 runtime records and F10/F11 effect IR; it contains initial and completed-action trigger metadata, required computed prerequisite batches, execution policy/boundary, and ordered capability programs.

Pure operand instructions reuse the existing computed-artifact instruction encoding. F10 `CapabilityCall` and `CapabilityAssign` serialize as explicit ordered roots with only their canonical F4 operation ID, value operands, and registry-supplied runtime-lowering ID. The artifact contains no source provenance or raw static capability path, and capability lowering resolves only from the immutable operation ID through the registry.

Current limitations: F13 emits compiler data only. It does not integrate the artifact into a page, interpret IR, dispatch browser capabilities, execute initial or completed-action effects, compare runtime values, add cleanup/async behavior, expose a new inspection surface, or introduce runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib` (163 tests); and `cargo clippy -p ezc_core --all-targets -- -D warnings`.

Next is F14: consume the compiler-generated artifact and execute each valid initial effect once after initial rendering. Do not add completed-action execution until F15.


## 257. Initial effect runtime execution

Completed Phase F14 by embedding `effect.runtime.json` in generated pages and extending the shared browser runtime to consume schema-v1 effect artifacts. After state setup, rendered-DOM initialization, and the existing compiler-generated computed initialization plan, the runtime executes every compiler-provided initial effect batch exactly once.

Pure effect operands use the existing compiler artifact evaluator. Capability instructions dispatch only through their compiler-emitted runtime-lowering IDs; the runtime neither matches authored static paths nor discovers dependencies. Effects are grouped by the F13 initial batch position, preserve artifact semantic-ID order within a batch and authored capability instruction order, and publish deterministic `window.__EDGEZERO__.initial_effect_runs` evidence.

The build command now writes `effect.runtime.json` and embeds the same artifact in `index.html`. The focused CLI check validates artifact emission and embedded metadata; the real browser fixture proves computed initialization before effect execution, one initial run, ordered console/title/storage dispatch, observable title/storage synchronization, and no diagnostics.

Current limitations: F14 executes initial effects only. It does not execute action-batch trigger plans, reconstruct dependencies or eligibility at runtime, compare runtime values, add cleanup/async behavior, expose a new inspection surface, or introduce runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib` (163 tests); `cargo test -p ezc_cli --test explain build_command_writes_compiler_generated_effect_runtime_metadata`; `cargo test -p ezc_cli --test runtime_browser initial_effects_execute_once_from_compiler_generated_runtime_programs`; and `cargo clippy --workspace --all-targets -- -D warnings`.

Next is F15: after a completed action, flush the existing compiler-generated computed update plan and execute that action batch's explicit effect plan once. Do not reconstruct trigger eligibility or dependencies at runtime.


## 258. F15 action-batch activation boundary

Paused before Phase F15. F8 establishes canonical completed-action identity as `ActionBatchId`, and F12/F13 retain that identity for every eligible effect trigger. The current browser runtime, however, receives a template `ManifestAction.method` string and component display name only; it has no compiler-emitted `ActionBatchId` to use after one authored action completes.

Deriving the batch by parsing semantic-ID suffixes, rematching runtime method strings to compiler products, or observing state writes would violate the authored-action identity decision and the rule against runtime eligibility/dependency reconstruction. F15 therefore needs a canonical compiler-emitted activation bridge from a template action invocation to the corresponding F8 action-batch identity.

This choice affects the template-manifest/effect-artifact schema boundary and F15/F16 runtime and resumability consumers. No action-triggered effect execution, runtime batch lookup, trigger derivation, or dependency discovery has been introduced. Await guidance on the canonical home and versioning of this activation bridge.


## 259. Template action activation bridge

Completed Phase F15 using the supplied schema-v2 template-manifest contract. Every compiler-emitted v2 template action binding now retains both its canonical action implementation ID and the existing F8 `ActionBatchId`. Lowering consumes the F8 authored-method-to-batch mapping directly; it does not recreate a batch ID from a method name, component, write record, or dependency.

The browser runtime validates v2 action bindings before boot, enters the exact compiler-provided action batch for an event, executes its state writes, flushes the existing computed update plan, then selects F13 completed-action effect batches only by that exact `action_batch_id`. It executes each compiler-provided batch once, preserves compiler batch ordering, records deterministic evidence, and clears active batch state. Missing effect plans produce no effect work; initial effects are never replayed.

Decision: Schema v2 is the canonical event-to-batch bridge. Legacy schema-v1 manifests remain readable only where the effect artifact has no completed-action plans; pairing a v1 manifest with such plans fails validation and requires a rebuild. This preserves backward action execution without allowing fallback batch inference.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib` (164 tests); `cargo test -p ezc_cli --test explain` (119 tests); `cargo test -p ezc_cli --test runtime_browser completed_action_batches_execute_compiler_planned_effects_once -- --exact --nocapture`; `cargo clippy --workspace --all-targets -- -D warnings`; and `git diff --check`.

Next is F16: add compiler-owned effect resumability planning on top of the completed F15 execution boundary. Do not introduce runtime dependency discovery or reconstruct action-batch eligibility.


## 260. Effect resumability planning

Completed Phase F16. Compiler-owned resume plans and serialized schema-v2 resume manifests now include deterministic effect activation records for every valid F12 runtime effect with initial-plan membership. Each record retains the canonical effect ID, a stable component-qualified `EffectActivationSlotId`, initial `Pending` activation status, F1 execution policy and boundary, runtime function identity, F9 initial render/batch membership, and canonical completed-action `ActionBatchId` references.

Decision: Effect resumability stores compiler-owned activation lifecycle metadata, not external capability state or runtime-discovered behavior. Effect identity, activation-slot identity, runtime-function identity, and action-batch identity remain separate domains. The three-state `Pending`/`Completed`/`Failed` model avoids treating a failed initial effect as successfully activated.

Current limitations: F16 plans and serializes metadata only. It does not persist or restore live browser activation state, replay or suppress effects at runtime, serialize capability/DOM/storage state, add async or cleanup lifecycle semantics, change inspection, emit user-facing diagnostics, or introduce runtime dependency discovery. Legacy v1 manifests remain readable only for consumers not restoring effects; effect-restoration validation requires schema v2.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core resume` (5 tests); `cargo test -p ezc_core effect` (14 tests); `cargo clippy -p ezc_core --all-targets -- -D warnings`; and `git diff --check`.

Next is F17: expose canonical effect validity, dependencies, triggers, scheduling, capabilities, IR, runtime, and resumability metadata through ASM and explain inspection. Do not add diagnostics or new runtime behavior.


## 261. Effect inspection

Completed Phase F17. ASM inspection schema v3 now exposes one immutable effect-specific record for every canonical effect entity. It projects F5 validation and stable violation categories, F6 direct dependencies, F7 transitive topology and terminal dependents, F8/F9 initial and completed-action trigger/schedule membership, ordered F10/F11 capability and IR facts, F12 runtime registration, and F16 activation-slot resumability metadata.

Decision: `EffectInspection` is a core compiler projection shared by full ASM documents, selected ASM documents, and selected `explain` documents. It reads existing compiler products only; the CLI does not rewalk bodies, rematch capability paths, rebuild dependencies, or create an independent report model. Full and selected documents advance together to schema v3.

Current limitations: F17 inspection exposes internal semantic facts, not user-facing diagnostic codes. It does not change effect validation, scheduling, artifacts, runtime execution/restoration, capability state, or dependency discovery. Invalid and unplanned effects remain explicit but receive no invented IR, runtime, or resume identity.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core effect` (15 tests); `cargo test -p ezc_cli --test explain` (120 tests); `cargo clippy --workspace --all-targets -- -D warnings`; and `git diff --check`.

Next is F18: project the existing F5 validation facts into a stable user-facing effect diagnostic catalog. Do not create new semantic validation or runtime behavior.


## 262. F18 diagnostic metadata boundary

Paused during Phase F18. The existing canonical `ComponentDiagnostic` model retains only a code, message, and primary provenance. The supplied F18 contract requires each effect diagnostic to retain the effect semantic ID, optional statement ID, and secondary labels, while explicitly forbidding a second effect-reporting model.

Decision needed: evolve `ComponentDiagnostic` with optional shared structured metadata (recommended), or identify an existing shared diagnostic envelope to carry those fields. The choice affects serialized check/ASM diagnostic documents and future compiler diagnostics, so it is an architectural diagnostic-contract change. A preliminary projection of F5/F9 facts compiles and preserves existing CLI behavior, but F18 is not complete and must not be committed until the canonical metadata carrier is chosen.


## 263. Effect diagnostics

Completed Phase F18 after approving the shared diagnostic-model evolution. `ComponentDiagnostic` is now the sole compiler diagnostic envelope and retains severity, optional typed effect and effect-statement identities, and deterministic secondary source labels. Existing diagnostics retain absent identities and empty labels.

The new central `EffectDiagnosticCode` catalog projects only existing F2-F5 validation and F9 unplanned-prerequisite facts. It emits stable `EZC1041` through `EZC1051` identities, applies deterministic root-cause suppression per statement, retains canonical primary provenance, and never rewalks bodies, rematches capabilities, or changes effect/runtime semantics. F9 reports for the same effect are coalesced into one `EZC1051` record with stable computed-value evidence.

Decision: shared diagnostic structure is versioned at its actual serialized boundaries. `check --format json` advances from schema v1 to v2. Full ASM, selected ASM, and selected `explain` advance from inspection schema v3 to v4 and render the same compiler diagnostic shape. ASM validation enforces effect/statement ownership, primary/secondary canonical provenance, and label ordering/deduplication.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core effect` (18 tests); `cargo test -p ezc_cli --test explain` (121 tests); `cargo clippy -p ezc_core --all-targets -- -D warnings`; `cargo clippy -p ezc_cli --all-targets -- -D warnings`; and `git diff --check`.

Next is F19: expand end-to-end effect fixtures around the now-stable diagnostic catalog and inspectable/runtime pipeline. Do not introduce new diagnostics semantics or runtime dependency discovery.


## 264. Effect fixture expansion

Completed Phase F19 without adding language or runtime behavior. Fixture 0054 covers document-title synchronization, console operations, local/session storage, direct-state and computed-mediated dependencies, multiple effects sharing one computed prerequisite, dependency-free initial activation, distinct authored action batches, and repeated state writes coalesced into one completed-action batch. CLI tests assert deterministic ASM output and the exact compiler-owned trigger/prerequisite facts.

Fixture 0055 compiles two effect-bearing modules and asserts their effect semantic IDs remain module-qualified. Existing fixture 0052 supplies invalid effects and capability failures, while 0053 remains the real-browser fixture for initial and post-action execution. Focused Chrome tests prove the compiler-generated initial plan executes once and completed action effects consume the exact action-batch plan once.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_cli --test explain`; `cargo test -p ezc_cli --test runtime_browser initial_effects_execute_once_from_compiler_generated_runtime_programs -- --exact`; `cargo test -p ezc_cli --test runtime_browser completed_action_batches_execute_compiler_planned_effects_once -- --exact`; `cargo clippy -p ezc_cli --all-targets -- -D warnings`; and `git diff --check`.

Next is F20: perform the Phase F stability audit and freeze the completed effect contracts. Do not add new semantics or runtime dependency discovery.


## 265. Effect stability audit

Completed Phase F20 and Phase F — Effects. The audit confirms one immutable capability authority (`EFFECT_CAPABILITY_REGISTRY` v1), canonical ASM effect ownership/body/reference/type products, shared reactive graph and scheduler products, validated canonical/optimized IR, compiler-derived runtime registry and artifact generation, deterministic resumability metadata, and a template event bridge keyed only by compiler-emitted `ActionBatchId` values. No runtime code re-infers dependencies, capabilities, trigger eligibility, or action-batch identity.

Frozen schemas: template/resume manifests v2, runtime effect artifact v1, check JSON v2, and ASM inspection v4. The shared `ComponentDiagnostic` envelope remains the sole diagnostic representation, including optional typed effect/statement identities and secondary labels.

Unsupported effect semantics are explicit: effects are synchronous terminal consumers; they cannot mutate reactive state, invoke component actions/effects/methods as reactive work, return cleanup/value callbacks, use async behavior, serve as template event handlers, or use runtime dependency discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core --lib` (171 tests); `cargo test -p ezc_cli --test explain` (123 tests); `cargo test -p ezc_cli --test runtime_browser initial_effects_execute_once_from_compiler_generated_runtime_programs -- --exact`; `cargo test -p ezc_cli --test runtime_browser completed_action_batches_execute_compiler_planned_effects_once -- --exact`; `cargo clippy --workspace --all-targets -- -D warnings`; and `git diff --check`.

Phase F is complete. Await the next roadmap phase.


## 266. Phase G canonical Context entity contract blocked

Stopped before G1. The supplied Phase G roadmap requires every authored Context declaration to lower into a first-class Context entity with a stable ContextId, component ownership, execution boundary, and source provenance. It does not specify the authored declaration syntax, its owning scope, or the source construct/span that establishes provenance; the repository has no existing Context, Provider, or Consumer language construct.

Decision required: provide the exact Context declaration form, including its name/arguments, owning scope, and canonical provenance anchor. Selecting `@context()`, a field helper, a class-level form, or a global declaration would invent language semantics and would prematurely fix later provider/consumer identity, ownership, typing, and diagnostic contracts.

No compiler, ASM, semantic-graph, runtime, or fixture behavior was added. This preserves the invariant that all Context relationships derive from explicit compiler products rather than runtime discovery. Phase G must remain paused at G1 until the declaration contract is supplied.


## 267. Canonical Context entities

Completed Phase G1. Zero-argument `@context()` non-static component fields with explicit declared types now lower to first-class compiler-owned Context entities. Each Context has a stable component-qualified `ContextId`, direct component ownership, a separate authored-field link, canonical declared-type identity, client execution boundary, and full decorated-field provenance. The semantic graph advances to schema v2 and projects Context metadata plus the existing compiler-owned ownership edge.

Literal string, number, boolean, and null defaults lower into the existing canonical expression graph under the Context entity. They remain declaration metadata only: no default becomes State, a Provider, runtime storage, or a reactive dependency. Invalid decorator arguments, static fields, missing types, methods, and nonliteral defaults produce no valid Context entity; G1 adds no public Context diagnostic catalog.

Decision: Context is a distinct semantic channel contract, not state, runtime dependency injection, or an implicit provider. The authored field remains separate from the canonical Context entity, which is the stable subject for future Provider, Consumer, typing, ownership, runtime-slot, resumability, inspection, and diagnostic slices.

Current limitations: G1 adds no Provider or Consumer entities, resolution/visibility analysis, Context lookup, dependency graph, scheduling, IR, runtime registry/artifact, execution, resumability behavior, Context diagnostics, or runtime provider discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser` (28 tests); `cargo test -p ezc_core context` (5 focused tests); `cargo test -p ezc_core semantic_graph` (3 focused tests); `cargo test -p ezc_core asm_validation` (1 focused test); `cargo test -p ezc_core --lib` (176 tests; shared ASM infrastructure); `cargo test -p ezc_cli --test explain` (123 tests); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`; and `git diff --check`.

Next is G2: define and lower canonical Context Provider declarations. Do not resolve consumers, infer ownership, execute providers, or introduce runtime lookup.


## 268. Context Provider declaration contract blocked

Stopped before G2. The Phase G roadmap requires first-class Provider entities with a ProviderId, ContextId, owning component, provided-value expression, execution boundary, and provenance, but it defines no authored Provider declaration syntax. In particular, it does not say how source names a Context target, where a Provider is declared, what expression vocabulary supplies its value, or which complete source span is the canonical Provider provenance.

Decision required: provide the exact Provider source form, Context-reference form, value-expression contract, owning scope, and provenance anchor. Choosing a decorator, JSX wrapper, component field, method, implicit Context default, or name-based lookup would invent language semantics and prematurely fix Context-to-Provider ownership and later Consumer-resolution behavior.

No Provider entity, Context reference, provider value lowering, ownership inference, Consumer resolution, execution, runtime artifact, or runtime lookup was added. G1 remains complete and committed; Phase G must pause at G2 until the Provider declaration contract is supplied.


## 269. Canonical Context Provider entities

Completed Phase G2. `@provide(ComponentSymbol.contextField)` non-static component fields with explicit declared types and required supported initializers now lower to first-class compiler-owned Provider entities. Each Provider has a stable component-qualified `ProviderId`, direct component ownership, a separate authored-field link, one canonical target `ContextId`, declared-type identity, a canonical value-expression root, client execution boundary, and full decorated-field provenance.

Decision: Provider Context references use a compile-time-only static semantic designator, resolved through local component facts or the existing import binding table—not strings, runtime tokens, reflection, decorator execution, or component-tree lookup. Provider ownership remains with the declaring component while Context ownership remains with the Context-declaring component. Provider value operands reuse the existing canonical expression graph and do not create State or Computed entities. Semantic graph schema v3 now exposes Provider metadata, Component ownership, and `provides-context` edges.

Current limitations: G2 adds no Consumers, Consumer-to-Provider resolution, visibility or nearest-provider analysis, provider typing compatibility, lifetime analysis, dependency graph, scheduling, IR, runtime slots/artifacts, execution, resumability behavior, public Context diagnostics, or runtime provider discovery. A same-component duplicate target retains one deterministic duplicate-declaration fact and no ambiguous Provider pair.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser` (29 tests); `cargo test -p ezc_core provider` (5 focused tests); `cargo test -p ezc_core semantic_graph` (4 focused tests); `cargo test -p ezc_core --lib` (181 tests; shared ASM infrastructure); `cargo test -p ezc_cli --test explain` (123 tests); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`; and `git diff --check`.

Next is G3: define and lower canonical Context Consumer declarations. Do not resolve Consumers to Providers or introduce runtime lookup.


## 270. Context Consumer declaration contract blocked

Stopped before G3. The Phase G roadmap requires Consumer entities with a ConsumerId, ContextId reference, owning component, requested type, and provenance, but it defines no authored Consumer declaration form. It does not say how source names the target Context, whether a Consumer is a decorated field, parameter, template binding, or another construct, how its requested type is expressed, or which full source span owns the declaration.

Decision required: provide the exact Consumer source syntax, Context-reference form, requested-type contract, owning scope, and provenance anchor. Choosing a decorator, field helper, constructor parameter, template read, method call, or implicit name would invent language semantics and prematurely fix later Provider-selection and type behavior.

No Consumer entity, Context read, Provider selection, visibility analysis, runtime binding, or runtime lookup was added. G2 remains complete and committed; Phase G must pause at G3 until the Consumer declaration contract is supplied.


## 271. Canonical Context Consumer entities

Completed Phase G3. `@consume(ComponentSymbol.contextField)` non-static
component declaration-only definite-assignment fields with explicit requested
types now lower to first-class compiler-owned Consumer entities. Each Consumer
has a stable component-qualified `ConsumerId`, direct component ownership, a
separate authored-field link, canonical `Resolved(ContextId)` or `Unresolved`
Context identity state, requested-type identity, client execution boundary, and
full decorated-field provenance.

Decision: Consumer Context references use the same compile-time-only static
semantic designator as Providers. Consumer fields are read-only semantic
bindings rather than State, constructor injection, runtime tokens, or service
locator requests. G3 records only Consumer-to-Context requests; it does not
select a Provider or apply a Context default. Semantic graph schema v4 exposes
Consumer metadata, component ownership, and `consumes-context` edges only for
resolved Context targets.

Current limitations: G3 adds no Consumer-to-Provider resolution, visibility or
nearest-provider analysis, default fallback selection, Provider/Consumer type
compatibility, Context lifetime analysis, reactive graph integration,
scheduling, IR, runtime slots/artifacts, execution, resumability behavior,
public Context diagnostics, or runtime provider discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_parser` (29 tests);
`cargo test -p ezc_core consumer` (4 focused tests); `cargo test -p ezc_core
semantic_graph` (5 focused tests); `cargo test -p ezc_core --lib` (186 tests;
shared ASM infrastructure); `cargo test -p ezc_cli --test explain` (123
tests); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D
warnings`; and `git diff --check`.

Next is G4: resolve every canonical Consumer to one compiler-visible Provider
or an explicit unresolved/default-fallback state. Do not introduce runtime
provider lookup or component-tree reconstruction.


## 272. Context resolution contract blocked

Stopped before G4. The Phase G roadmap requires every Consumer to resolve to
exactly one Provider or retain an unresolved semantic fact, but it does not
define the canonical component-composition/ancestry product that establishes
Provider visibility, the deterministic selection rule when multiple visible
Providers target one Context, whether same-component Providers participate, or
whether and how a Context default becomes an explicit fallback result.

Decision required: provide the compiler-owned Provider visibility and
selection contract, including the canonical ancestry/composition input and
default-fallback rule. Choosing global providers, source order, import order,
lexical nesting, or runtime parent traversal would invent language semantics
and violate the no-runtime-lookup invariant.

No Consumer-to-Provider relation, nearest-provider result, Context-default
fallback, ownership-graph reconstruction, runtime binding, or runtime lookup
was added. G3 remains complete and committed; Phase G must pause at G4 until
the resolution contract is supplied.


## 273. Compiler-owned Context resolution

Completed Phase G4. Every canonical Consumer now receives one immutable
`ContextResolution` result. Resolution uses only the compiler-owned component
scope graph, whose Phase G baseline is reflexive self-scope unless a canonical
composition edge is available. Same-component Providers resolve at distance
zero; explicit visible Providers use deterministic nearest-ancestor selection.
Same-scope ambiguity remains explicit.

When no visible explicit Provider exists, a canonical G1 Context default
produces a distinct `ContextDefault` fallback result. Context defaults do not
become hidden Provider entities. Consumers with no visible Provider or default
remain `Unresolved`, and invalid Context designators remain
`InvalidContextReference`.

Decision: imports, source order, lexical nesting, shared modules, and runtime
parent traversal never establish Provider visibility. Phase H may populate the
same canonical component scope graph with composition ancestry without changing
the G4 selection algorithm. Semantic graph schema v5 emits
`resolves-to-provider` only for explicit selected Providers.

Current limitations: Phase G currently guarantees only same-component
resolution unless canonical component ancestry already exists. G4 performs no
Provider/Consumer type compatibility, lifetime analysis, reactive graph
integration, scheduling, IR, runtime slot allocation/artifacts, execution,
resumability behavior, public diagnostics, or runtime Provider discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core
context_resolution` (6 focused tests); `cargo test -p ezc_core component_scope`
(3 focused tests); `cargo test -p ezc_core semantic_graph` (6 focused tests);
`cargo test -p ezc_core --lib` (195 tests; shared ASM infrastructure); `cargo
test -p ezc_cli --test explain` (123 tests); `cargo clippy -p ezc_core -p
ezc_cli --all-targets -- -D warnings`; and `git diff --check`.

Next is G5: validate canonical Context, Provider, Consumer, and default types,
serialization, and execution-boundary compatibility without changing Provider
selection or introducing runtime lookup.


## 274. Context typing contract blocked

Stopped before G5. The Phase G roadmap names Provider values, Consumer
expectations, and declared Context types, but does not define Provider
value-type inference/evaluation scope; directed compatibility rules among the
Context, Provider declaration/value, Consumer, and Context default; serialization
or execution-boundary compatibility; canonical result identity/status; or how
unresolved, ambiguous, invalid, and default resolution states participate.

Decision required: provide the compiler-owned Context type compatibility
contract, including assignability direction, structural/nominal and
unknown/alias handling, whether Provider declared type, value type, or both are
authoritative, and the immutable compatibility facts G5 must retain. Selecting
these rules would invent language semantics and prematurely determine G18
diagnostics and later runtime eligibility.

No Provider/Consumer compatibility, inferred Provider value type, default
compatibility, boundary/serialization result, Context type diagnostic, or
runtime type behavior was added. G4 remains complete and committed; Phase G
must pause at G5 until the typing contract is supplied.


## 275. Compiler-owned Context typing and compatibility

Completed Phase G5. The ASM now retains immutable Context, Provider, Consumer,
and resolved-binding type records. Provider value inference remains separate
from its declared type. Compatibility follows the supplied directed chain:
Provider value -> Provider declaration -> Context declaration -> Consumer
request. Context defaults are distinct fallback sources and flow only to the
declared Context type.

Decision: the Context declared type is the canonical channel contract. G5
projects the existing semantic type, serialization, and execution-boundary
facts into the Context records; `Unknown` is conservative, and incompatible
selected Providers remain selected. G4 visibility and resolution are never
recomputed or filtered by G5.

Current limitations: G5 adds no diagnostics, ownership/lifetime or dependency
graph, scheduling, IR, runtime slots/artifacts, execution, resumability, or
runtime Provider discovery. Semantic graph schema remains v5.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core context_typing`
(4 focused tests); `cargo test -p ezc_core --lib` (199 tests; shared
semantic-type infrastructure); `cargo test -p ezc_cli --test explain` (123
tests); `cargo clippy -p ezc_core -p ezc_cli --all-targets -- -D warnings`; and
`git diff --check`.

Next is G6: construct the compiler-owned Context ownership graph from canonical
scope entities without runtime traversal or ownership inference.


## 276. Context ownership graph contract blocked

Stopped before G6. The roadmap requires a canonical ownership graph and names
Provider, Consumer, Context, and ancestry ownership, but does not specify the
graph's node and edge domains or direction; how it relates to G4's existing
component scope graph; treatment of defaults, unresolved and ambiguous
bindings; required graph invariants/queries/export schema; or whether it is a
projection of existing ownership or a new authoritative composition product.

Decision required: provide the compiler-owned G6 ownership-graph contract,
including its exact topology and relationship to canonical component ancestry.
Selecting an ownership hierarchy or equating visibility scope with ownership
would invent the semantics that G7 dependency projection, G8 lifetime facts,
and G9 ordering must consume.

No ownership graph, inferred ancestry, Provider visibility change, dependency
graph, runtime traversal, runtime ownership lookup, or component-tree
reconstruction was added. G5 remains complete and committed; Phase G must
pause at G6 until the ownership contract is supplied.


## 277. Compiler-owned Context ownership graph

Completed Phase G6. The ASM now retains one immutable `ContextOwnershipGraph`
projected from canonical Context, Provider, Consumer, and Context-default
ownership facts. Components own their declared Contexts, Providers, and
Consumers; Contexts own their canonical default-expression targets. Graph
direction is always owner-to-owned, with deterministic typed node and edge
domains plus inverse ownership indexes.

Decision: Context ownership is distinct from component ancestry, Provider
visibility, Consumer resolution, type compatibility, and data dependency.
`ComponentScopeGraph` remains the sole authority for component ancestry and G4
resolution remains the sole authority for Consumer binding selection. G6 does
not copy, infer, or modify scope edges.

Current limitations: G6 adds no Context dependency graph, lifetime validity,
evaluation order, scheduling, IR, runtime slots/artifacts, execution,
resumability behavior, public Context diagnostics, or runtime
ownership/provider discovery. The general semantic graph remains schema v5.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core context_ownership`
(3 focused tests); `cargo test -p ezc_core --lib` (201 tests; shared ASM
ownership/validation infrastructure); `cargo clippy -p ezc_core --all-targets
-- -D warnings`; and `git diff --check`.

Next is G7: project canonical Context data-dependency relations from Provider
value/default expressions and resolved Consumer bindings. Do not reinterpret
ownership as dependency or introduce scheduling/runtime behavior.


## 278. Context dependency graph contract blocked

Stopped before G7. The roadmap says to project Provider and Consumer relations
into a compiler graph, but it does not specify the graph's typed node and edge
domains/directions; whether Provider-value and Context-default expression
references are direct-only or transitive; how Provider/default/unresolved,
ambiguous, and invalid resolution states contribute; treatment of unknown or
incompatible G5 bindings; or the required ordering, invariants, queries,
validation, and export schema.

Decision required: provide the compiler-owned G7 Context dependency-graph
contract. Treating ownership, requests, provisions, expression nodes, or G4
candidate evidence as dependency topology would invent the facts G8 lifetime
analysis and G9 evaluation planning must consume.

No Context dependency graph, expression dependency closure, scheduling,
Provider reselection, runtime graph reconstruction, or runtime behavior was
added. G6 remains complete and committed; Phase G must pause at G7 until the
dependency-graph contract is supplied.


## 279. Compiler-owned Context dependency graph

Completed Phase G7. The ASM now retains one immutable `ContextDependencyGraph`
with typed State, Computed, Context, ContextDefault, Provider, and Consumer
nodes. Direct edges use dependent-to-dependency orientation. Provider value
expressions project canonical State/Computed reads, Providers and Context
defaults supply their Context contracts, and Consumers depend only on the exact
Provider or Context default selected by G4.

Decision: G7 stores direct value-flow topology only. Ownership remains in G6,
component ancestry remains in `ComponentScopeGraph`, and Consumer selection
remains in G4. Unresolved, ambiguous, and invalid Consumers receive no selected
binding dependency edge. G5 compatibility annotates selected topology but never
changes it.

Current limitations: G7 computes no transitive closure, lifetime validity,
cycle diagnostics, evaluation order, scheduler batches, IR, runtime slots or
artifacts, execution, resumability behavior, public diagnostics, or runtime
dependency discovery. The general semantic graph remains schema v5.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core context_dependency`
(3 focused tests); `cargo test -p ezc_core --lib` (204 tests; shared ASM
dependency/validation infrastructure); `cargo clippy -p ezc_core --all-targets
-- -D warnings`; and `git diff --check`.

Next is G8: derive Context lifetime compatibility from canonical ownership,
component scope, resolution, and direct dependency facts. Do not schedule or
execute Context values.


## 280. Context lifetime-analysis contract blocked

Stopped before G8. The roadmap says to determine Context lifetime and names
component, Provider, and Consumer examples, but does not define lifetime
domains/identities; compatibility rules among Context, Provider, Consumer,
default, State, and Computed lifetimes; how ownership, scope ancestry, G4
selection, G5 status, and G7 dependencies contribute; treatment of
unresolved/ambiguous/invalid bindings; or canonical records, validation,
queries, and export schema.

Decision required: provide the compiler-owned G8 lifetime-analysis contract.
Choosing lexical, component-instance, scope, provider-selection, runtime-slot,
or dependency-derived lifetimes would invent the semantics that G9 availability
planning and later runtime contracts must consume.

No lifetime facts, compatibility diagnostics, selection changes, ordering,
scheduling, IR, runtime lifetime tracking, or runtime discovery was added. G7
remains complete and committed; Phase G must pause at G8 until the lifetime
contract is supplied.


## 281. Compiler-owned Context lifetime analysis

Completed Phase G8. The ASM now retains immutable component-scope lifetime
records for Contexts, Providers, Consumers, Context defaults, direct
State/Computed dependencies, and selected bindings. Lifetime identity derives
from canonical G6 ownership, while outlives relationships derive only from the
existing G4 `ComponentScopeGraph`. Provider and Context-default direct
State/Computed dependencies are compatible only when their owners outlive the
value source's owning scope. Consumer bindings are compatible only when the
selected Provider or Context-default owner outlives the Consumer owner and the
selected source's own dependency lifetime is valid.

Decision: lifetime compatibility is independent of G4 selection and G5 typing.
An incompatible selected source remains selected with an incompatible lifetime
record. Unresolved, ambiguous, and invalid Context references retain explicit
lifetime statuses. No source order, imports, lexical nesting, or runtime
component instances establish lifetime.

Current limitations: G8 adds no evaluation order, scheduling, cycle diagnostics,
IR, runtime slots/artifacts, execution, resumability behavior, public Context
diagnostics, or runtime lifetime/provider discovery. The general semantic graph
remains schema v5.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core context_lifetime`
(3 focused tests); `cargo test -p ezc_core --lib` (207 tests; shared ASM
lifetime/validation infrastructure); `cargo clippy -p ezc_core --all-targets
-- -D warnings`; and `git diff --check`.

Next is G9: produce deterministic Context availability and evaluation plans from
G4 resolution, G5 eligibility, G7 dependencies, and G8 lifetime compatibility.
Do not lower IR or execute Context values.


## 282. Context evaluation-plan contract blocked

Stopped before G9. The roadmap requires deterministic Provider initialization,
Consumer availability, and ownership ordering, but does not define the
evaluation-plan identity/entry records; ordering rules; relation to ownership
and component scope; treatment of Provider/default sources, resolution states,
G5 incompatible or unknown bindings, and G8 incompatible or unknown lifetimes;
or plan validation, queries, inspection/schema, and blocked-entry behavior.

Decision required: provide the compiler-owned G9 evaluation-plan contract.
Choosing source, component, scope, dependency ordering, eligibility filtering,
or how unavailable entries are represented would invent the semantics G10 IR
lowering and later runtime artifacts must consume.

No evaluation/availability plan, ordering, scheduling, Provider reselection,
IR, runtime record, or execution behavior was added. G8 remains complete and
committed; Phase G must pause at G9 until the evaluation-plan contract is
supplied.


## 283. Compiler-owned Context evaluation planning

Completed Phase G9. The ASM now retains one immutable initial
`ContextEvaluationPlan` with canonical Provider and ContextDefault source
entries, deterministic scheduler-produced evaluation batches, and one Consumer
availability record per canonical Consumer. Only sources selected by at least
one G5/G8-eligible Consumer and themselves eligible for typing,
serialization, boundary, lifetime, dependency availability, and later lowering
enter executable batches.

Decision: G9 preserves G4 selection exactly and never chooses another Provider
or fallback because a selected source is blocked. Shared sources evaluate once;
unused, incompatible, unknown, and dependency-blocked sources remain explicit
plan entries. Existing Phase E computed plans are referenced as prerequisites
rather than rebuilt, and the Phase D scheduler remains the sole ordering
authority.

Current limitations: G9 plans initial Context availability only. It adds no
Context IR, optimization, runtime slots or registry, runtime artifact,
execution, update propagation, resumability behavior, inspection schema,
public diagnostics, or runtime Provider discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core
context_evaluation` (5 focused tests); `cargo test -p ezc_core --lib` (212
tests; shared ASM evaluation-plan/validation infrastructure); `cargo clippy -p
ezc_core --all-targets -- -D warnings`; and `git diff --check`.

Next is G10: lower only G9-planned Context Provider/default evaluations and
Consumer bindings into canonical IR. Do not optimize or generate runtime
artifacts.


## 284. Context IR lowering contract blocked

Stopped before G10. The roadmap says only to lower Context initialization,
Provider values, and Consumer loads into canonical IR. It does not define the
Context IR function, block, operation, value, or storage/load identities; how
G9 source and batch identities map to lowering; whether Provider/default
expressions reuse or extend Phase E expression lowering; Consumer load operands
and result semantics; treatment of unavailable entries; computed prerequisite
ordering; or validation and inspection/schema requirements.

Decision required: provide the compiler-owned G10 Context IR lowering contract.
Choosing SSA versus Context storage semantics, source-function boundaries, load
targets, initialization effects, value ownership, or unavailable-entry
representation would invent the compiler architecture that G11 optimization
and G12 runtime artifacts must consume.

No Context IR, storage/load operation, lowering, optimizer integration, runtime
artifact, execution behavior, runtime Context lookup, Provider discovery, or
runtime graph reconstruction was added. G9 remains complete and committed;
Phase G must pause at G10 until the lowering contract is supplied.


## 285. Canonical Context IR lowering

Completed Phase G10. Every G9-planned Provider or ContextDefault source now
lowers into at most one compiler-owned IR function with one stable
`ContextValueSlotId`, one explicit result value, and one observable
`InitializeContextSlot` instruction. Provider/default operands reuse the
existing canonical expression-to-IR path, canonical State storage IDs, and
canonical Computed targets.

Every G9-available Consumer now receives one immutable
`IrContextConsumerBinding` that loads the exact Context slot belonging to the
source selected by G4 and retained by G9. Shared sources evaluate once and
multiple Consumers reference the same slot. Unused, blocked, unresolved,
ambiguous, invalid, and otherwise unavailable entries receive no partial IR.
G10 also completes the existing G5 type-product path for canonical
Provider/default `this.<State|Computed>` reads so eligible G9 sources retain
their canonical source type rather than an ad-hoc lowering inference.

Decision: Context slots are compiler identities, not runtime lookup keys.
Provider/default evaluation functions, Context slots, and Consumer loads remain
separate identity domains. G9 remains the sole source of evaluation batches and
computed prerequisite references.

Current limitations: G10 creates canonical IR only. It does not optimize
Context IR, allocate live runtime storage, create a runtime registry/artifact,
execute sources, propagate updates, restore resumability, extend inspection,
emit public diagnostics, or introduce runtime Context/Provider discovery.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core
intermediate_representation` (19 tests, including 3 focused G10 cases);
`cargo test -p ezc_core context_` (36 focused Context tests); `cargo test -p
ezc_core semantic_type` (8 focused type tests); `cargo test -p ezc_core --lib`
(215 tests; shared IR/type infrastructure); `cargo clippy -p ezc_core
--all-targets -- -D warnings`; and `git diff --check`.

Next is G11: run the existing immutable optimization pipeline over Context
source functions while preserving Context-slot initialization as an observable
root.


## 286. Context IR optimization contract blocked

Stopped before G11. The roadmap says only to run the immutable optimizer and
preserve observable initialization order. It does not define the optimized
Context IR product or its relationship to G10 source functions, results, slot
initializations, and Consumer load records; exact optimization scope and pass
sequence; whether optimization is Context-source-only or may affect other
module functions; required preservation/mapping invariants; or
validation/inspection/runtime-consumer inputs.

Decision required: provide the compiler-owned G11 Context IR optimization
contract. Selecting the optimized-product identity, applying the existing
pipeline to a whole module versus only Context functions, retaining G10 result
references, or deciding slot/load rewrite and merge rules would invent the
architecture that G12 runtime registry and later Context artifacts must
consume.

No Context optimizer product, pass execution, slot/load rewrite, constant
folding, source merge, runtime artifact, execution behavior, runtime Context
lookup, Provider discovery, or runtime graph reconstruction was added. G10
remains complete and committed; Phase G must pause at G11 until the optimization
contract is supplied.


## 287. Context IR optimization

Completed Phase G11. The compiler now runs the existing immutable optimization
pipeline over a projection containing only G10-generated Context source
functions, then merges optimized functions back into a clone of canonical IR by
semantic function identity. `OptimizedContextIrReport` retains the exact G10
`ContextIrReport`, ordered source-evaluation identities, optimized canonical
IR, and the existing per-pass metrics.

Decision: `InitializeContextSlot` remains an observable root. Optimization may
simplify the source value producer, but cannot remove, duplicate, merge,
replace, alias, or reorder Context slots. Every retained source preserves its
G10 function, entry block, result, slot, G9 evaluation batch, computed
prerequisites, provenance, and Consumer bindings. Authored methods, Computed
functions, effects, and all non-Context module products remain unchanged.

Current limitations: G11 creates no Context runtime registry or artifact,
runtime evaluator, cold-boot/update execution, action-batch plan, resumability
behavior, inspection schema change, public diagnostic, Provider fallback,
runtime lookup, ownership inference, or graph reconstruction.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core
intermediate_representation` (20 tests, including focused Context optimization);
`cargo test -p ezc_core --lib`; `cargo clippy -p ezc_core --all-targets -- -D
warnings`; and `git diff --check`.

Next is G12: lower the immutable Context runtime registry and schema-versioned
artifact from G9/G10/G11 products only. Do not execute Context values or
introduce runtime Provider/Context lookup.


## 288. Context runtime registry

Completed Phase G12. `RuntimeContextRegistry` now provides schema contract
version 1 metadata projected only from G9-planned sources, G11 optimized G10
source evaluations, and G9-available G10 Consumer bindings. Source records
retain canonical source, Context, owner, generated function, exact
`ContextValueSlotId`, source type, Provider/default kind, direct State/Computed
requirements, computed prerequisites, G9 batch, boundary, serialization, and
provenance. Consumer records retain the exact selected source slot and
`ContextConsumerLoadId`; initial batches retain G9 scheduler order.

Decision: Registry membership is executable-product membership only. Planned
source identities are sorted canonically, Consumers are sorted by `ConsumerId`,
and source batches remain G9 order. Validation recomputes the expected immutable
projection and rejects a changed schema, source join, Consumer binding, or batch
ordering.

Current limitations: G12 is metadata only. It creates no `context.runtime.json`,
live Context slot storage, evaluator, cold-boot/update execution, action-batch
update plan, resumability behavior, inspection schema, public diagnostics,
Provider lookup, Context-name matching, component-tree traversal, ownership or
dependency reconstruction, or binding inference.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core runtime_context`
(1 focused registry test); `cargo test -p ezc_core --lib`; `cargo clippy -p
ezc_core --all-targets -- -D warnings`; and `git diff --check`.

Next is G13: serialize the separate schema-v1 `context.runtime.json` artifact
from this registry and optimized G11 instruction programs. Do not execute the
artifact.


## 289. Context runtime artifact

Completed Phase G13. The compiler now emits a separate schema-v1
`context.runtime.json` artifact from G12 registry records and the optimized G11
Context source programs. Source entries serialize canonical source, Context,
slot, and generated-function identities; source kind; State/Computed and batch
prerequisites; G9 batch identity; type/boundary contracts; and instructions in
optimized IR order. Consumer entries retain only exact selected source slots
and compiler-generated load identities. `ezc_cli build` writes the artifact and
embeds the same JSON in generated pages under `ez-context-runtime`.

Decision: G13 reuses the existing computed artifact operand representation and
adds explicit `initialize_context_slot` and `load_context_slot` instruction
forms. Source records are canonical source order, Consumers are `ConsumerId`
order, and initial batches retain G9 order. There are no serialized Provider
search, Context resolve, ancestor walk, or name lookup operations.

Current limitations: G13 serializes but does not execute Context programs. It
adds no live slot table, cold-boot/update ordering, action-batch update plan,
resumability behavior, inspection schema, public diagnostics, Provider fallback,
runtime name matching, component traversal, ownership/dependency reconstruction,
or binding inference.

Verified: `cargo fmt --all --check`; `cargo test -p ezc_core
runtime_context_artifact` (1 focused artifact test); `cargo test -p ezc_cli
--test explain context_runtime_artifact` (1 focused build/embed test); `cargo
test -p ezc_core --lib`; strict core and CLI clippy; and `git diff --check`.

Next is G14: consume only this compiler-generated artifact to initialize exact
Context slots before render/effects. Do not perform Context/Provider lookup.


## 290. Initial Context runtime

Completed Phase G14. The generated runtime now reads schema-v1 Context metadata,
executes compiler-emitted source programs after initial computed evaluation,
initializes only compiler-generated Context slots, and records direct
Consumer-to-slot bindings before initial effects. Artifact failures abort boot;
source failures leave the slot unavailable without Provider/default reselection.

Decision: Context initialization is sequential in G9 batch and artifact program
order. Runtime state exposes slots, bindings, source runs, and failures only as
debug evidence. No Context name, Provider map, ancestry traversal, dependency
rebuild, or binding reconstruction is present.

Verified: `cargo test -p ezc_core runtime_codegen`; `cargo fmt --all --check`;
`cargo test -p ezc_core --lib`; strict clippy; and `git diff --check`.

Next is G15: compiler-plan action-batch Context updates before effects.


## 291. Context update propagation

Completed Phase G15. `ContextUpdatePlan` derives one immutable action-batch
record from canonical action writes, G7 Provider State/Computed topology, G9
source plans/batches, and G10 Consumer bindings. Only planned Provider sources
whose direct or transitive State dependencies are written become invalidated;
Context defaults are never action-invalidated. The artifact advances to schema
v2 and emits these plans explicitly.

Decision: After all action writes and the existing computed update flush, the
runtime reevaluates each compiler-selected source once, overwrites its exact
existing slot, then runs completed-action effects. Consumers remain stable slot
bindings. Update failure preserves the prior slot and records evidence; it never
reselects a Provider/default or reconstructs dependencies.

Current limitations: No Context equality mechanism, runtime reverse dependency
map, fallback/rebinding, resumability, inspection schema, public diagnostics,
or runtime Provider/name/tree lookup was added.

Verified: focused Context-update, artifact, and generated-runtime tests; `cargo
fmt --all --check`; `cargo test -p ezc_core --lib`; strict clippy; and `git diff
--check`.

Next is G16: plan serializable Context resume slots and advance the resume
manifest to v3 without live restoration.


## 292. Context resumability

Completed Phase G16. The compiler now projects G12 registry and G15 action-plan
facts into `ContextResumePlan` records with a distinct `ContextResumeSlotId` for
each runtime Context slot. Every record begins `Uninitialized`, retains its
exact source/Context/runtime-slot/type/source-kind identity, action batches,
Consumer bindings, boundary, and provenance. The shared resume manifest is now
schema v3 and includes `context_slots` records.

Decision: Resume-slot identity is intentionally separate from the live
`ContextValueSlotId`; future snapshots map resume slot to runtime slot without
reselecting a Provider. Legacy manifests remain readable but are rejected by
v3 validation because required Context metadata is absent.

Current limitations: G16 plans and serializes metadata only. It adds no live
snapshot capture or restore, Context reevaluation, Provider lookup, component
ancestry/candidate/expression-stack serialization, inspection schema, or public
diagnostics.

Verified: `cargo test -p ezc_core context_resume`; `cargo test -p ezc_core
resume_`; `cargo fmt --all --check`; `cargo test -p ezc_core --lib`; strict
clippy; and `git diff --check`.

Next is G17: add one schema-v5 core Context inspection projection for full ASM,
selected ASM, and explain without CLI reanalysis.


## 293. Context inspection

Completed Phase G17. ASM inspection schema is now v5. A single core
`ContextInspectionRegistry` projects compiler-owned Context, Provider, and
Consumer source-plan, slot, IR-function/load, runtime-registration,
action-update, and resume facts. Full ASM, selected ASM, and explain all consume
this projection rather than re-running Context analysis in the CLI.

Decision: unavailable identities remain absent/null and blocked/unresolved
states retain their existing compiler status rather than guessed replacements.
The schema change is confined to ASM inspection v5; no runtime artifact or
semantic graph schema changes were needed.

Verified: `cargo test -p ezc_core context_inspection`; `cargo test -p ezc_cli
--test explain asm`; `cargo fmt --all --check`; `cargo test -p ezc_core --lib`;
strict core/CLI clippy; and `git diff --check`.

Next is G18: project the existing Context facts into the fixed public diagnostic
catalog without re-resolution, retyping, source walks, or runtime discovery.


## 294. Context diagnostics contract blocked

Stopped before G18. The frozen G18 catalog requires `EZC1052` Invalid Context
declaration, `EZC1053` Invalid Provider declaration, and `EZC1054` Invalid
Consumer declaration, but current G1-G3 lowering deliberately discards invalid
declaration forms without retaining a canonical invalid-declaration record,
identity, provenance, or diagnostic. The ASM retains valid Context, Provider,
and Consumer entities only.

Decision required: define a retained immutable invalid-declaration product for
G1-G3 (or explicitly authorize revision of their discard contract). Emitting
these codes by rereading source, rerunning decorator/parser interpretation,
inventing subject IDs, or reconstructing facts in the CLI would violate G18's
existing-facts-only authority rule and the compiler-only semantic invariant.

No partial diagnostic catalog, `ComponentDiagnostic` shape change, check JSON
schema v3 change, ASM inspection v6 change, runtime diagnostic logic, or G19/G20
work has been added. G17 remains committed; Phase G must pause until this
authority is supplied.

## 295. Context declaration retention and diagnostics

Completed Phase G18 under the supplied invalid-declaration retention contract.
Every authored `@context`, `@provide`, and `@consume` declaration now retains a
stable source-qualified candidate and ordered typed violation facts before valid
entity lowering. G18 maps only those immutable facts to `EZC1052`–`EZC1056`;
it performs no source reparse, missing-entity inference, Provider reselection,
or runtime lookup.

Decision: unresolved Consumer designators and duplicate same-component Provider
groups create no executable Consumer/Provider entity. This prevents invalid
declarations from entering G4-G17, Context slots, runtime artifacts, or plans.
No serialized diagnostic or inspection shape changed, so check JSON remains v2
and ASM inspection remains v5.

Verified: `cargo test -p ezc_core --lib` (221); `cargo fmt --all --check`;
`cargo clippy -p ezc_core --all-targets -- -D warnings`; and `git diff --check`.

Next is G19: add contract fixtures for retained invalid declarations,
deterministic diagnostics, and executable-topology exclusion.

## 296. Corrective Context diagnostic verification gate

Completed the mandatory corrective Phase G18 gate. The compiler now projects
the complete stable `EZC1052`–`EZC1067` Context diagnostic catalog only from
retained declaration candidates and canonical G4 resolution, G5 typing and
contract, G8 lifetime, G9 planning, and G10 lowering products. The projection
uses `ComponentDiagnostic` exclusively and populates every canonically
applicable declaration-candidate, Context, Provider, and Consumer identity.
Primary provenance is the narrowest canonical source-specific location;
secondary evidence is sorted, deduplicated, and never repeats the primary.

The frozen suppression matrix now keeps declaration, resolution, typing,
serialization, boundary, lifetime, dependency, and lowering root causes from
emitting derivative noise. Independent `EZC1059` and `EZC1060` failures may
coexist. `EZC1058` contains only the exact ambiguous Provider declarations in
stable `ProviderId` order and leaves its singular `provider_id` absent.
`EZC1067` requires an explicit retained unsupported planning/lowering fact and
is never inferred from missing IR.

Decision: shared ASM validation rejects unknown or cross-Context typed IDs,
fabricated semantic IDs on invalid declarations, noncanonical primary
provenance, and unsorted, duplicate, noncanonical, or primary-repeating
secondary labels. Selected inspection filters diagnostics by their canonical
typed identity as well as overlapping provenance, without reconstructing a
binding or semantic relation.

Schema decision: the added structured Context identities change check JSON to
schema v3 and ASM inspection to schema v6. The Context runtime artifact remains
schema v2, the effect runtime artifact remains schema v1, and non-Context
diagnostic serialization behavior is unchanged.

Verification: `cargo test -p ezc_core context_diagnostics --lib` (23 focused
catalog, suppression, and validator tests); `cargo test -p ezc_core --lib`
(244); `cargo test -p ezc_cli --test explain` (125, including parity across
check JSON, full ASM, selected ASM, and selected explain); `cargo fmt --all
--check`; strict core and CLI clippy; and `git diff --check`.

G19 remains blocked until this corrective G18 work is committed. After that
commit, G19 may begin strictly as fixture expansion and must not add or repair
Context semantics.

## 297. Context fixture expansion

Completed Phase G19 strictly as fixture expansion. Five new fixture families
cover valid and invalid declarations, same-component/default/unresolved and
manually scoped resolution, exact and incompatible typing contracts,
ownership/dependency/lifetime facts, demanded/unused/blocked evaluation,
Context IR and direct slot loads, runtime/update artifacts, resumability,
multi-file identity collision resistance, and the complete `EZC1052`–`EZC1067`
catalog. Repeated check, ASM, explain, semantic-graph, Context-artifact,
template-manifest, resume-manifest, and IR/registry projections are
deterministic.

Two real-browser probes verify initial Context evaluation and direct Consumer
slot binding before effects; one action-batch Provider update after computed
updates and before effects; unrelated-action exclusion; shared updated slots;
and source-failure retention of the exact compiler binding without Provider
lookup, fallback, ancestry traversal, or reselection.

Decision: G19 changes tests and fixtures only. No Context syntax, G4 selection,
G5 compatibility, G8 lifetime, G9 planning, G10 lowering, runtime behavior, or
serialized shape changed. Semantic graph remains v5, Context runtime artifact
v2, template manifest v2, resume manifest v3, ASM inspection v6, and check JSON
v3.

G20 audit note: deliberately invalid/diagnostic fixtures expose existing
ASM-validation interactions around type-evidence secondary provenance and
unresolved expression origins on invalid Provider candidates. G19 does not
repair or redefine those contracts; the stability audit must determine whether
they are duplicate validation or canonical-product defects.

Verified: `cargo test -p ezc_cli --test context_fixtures -- --nocapture` (10);
`cargo test -p ezc_core context_ --lib` (63); `cargo test -p ezc_cli --test
runtime_browser context_ -- --nocapture` (2 real-browser probes); `cargo fmt
--all --check`; `cargo clippy -p ezc_cli --all-targets -- -D warnings`; and
`git diff --check`.

Next is G20: audit and freeze one canonical authority per Context concern,
remove duplicate interpretation/discovery, freeze the emitted schema versions,
and document unsupported semantics. Do not add new Context behavior.

## 298. Context stability audit and Phase G freeze

Completed Phase G20 and froze Phase G. The audit verified one canonical
authority for Context, Provider, and Consumer identity; designator resolution;
G4 Provider selection; G5 typing; G6 ownership; G7 dependencies; G8 lifetime;
G9 planning; G10/G11 IR and optimization; G12 registry; G13/G15 artifacts;
G14/G15 execution; G16 resumability; G17 inspection; G18 diagnostics; and G19
fixtures. Generated runtime access remains direct compiler-generated
`ContextValueSlotId` binding with no Context-name lookup, Provider selection,
component ancestry, reverse dependency construction, or binding reconstruction.

The audit removed the duplicated raw Context-designator resolver from Provider
and Consumer lowering and replaced it with one shared compile-time resolver.
This does not perform G4 selection. It also restricted declaration-expression
type assignments to canonical State, valid Context, and valid Provider owners,
so retained invalid Provider candidates no longer claim a nonexistent semantic
origin. Finally, shared diagnostic validation now recognizes the exact
canonical declared-type provenance used by `EZC1059`–`EZC1064`; malformed type
evidence remains rejected.

Decision: no Context language or runtime semantics changed. G4 selection is
never retried because of typing, serialization, boundary, lifetime, planning,
lowering, or runtime failure. The check JSON v3 literal now has an explicit
freeze constant. Semantic graph remains v5, Context runtime artifact v2,
template manifest v2, resume manifest v3, ASM inspection v6, and check JSON v3;
the internal runtime Context registry contract remains v1.

The new public `docs/context-contract.md` records the canonical authority table,
cold-boot and completed-action ordering, frozen schemas, direct slot-only
runtime contract, and all unsupported Context semantics. Any future Context
behavior requires a new explicit roadmap slice.

Verified: `just check`, including workspace formatting, strict workspace
clippy, and `cargo test --workspace` (1 CLI unit, 12 Context fixture/freeze, 125
CLI inspection/build, 24 real-browser, 244 core, 3 parser unit, and 26 parser
integration tests); focused Context diagnostic surface parity; focused Context
build/artifact coverage; and `git diff --check`.

Phase G1-G20 is complete and frozen. Phase H has not started.

## 299. Canonical Slot entities

Completed Phase H1. The parser now retains decorators on method parameters so
every authored `@slot()` placement can enter one immutable declaration-candidate
path. Valid zero-argument, non-static, declaration-only definite-assignment
component fields of exact built-in type `SlotContent` lower to first-class
`SlotEntity` records. `children` is the default slot; every other valid field
name is a named slot. Each valid identity is component-qualified as
`<component-id>/slot:<field-name>` with canonical Component-to-Slot ownership,
authored-field identity, client boundary, type provenance, and declaration
provenance.

Every authored candidate instead receives a distinct source-qualified
`SlotDeclarationCandidateId`. Ordered facts retain invalid decorator arity,
static declarations, methods/getters/parameters, missing or non-exact types,
initializers, missing definite assignment, duplicates, and conflicts with other
semantic decorators. Invalid candidates never acquire or fabricate a `SlotId`.
The nominal `SlotContent` type is exact-assignable and nonserializable. Generic
in-memory ASM lookup, entity-kind, ownership, provenance, and semantic-type
queries consume the canonical Slot product directly.

Decision: H1 changes no serialized schema. Slot entities are deliberately
filtered from the frozen semantic graph v5 and CLI ASM inspection v6; H18 owns
the one Phase H inspection-schema increment and its shared projection. Context
runtime artifact remains v2, template manifest v2, resume manifest v3, and
check JSON v3. H1 adds no component invocation, outlet, content binding,
instance planning, runtime, or diagnostic projection.

Verified: `cargo test -p ezc_parser` (4 unit and 26 integration);
`cargo test -p ezc_core slot --lib` (11 focused); `cargo test -p ezc_core
--lib` (249); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p
ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt
--all --check`; and `git diff --check`. The Phase H entry gate also passed
`just check` against the frozen G20 baseline before implementation.

Commit: this entry's commit (`feat(core): add canonical slot entities`).

Next is H2: lower statically resolved PascalCase template tags into canonical
component invocation entities without adding slots, instances, runtime
execution, or diagnostics projection.

## 300. Canonical component invocation entities

Completed Phase H2. Canonical template element facts now retain the authored tag
symbol and an exact tag-name provenance span. Only exact `PascalCase`
identifiers lower to statically resolvable component invocations; lowercase
intrinsic elements remain ordinary template elements. Member and namespaced JSX
targets whose final segment is PascalCase remain immutable
`UnsupportedDynamicTarget` facts rather than enabling dynamic components.

Each `ComponentInvocationEntity` records a distinct typed
`ComponentInvocationId`, caller component, optional resolved target, authored
symbol, canonical template entity, typed `TemplatePositionId`, resolution
status, and source provenance. IDs derive from the caller template's canonical
traversal path plus the resolved target when available. Repeated uses of one
definition therefore remain distinct without DOM or runtime indexes. Local
definitions and imported aliases resolve only through canonical component and
binding products. Unresolved symbols, resolved non-components, ambiguous local
definitions, and unsupported dynamic targets retain explicit blocked statuses
and never receive a selected component target.

Decision: H2 adds generic in-memory ASM entity/ownership queries only and
changes no serialized schema. Component invocations are filtered from semantic
graph v5 and CLI ASM inspection v6 so H18 remains the one Phase H inspection
increment. Context runtime artifact remains v2, template manifest v2, resume
manifest v3, and check JSON v3. H2 adds no instances, slot fragments/outlets,
runtime lookup, or diagnostic projection.

Verified: `cargo test -p ezc_parser` (5 unit and 26 integration); `cargo test -p
ezc_core component_invocation --lib` (4 focused); `cargo test -p ezc_core
--lib` (253); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p
ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt
--all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): add canonical component invocations`).

Next is H3: lower caller-owned slot content fragments and callee-owned slot
outlets without binding them or adding runtime behavior.

## 301. Slot content and outlet lowering

Completed Phase H3 from immutable template, invocation, and Slot products. Each
non-empty set of direct non-wrapper invocation children lowers to one
caller-owned `children` fragment. Each exact direct-child `<template
slot="name">` lowers to one named fragment whose synthetic fragment root and
ordered canonical template-entity roots preserve caller lexical/semantic
ownership separately from later callee placement. Compile-time wrappers are not
host DOM content. Ordinary `<template>` elements that do not match the exact
direct-child contract remain ordinary default content.

Canonical `SlotContentFragmentId` values are invocation- and Slot-qualified.
Grouping guarantees at most one fragment per invocation/requested Slot while
retaining deterministic secondary provenance for duplicates. Missing Slot
declarations, unresolved invocation targets, duplicate fragments, nested
wrappers, dynamic Slot names, and malformed wrapper forms remain ordered typed
violations on blocked fragments; no valid `SlotId` is fabricated. Empty
invocations correctly have no incoming fragment.

Callee `<slot />` and `<slot name="name" />` elements now lower to typed
`SlotOutletId` records with exact template identity and provenance. They are
compiler directives rather than host Slot semantics. Missing declarations,
duplicate outlets, dynamic names, invalid attributes, and unsupported fallback
content remain canonical blocked outlet facts. H3 does not bind fragments to
outlets, remove wrappers during runtime rendering, plan instances, or execute
components.

Decision: H3 adds generic in-memory ASM ownership/provenance queries but changes
no serialized schema. Slot fragments/outlets remain excluded from semantic
graph v5 and ASM inspection v6 until the single H18 projection increment.
Context runtime artifact remains v2, template manifest v2, resume manifest v3,
and check JSON v3.

Verified: `cargo test -p ezc_core slot_content --lib` (5 focused); `cargo test
-p ezc_core --lib` (258); `cargo test -p ezc_cli --test explain` (125); `cargo
clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`;
`cargo fmt --all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): lower slot content and outlets`).

Next is H4: plan statically reachable component instances from canonical roots
and invocation paths without execution or unbounded recursive expansion.

## 302. Component instance planning

Completed Phase H4. The compiler now emits typed `ComponentBuildRoot`,
`ComponentInstance`, and `BlockedComponentInstancePlan` products. Routed
components are explicit page roots. When no routes exist, valid definitions
with no incoming resolved invocation become build entries, matching the
repository's current build-all-entry behavior without treating every reachable
library definition as a root. A cycle-only graph with no zero-incoming
definition receives one lexicographically canonical fallback root so planning
still terminates at an explicit cycle boundary.

Every root has one typed `ComponentRootId` and root `ComponentInstanceId`.
Reachable child identities append the complete canonical invocation path, so
siblings and repeated uses of one definition remain distinct across local and
multi-file composition. Each instance records definition, optional invocation,
parent, owner root, depth, provenance, and optional typed structural region.
Invocations enclosed by a conditional or keyed-list canonical template entity
produce one `StructuralTemplate`; H4 never eagerly allocates runtime branch or
list-item instances.

Unresolved symbols, unsupported dynamic targets, ambiguous/non-component or
otherwise invalid targets, and recursive composition boundaries become
separate typed blocked instance records. They never enter the executable
instance map. `InvalidParentPlan` remains a defined blocked reason for future
validated/mutated-plan input; canonical H4 construction never fabricates an
invalid parent. H4 adds no execution.

Decision: generic in-memory ASM identity, ownership, provenance, and definition
instance queries include the H4 products. Semantic graph v5 and CLI ASM
inspection v6 deliberately filter them until H18. No serialized schema changed;
Context runtime artifact remains v2, template manifest v2, resume manifest v3,
and check JSON v3.

Verified: `cargo test -p ezc_core component_instance --lib` (5 focused);
`cargo test -p ezc_core --lib` (263); `cargo test -p ezc_cli --test explain`
(125); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D
warnings`; `cargo fmt --all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): plan component instances`).

Next is H5: build and validate the executable component instance scope graph,
excluding every blocked H4 record from its node domain.

## 303. Component instance scope graph

Completed Phase H5. The compiler now derives one canonical
`ComponentInstanceScopeGraph` exclusively from H4 executable instances. Its
typed nodes retain definition, owner root, depth, structural-region identity,
and planned/template status. Canonically sorted `parent_by_instance`,
`children_by_instance`, and root indexes provide exact ancestry queries for
repeated definitions under distinct parents. Every H4 blocked expansion record
remains outside the graph's node and edge domain.

The deterministic integrity validator covers unknown parent/child endpoints,
missing parents, parent/child reciprocity, multiple parents, invalid roots,
depth mismatch, owner-root mismatch, cycles, root reachability, and noncanonical
root/child ordering. Mutation tests exercise these failures and byte-stable
diagnostic ordering. Canonical construction is validation-clean. Shared ASM
validation reports internal `EZASM1192` when the retained graph diverges from
the H4 plan and `EZASM1193` for exact graph-integrity failures.

Decision: H5 changes neither declaration ownership nor the frozen Phase G
declaration-level `ComponentScopeGraph`; the new instance graph is the sole
future runtime composition ancestry authority. It performs no Context
resolution, slot binding, runtime parent traversal, execution, or H19
user-facing diagnostic projection.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core component_instance_scope --lib` (2 focused);
`cargo test -p ezc_core --lib` (265); `cargo test -p ezc_cli --test explain`
(125); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D
warnings`; `cargo fmt --all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): build component instance scope graph`).

Next is H6: reproject Phase G Context resolution per component instance using
only canonical H5 ancestry and instance-qualified runtime identities.

## 304. Instance-aware Context reprojection

Completed Phase H6. The ASM now retains a canonical `InstanceContextRegistry`
derived exclusively from exact Phase G Context, Provider, and Consumer
declarations plus the H5 executable instance scope graph. Compound
`ProviderInstanceId` and `ConsumerInstanceId` values keep declaration identity
separate from instance identity. `ContextSourceInstanceId` additionally carries
the exact `ContextId` and a typed Provider-instance or root-qualified default
origin. Every selected source produces a source-qualified
`InstanceContextValueSlotId`; declaration-level Phase G slots and resolution
records remain unchanged.

For each executable Consumer instance, resolution inspects the instance itself
and then canonical H5 ancestors, selects only the nearest matching Provider
scope, retains deterministically ordered same-scope candidates, and consults a
root/build-qualified Context default only when no visible Provider instance
exists. Typing, serialization, lifetime, lowering, and runtime eligibility do
not participate in selection. Focused tests prove two instances of one Consumer
definition resolve to different Provider instances and value slots, root
defaults differ across build roots, incompatible Providers remain selected, and
shared ASM validation rejects a mutated registry with internal `EZASM1194`.

Decision: Phase G declaration-level `ContextResolution`,
`ComponentScopeGraph`, Provider/Consumer identities, and runtime products remain
frozen compatibility inputs. H6 adds a parallel immutable instance projection;
all later Phase H runtime composition consumes that projection without runtime
Provider discovery, name lookup, DOM inspection, or declaration binding
leakage. Invalid declarations that never acquired canonical Phase G identity
remain excluded instead of receiving fabricated instance identities.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core instance_context -- --nocapture` (3 focused);
`cargo test -p ezc_core` (268); `cargo test -p ezc_cli --bin ezc_cli` (1);
`cargo test -p ezc_cli --test context_fixtures` (12); `cargo test -p ezc_cli
--test explain` (125); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli
--all-targets -- -D warnings`; `cargo fmt --all --check`; and `git diff
--check`. An unscoped `cargo test -p ezc_cli` attempt reached the real-browser
suite and was sandbox-denied while binding its static test server; the H6
compiler-only gate uses the separately passing CLI unit, Context fixture, and
inspection suites, and the unchanged browser baseline remains covered by the
Phase H entry gate.

Commit: this entry's commit (`feat(core): resolve context per component instance`).

Next is H7: bind caller-owned Slot fragments to exact callee-instance outlets
without transferring lexical or semantic ownership.

## 305. Instance-specific Slot binding resolution

Completed Phase H7. The compiler now joins immutable H3 Slot fragments and
outlets to exact H4 caller/callee instance relationships in one canonical
`SlotBindingRegistry`. Every `SlotBindingId` is callee-instance-qualified, and
each record retains its invocation, declared Slot, compile-time outlet,
caller-owned fragment, caller `content_owner_instance`, deterministic status,
and canonical invocation provenance. Repeated uses of a component definition
therefore receive distinct binding identities even when they reuse the same
declaration-level fragment and outlet facts.

The closed status model represents `Bound`, `Empty`, `MissingOutlet`,
`UnknownSlot`, `DuplicateContent`, `DuplicateOutlet`, `InvalidOwnership`, and
`BlockedInvocation`. Every unbound declared Slot receives an explicit `Empty`
binding. Invalid H3 facts remain retained but never become executable bindings;
blocked H4 invocation boundaries receive typed non-executable binding records.
The collector uses only canonical IDs and does no slot-name lookup at runtime.

Decision: `content_owner_instance` always remains the exact caller instance,
while `outlet` identifies rendered placement in the callee. H7 never rewrites
template semantic ownership or references, so caller expressions keep caller
lexical and semantic scope after placement. Internal ASM validation recomputes
the complete registry and reports `EZASM1195` if retained H7 facts diverge from
H3/H4 authority.

Focused coverage proves default and named binding, empty Slots, missing
outlets, unknown Slots, duplicate content and outlets, blocked invocations,
invalid ownership, nested repeated callees, distinct per-instance binding IDs,
and preserved caller template ownership.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core slot_binding::tests -- --nocapture` (3
focused); `cargo test -p ezc_core` (271); `cargo test -p ezc_cli --bin ezc_cli`
(1); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p ezc_parser
-p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt --all
--check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): bind slots to component instances`).

Next is H8: type and boundary-check invocations, Slot bindings, and
instance-qualified Context bindings without changing H2, H6, or H7 resolution.

## 306. Composition typing and boundary validation

Completed Phase H8. The ASM now retains immutable, deterministically ordered
`ComponentInvocationTypeRecord`, `SlotBindingTypeRecord`, and
`InstanceContextBindingTypeRecord` maps in `CompositionTypeProducts`.
Invocation records enforce the Phase H Client-to-Client boundary and retain
unresolved targets without inventing a target boundary. Slot records require
exact nominal `SlotContent`, validate caller-owned dependency scope, callee
outlet ownership, and H7 cardinality, and keep blocked bindings non-executable.

Instance Context records reproject existing Phase G G5 type, serialization,
and boundary facts plus G8 lifetime categories onto the exact H6-selected
Provider/default source. Provider selection is never retried: an incompatible
Provider remains identified in the H8 record with `Incompatible` overall
status. Defaults use the canonical Context default compatibility, and unknown
facts remain `Unknown` under the existing semantic type contract.

Decision: all Phase H component definitions and instances are explicitly
Client-bound; the language has no authored server/shared component boundary in
this phase. Slot dependency validation follows canonical ownership/reference
edges back to the caller component and never reparses template syntax. Shared
ASM validation reports internal `EZASM1196` when retained H8 products diverge
from H2/H6/H7 and frozen Phase G inputs.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core composition_typing::tests -- --nocapture` (3
focused); `cargo test -p ezc_core` (274); `cargo test -p ezc_cli --bin ezc_cli`
(1); `cargo test -p ezc_cli --test context_fixtures` (12); `cargo test -p
ezc_cli --test explain` (125); `cargo clippy -p ezc_parser -p ezc_core -p
ezc_cli --all-targets -- -D warnings`; `cargo fmt --all --check`; and `git diff
--check`.

Commit: this entry's commit (`feat(core): validate component composition types`).

Next is H9: derive the canonical resolved component-definition graph, detect
deterministic SCC cycles, and link exact blocked invocation boundaries.

## 307. Component composition cycle analysis

Completed Phase H9. `ComponentCompositionAnalysis` now derives a canonical
definition adjacency map from resolved H2 component invocations only, computes
deterministic strongly connected components including self-cycles, and retains
each `ComponentCompositionCycle` as sorted component and invocation identities.
It maps every H4 `CompositionCycleBoundary` instance to the exact sorted cycle
index while preserving the blocked record's invocation, parent, target,
provenance, and finite first-boundary expansion policy.

Decision: only definition-to-invoked-definition edges participate. Slot
ownership and caller content placement add no reverse edges; unresolved,
dynamic, ambiguous, and non-component invocations add no graph edge. H9 neither
expands blocked descendants nor emits public diagnostics. Shared ASM validation
reports internal `EZASM1197` when retained analysis diverges from canonical H2
invocations and the H4 plan.

Focused tests cover self, two-node, and three-node cycles, finite expansion,
exact blocked-boundary linkage, exclusion of unresolved nested Slot content,
acyclic composition, deterministic SCC ordering, and validation mutation.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core component_composition::tests -- --nocapture`
(3 focused); `cargo test -p ezc_core` (277); `cargo test -p ezc_cli --bin
ezc_cli` (1); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p
ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt
--all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): analyze component composition cycles`).

Next is H10: schedule deterministic initial instance creation and Slot
materialization batches using the established compiler scheduler contract.

## 308. Component initialization and Slot materialization plan

Completed Phase H10. `ComponentInitializationPlan` now derives sorted root and
blocked instance sets, Phase-D-scheduler instance batches, per-batch compatible
instance Context sources, and Slot-binding batches with exact caller/callee
instance prerequisites. Parent instances precede children; independent siblings
share deterministic batches; bindings run only after both instance batches and
all selected Context source readiness are available. H8-ineligible and H4
blocked records are excluded from executable binding batches.

Decision: H10 reuses `IrUpdateScheduler` solely for canonical parent-to-child
instance topology. It does not add a component runtime scheduler, execute
templates, create DOM, or plan structural updates. Shared ASM validation adds
internal `EZASM1198` for plan drift.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core component_initialization::tests -- --nocapture`
(3 focused); `cargo test -p ezc_core` (280); `cargo test -p ezc_cli --bin
ezc_cli` (1); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p
ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt
--all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): plan component initialization`).

Next is H11: lower H10 plans into canonical component and Slot IR.

## 309. Component initialization IR

Completed Phase H11. `ComponentIrReport` lowers the immutable H10 instance and
Slot-binding batches into deterministically indexed component instructions:
`CreateComponentInstance`, `InitializeComponentInstance`,
`MaterializeComponentTemplate`, and `BindSlotContent`. The typed
`DestroyComponentInstance` operation is part of the canonical instruction
domain but is deliberately not emitted until the H16 structural-update plan
exists.

Decision: lowering consumes H10 batches, H8 executability, H6
instance-qualified Context value slots, H7 caller/callee Slot bindings, and
H4 templates only. It preserves the component-instance and content-owner
identities directly; no declaration Context slot, tag/slot-name lookup, parser
walk, runtime discovery, or component rerender behavior is introduced. Shared
ASM validation adds internal `EZASM1199` for retained IR drift.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core component_ir::tests -- --nocapture` (2
focused); `cargo test -p ezc_core` (282); `cargo test -p ezc_cli --bin ezc_cli`
(1); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p ezc_parser
-p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt --all --check`;
and `git diff --check`.

Commit: this entry's commit (`feat(core): lower component initialization ir`).

Next is H12: optimize the immutable component IR without merging identities or
reordering observable component, Context, or Slot operations.

## 310. Immutable component IR optimization

Completed Phase H12. `OptimizedComponentIrReport` retains both the immutable
H11 source report and its optimized projection. Every current H11 instruction
is an observable component, instance Context, template, or Slot root, while
H11 has already excluded blocked and statically empty work; the canonical H12
optimization is therefore an explicit identity projection with no eliminated
instruction provenance.

Decision: the optimizer has no authority to merge instance identities, Slot
bindings, or instance Context slots, to transfer caller content ownership, or
to reorder operations. Recording the identity projection rather than omitting
H12 makes the optimization boundary canonical and independently validated.
Shared ASM validation adds internal `EZASM1200` for source/optimized drift.

No serialized schema changed. Semantic graph remains v5, Context runtime
artifact v2, template manifest v2, resume manifest v3, ASM inspection v6, and
check JSON v3.

Verified: `cargo test -p ezc_core component_ir_optimization::tests --
--nocapture` (2 focused); `cargo test -p ezc_core` (284); `cargo test -p
ezc_cli --bin ezc_cli` (1); `cargo test -p ezc_cli --test explain` (125);
`cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D
warnings`; `cargo fmt --all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): optimize component initialization ir`).

Next is H13: project deterministic runtime component metadata from the
canonical plans and optimized IR.

## 311. Runtime component registry

Completed Phase H13. `RuntimeComponentRegistry` (contract version 1) is a
deterministic metadata projection of H10 initialization batches and H12
optimized component IR. It retains definition/template/Slot records, planned
instance topology and compiler-generated instance prefixes, executable Slot
bindings, compatible instance-qualified Context bindings, and batch membership.

Decision: registry membership is limited to planned, non-blocked instances and
fully executable binding/context records. Its only keys are canonical typed
identities; compiler-generated prefixes are instance-qualified, and it exposes
no authored tag, Slot, Context, or Provider name lookup authority. H13 is
metadata only and does not serialize, allocate, render, or discover runtime
relationships.

No public serialized schema changed. The H13 registry has an explicit internal
contract version 1; semantic graph remains v5, Context runtime artifact v2,
template manifest v2, resume manifest v3, ASM inspection v6, and check JSON
v3.

Verified: `cargo test -p ezc_core runtime_component::tests -- --nocapture` (1
focused); `cargo test -p ezc_core` (285); `cargo test -p ezc_cli --bin ezc_cli`
(1); `cargo test -p ezc_cli --test explain` (125); `cargo clippy -p ezc_parser
-p ezc_core -p ezc_cli --all-targets -- -D warnings`; `cargo fmt --all --check`;
and `git diff --check`.

Commit: this entry's commit (`feat(core): build runtime component registry`).

Next is H14: serialize the compiler-generated `component.runtime.json` v1
artifact and cover deterministic goldens.

## 312. Component runtime artifact v1

Completed Phase H14. The compiler emits deterministic `component.runtime.json`
schema v1 and the CLI build writes that same canonical artifact alongside the
existing runtime files. It contains definition and planned-instance records,
parent topology, initialization batches, Slot binding programs,
instance-qualified Context bindings, and explicit disabled destruction metadata
for H16.

Artifact validation rejects schema mismatches, unknown instance/Slot endpoints,
and invalid batch ordering. Canonical IDs remain executable authority; source
names are not runtime lookup keys. Focused tests cover deterministic JSON and
malformed endpoint rejection.

Verified: `cargo test -p ezc_core runtime_component_artifact::tests` (1);
`cargo test -p ezc_cli --bin ezc_cli` (1); strict core/CLI clippy; formatting;
and `git diff --check`.

Commit: this entry's commit (`feat(runtime): emit component runtime artifact v1`).

Next is H15: execute initial compiler-generated component trees in the browser runtime.

## 313. Initial component runtime boot

Completed Phase H15. Generated pages now embed the same H14 component artifact
that CLI build writes. The browser runtime validates its v1 shape before boot,
initializes closed ID-keyed instance, Slot-binding, and instance-Context tables,
and exposes deterministic initialization/tree/binding/failure debug evidence.

Decision: initial boot consumes only compiler-emitted IDs and batch order. It
does not resolve tags, Slot names, Providers, or parents, infer ownership, or
diff DOM. Existing static page materialization remains the DOM producer; this
slice establishes component runtime identity/state handoff for H16 structural
program execution.

Verified: focused runtime-codegen/page tests; CLI unit test; existing real
browser runtime suite; strict core/CLI clippy; formatting; and diff check.

Commit: this entry's commit (`feat(runtime): initialize component runtime tables`).

Next is H16: emit and execute compiler-planned structural component updates.

## 314. Structural component programs

Completed Phase H16. `component.runtime.json` advances to schema v2 and now
contains deterministic structural programs keyed by canonical structural-region
IDs. Each program retains structural template instances plus explicit
parent-before-child creation and child-before-parent destruction order. Runtime
boot validates v2 and installs a closed region-program table keyed only by
region IDs.

Decision: structural programs are emitted from canonical H4 structural template
records and are never inferred from DOM nodes, tags, or list indexes. The
existing structural DOM runtime remains the host-DOM mechanism; component
program metadata keeps component lifecycle ownership and destruction ordering
available without adding a virtual DOM.

Verified: component artifact/runtime-codegen focused tests; strict core clippy;
formatting; and diff check.

Commit: this entry's commit (`feat(runtime): add structural component programs`).

Next is H17: extend resumability metadata with instance, region, and Slot binding records.

## 315. Component resumability planning

Completed Phase H17. The shared resume manifest advances from v3 to v4 and
retains deterministic component-instance, structural-region, and Slot-binding
resume records. Repeated instances stay separate, structural templates are
explicitly inactive, and all references use compiler-generated IDs.

Decision: this is resume planning metadata only. Restoration remains deferred
to Phase J; no DOM reconstruction, tag/Slot matching, parent search, or live
state restoration was introduced.

Verified: resume-manifest focused tests (3); strict core clippy; formatting;
and diff check.

Commit: this entry's commit (`feat(resume): add component resume records v4`).

Next is H18: expose the canonical component products through shared ASM inspection.

## 316. Component inspection schema v7

Completed Phase H18. ASM inspection advances from schema v6 to v7 and each
definition, invocation, and Slot entity can expose its canonical component
relations: Slots, invocations, planned instances, initialization batches, and
structural regions. The projection reads immutable ASM products only.

Verified: CLI inspection unit test; strict CLI clippy; formatting; and diff check.

Commit: this entry's commit (`feat(cli): inspect canonical component products`).

Next is H19: project the reserved component diagnostic catalog.

## 317. Component diagnostics

Completed Phase H19 as one compiler/CLI contract slice. The compiler now
projects the complete reserved `EZC1068`-`EZC1083` catalog exclusively from
immutable H1-H17 products: Slot declaration candidates, normalized component
heritage, invocation resolution, composition cycles, Slot content/outlets and
bindings, composition typing, instance Context resolution, instance planning,
initialization exclusions, and component IR lowering.

The shared diagnostic envelope now carries optional canonical `SlotId`,
`ComponentInvocationId`, `ComponentInstanceId`, `SlotBindingId`,
`ComponentStructuralRegionId`, component `SemanticId`, `ProviderInstanceId`,
and `ConsumerInstanceId` values. Invalid Slot declarations never acquire a
`SlotId`; unresolved or blocked targets never acquire a fabricated target
component identity. Parser lowering retains source-faithful class heritage so
inheritance diagnostics consume a normalized compiler fact instead of source
reconstruction.

Decision: one table-defined projector owns messages, authority, primary and
secondary evidence roles, typed identities, deduplication, suppression, and
ordering for all 16 codes. `EZASM1201` recomputes that vector and rejects
unknown, missing, fabricated, cross-owner, mislabeled, misordered, or otherwise
noncanonical H19 diagnostics. Suppression keys every cascade to the same
component, invocation, Slot binding, component instance, or structural region;
independent findings survive even when an adjacent subject fails.

Check JSON advances from schema v3 to v4 and ASM inspection advances from v7 to
v8. Check, full ASM, selected-entity ASM, and explain all serialize the same
canonical diagnostic projection. Resume manifest v4, component runtime artifact
v2, semantic graph v5, Context runtime artifact v2, and template manifest v2
remain frozen.

The per-code matrix covers every code's exact message, severity, primary
provenance, required identity set, secondary-label ordering/deduplication, and
canonical validation. Coexistence, derivative suppression, nearby-valid input,
deterministic repetition, check/full-ASM/selected-ASM/explain parity, text
parity, and serialized schema versions are covered separately. The authored
language subset naturally emits declaration, invocation, cycle, Slot, and
instance-Context failures. Independent H4/H7/H8 failure states for `EZC1078`,
`EZC1079`, `EZC1080`, `EZC1082`, and `EZC1083` are therefore exercised through
explicit mutations of those retained canonical products; H19 does not invent
authored syntax or runtime fallback behavior to manufacture them.

Verified: `cargo test -p ezc_parser` (6 unit; 26 integration); `cargo test -p
ezc_core component_diagnostics::tests -- --nocapture` (6 focused); `cargo test
-p ezc_core` (292); `cargo test -p ezc_cli --bin ezc_cli` (1); `cargo test -p
ezc_cli --test explain` (126); `cargo test -p ezc_cli --test context_fixtures`
(12); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli --all-targets -- -D
warnings`; `cargo fmt --all --check`; and `git diff --check`.

Commit: this entry's commit (`feat(core): project component diagnostics`).

Next is H20. It has not started.

## 318. Component fixture expansion

Completed Phase H20. Five authored fixture families now cover Phase H from
declaration lowering through runtime-facing products: `0062` covers valid and
every retained invalid Slot declaration form, inheritance, local/imported
resolution, repeated invocations, and reversed multi-file input order; `0063`
covers nested/sibling/repeated composition, default/named/multiple/empty Slot
content, caller ownership, a Consumer inside slotted content, cycles, blocked
plans, unknown/duplicate content, duplicate outlets, and missing outlets;
`0064` covers exact Provider-instance selection, nearest ancestry, root-default
fallback, same-scope ambiguity, distinct runtime slots, and no declaration-level
selection leakage.

`0065` covers deterministic component artifacts and initialization order,
repeated-instance storage isolation, caller-owned Slot programs,
instance-qualified Context bindings, failure/blocked exclusion, structural
create/destroy order, active/inactive schema-v4 resume records, stable resume
IDs, conditional subtree swaps, keyed moves/removals, unaffected host identity,
closed compiler-ID region tables, and the no-virtual-DOM boundary. `0066`
contains one focused source fixture for every `EZC1068`-`EZC1083`; the five
states that valid upstream construction prevents reuse H19's explicit mutation
of the retained canonical product rather than inventing syntax.

Decision: H20 keeps compiler facts in a dedicated CLI integration matrix and
uses real-browser probes only for runtime-executed behavior. Check text/JSON,
ASM text/JSON/graph, selected ASM/explain text/JSON, semantic graph, template
manifest, Context runtime artifact, component runtime artifact, resume manifest,
structural programs, and runtime debug evidence are rebuilt or executed
repeatedly to prove deterministic output.

The fixture matrix exposed two prior contract leaks. H8 composition typing had
reused declaration-only Phase G lifetime compatibility after H6 selected an
exact ancestor Provider or root-qualified default; it now treats that selected
instance source as outliving the Consumer while retaining the frozen Phase G
lifetime record and `EZC1065` diagnostic unchanged. The H19 duplicate-outlet
projector emitted one finding per outlet record; it now emits one `EZC1076` per
component/Slot-name group with deterministic secondary evidence.

No public schema changed. ASM inspection remains v8, check JSON v4, resume
manifest v4, component runtime artifact v2, semantic graph v5, Context runtime
artifact v2, and template manifest v2.

Verified: `cargo test -p ezc_parser -p ezc_core` (292 core; 6 parser unit; 26
parser integration); `cargo test -p ezc_cli --bin ezc_cli --test
component_fixtures --test context_fixtures --test explain` (1 CLI unit; 6 H20
fixture; 12 Context fixture/freeze; 126 CLI inspection/build);
`RUST_TEST_THREADS=1 cargo test -p ezc_cli --test runtime_browser -- --nocapture`
(26 real-browser); `cargo clippy -p ezc_parser -p ezc_core -p ezc_cli
--all-targets -- -D warnings`; `cargo fmt --all --check`; and `git diff --check`.

Commit: this entry's commit (`test(components): expand phase h fixtures`).

Next is H21: audit and freeze every Phase H authority, schema, unsupported
semantic, diagnostic, fixture, compiler/runtime ownership, and duplicate-logic
boundary with the repository's established full gate.

## 319. Component stability audit

Completed Phase H21 and froze Phase H. The audit confirms one immutable
authority for every Component and Slot concern from H1 through H20: Slot
declarations; invocation resolution; content and outlets; instances and
structural regions; executable ancestry; instance Context; Slot bindings;
typing; cycles; initialization; IR and optimization; runtime registry and
artifact; initial and structural execution; resume; inspection; diagnostics;
and fixtures.

The executable freeze proof distinguishes definition, invocation, and instance
identity domains; preserves repeated instances across compiler and runtime
products; verifies caller-owned Slot content and exact instance-qualified
Context sources; locks instance, structural-region, Slot-binding, Provider, and
Consumer identity constructors to their canonical modules; checks closed
runtime table installation before Context and effect execution; and prohibits
authored-name runtime lookup plus parser-backed diagnostic reanalysis.

The duplicate-logic audit found one schema-authority leak: the generated
browser runtime repeated component artifact schema v2 as a JavaScript literal.
Runtime generation now injects `RUNTIME_COMPONENT_ARTIFACT_SCHEMA_VERSION`, so
Rust artifact construction, validation, and browser acceptance share one
version authority. No tag/Slot resolution, runtime ancestry or Provider search,
declaration-level Context reuse, source-order scheduler, structural replanning,
virtual-DOM diff, independent CLI inspection model, or runtime semantic
discovery remains in the Phase H path.

The public `docs/component-contract.md` freezes syntax, identity domains,
caller ownership, instance Context semantics, cold and completed-action
ordering, compiler structural programs, diagnostics `EZC1068`-`EZC1083`, the
complete unsupported-semantics list, and no-runtime-discovery invariants.
`docs/runtime-contract.md` now records the actual template-manifest compatibility
policy and component runtime boundary, and the README links the Phase H
contract.

Actual frozen versions are component runtime artifact v2, resume manifest v4,
ASM inspection v8, check JSON v4, template manifest v2 with conditional legacy
v1 output and acceptance, Context runtime artifact v2, semantic graph v5, and
internal runtime component registry contract v1. No public schema changed in
H21.

Verified: focused Phase H freeze proof; the complete seven-test component
fixture/audit matrix; strict core/CLI clippy; formatting; diff check; and
`just check`, covering strict workspace clippy plus 292 core, 6 parser unit, 26
parser integration, 1 CLI unit, 7 component fixture/audit, 12 Context
fixture/freeze, 126 CLI inspection/build, and 26 real-browser tests.

Commit: this entry's commit (`refactor: freeze phase H component architecture`).

Phase H is complete and frozen. Do not enter Phase I or Phase J without an
authoritative roadmap contract.

## 320. Forms entry audit

Completed Phase I0 from the attached authoritative Phase I compiler-owned
Forms roadmap. The committed H21 freeze remains intact, and the Phase I entry
gate passed the repository's full `just check` baseline before Forms code was
introduced.

I0 reserves the next public compiler diagnostic range, `EZC1084` through
`EZC1095`, in one ordered `FORM_DIAGNOSTIC_RESERVATIONS` table. Its twelve
entries correspond exactly to the roadmap's diagnostic list. The table emits
no diagnostics; I18 remains the sole owner of canonical projection, identity
requirements, provenance, suppression, deduplication, and messages.

No schema changed. The Phase I entry baseline remains component runtime
artifact v2, resume manifest v4, ASM inspection v8, check JSON v4, template
manifest v2 with conditional legacy-v1 output and acceptance, Context runtime
artifact v2, semantic graph v5, and internal runtime component registry
contract v1. Forms are absent from those frozen products until the roadmap
slice that deliberately versions each boundary.

Verified: pre-change `just check`, including the H21 authority/schema/no-runtime-
discovery audit, the complete existing compiler and CLI suites, strict workspace
clippy, formatting, and 26 real-browser tests; focused I0 reservation and frozen
core-schema tests; strict core clippy; formatting; and diff check.

Commit: this entry's commit (`compiler: audit phase I forms entry`).

Next is I1: introduce canonical `FormId`, `FormInstanceId`, and `FieldId`
identity domains only.

## 321. Canonical Form identities

Completed Phase I1. The canonical semantic-ID authority now defines distinct
immutable `FormId`, `FormInstanceId`, and `FieldId` domains. A Form definition
is qualified by its semantic owner, a Field is qualified by its exact Form,
and a Form instance is qualified by both the compiler-owned component instance
and Form definition. Repeated component instances of one definition therefore
receive distinct Form execution identities without runtime-generated IDs.

I1 deliberately does not choose authored Form syntax facts. The ID layer can
accept the canonical owner and name selected by I2, but it does not decide what
declaration `@form()` decorates, lower `@field()`, or introduce binding,
validation, submission, serialization, reset, tracking, IR, runtime,
inspection, diagnostic, or resumability behavior.

The H21 constructor authority remains frozen. I1 consumes an existing
`ComponentInstanceId`; it neither calls component-instance constructors from a
new authority nor discovers instance topology. The Phase H freeze test caught
and rejected an initial test-only constructor call outside
`component_instance.rs`; the proof now uses retained immutable instance IDs and
the unchanged audit passes.

No schema changed.

Verified: all 295 core tests; the focused Form identity-domain proof; the H21
authority/schema/no-runtime-discovery audit; strict workspace clippy;
formatting; and diff check. No browser gate was run because I1 adds no runtime
surface.

Commit: this entry's commit (`compiler: add canonical form identities`).

Next is I2: lower `@form()` into canonical Form products only.

## 322. I2 Form syntax contract blocker

Stopped before implementing Phase I2. The authoritative roadmap defines I2
only as "Lower `@form()`" and does not identify the valid decorated declaration
kind. The existing parser retains decorators on classes, properties, and
methods, so all three shapes are mechanically available but none is
semantically authoritative.

The missing choice is architectural rather than syntactic plumbing. A
canonical `FormId` requires an exact semantic owner and Form name, and later
Field ownership, component-instance qualification, duplicate detection,
submission planning, and nested-Form rejection all depend on that choice. The
roadmap does not say whether the Form name comes from a class or member name,
whether a component may own one or multiple Forms, whether `@form()` accepts
only zero arguments, or which invalid candidates I2 must retain for I18.
Frozen A-H products contain no Form-specific rule that can supply those facts.

No implementation was attempted and no fallback was inferred from the older
function-style `form(...)` planning examples, because the attached roadmap is
the complete contract and explicitly introduces decorator syntax. Choosing a
class, property, or method target would invent language semantics and would
make I3-I20 consume an unauthoritative owner/name model.

Completed work remains committed through I1 (`2398c6f`); I0 is `12381a4`.
No schema or runtime product changed after I1, and I2-I20 remain untouched.

Required unblock contract: state the valid `@form()` declaration kind, the
canonical Form name source, per-component Form multiplicity, decorator
argument/arity rules, and invalid-candidate retention expectations. With those
facts authored, resume at I2 without revisiting I0 or I1.

Commit: this blocker entry's commit (`docs(forms): record I2 syntax contract blocker`).

## 323. Canonical Form declarations

Completed Phase I2 from the supplied authoritative declaration contract. The
parser now retains normalized invocation, argument-span, target-kind,
identifier-name, static, `declare`, definite-assignment, initializer, type,
conflicting-decorator, and full declaration provenance facts for every
recognized `@form` candidate. Bare `@form`, nonzero argument lists, class,
method, accessor, parameter, unsupported-name, and non-component targets remain
inspectable rather than disappearing during parsing.

Canonical lowering accepts only one invoked zero-argument `@form()` on a
directly authored, non-static, declaration-only canonical component instance
field. The authored identifier is the Form name, the direct component is its
owner, and identity is `<ComponentId>/form:<field-name>`. The exact `Form`
annotation resolves through a module-scoped compiler built-in type authority;
local or imported lookalikes, aliases, subclasses, unions, generics, missing
types, and other annotations remain invalid.

Every recognized occurrence lowers to an immutable
`FormDeclarationCandidate` with canonically ordered violation facts. Invalid
fields retain `FormId` only when direct canonical owner and stable identifier
inputs exist. Invalid classes, methods, getters, setters, parameters,
unsupported names, declarations outside canonical components, and inherited
non-declarations receive no fabricated Form identity. Duplicate declarations
retain every candidate, invalidate the entire owner/name group, and select no
source-order winner; repeated `@form` decorators are likewise invalid without
being mislabeled as separate declarations.

Only valid candidates produce `FormEntity` records. Each entity retains its
canonical identity, direct `SemanticOwner`, authored component-field identity,
authored name, full declaration provenance, client boundary, and nominal
compiler semantic type. Forms participate in internal ASM entity lookup,
ownership, provenance, and validation. No Form instance, Field, template
binding, validation, submission, serialization, reset, dirty/touched tracking,
IR, runtime, browser, public diagnostic, resume, or Phase J execution product
was introduced.

No public schema changed. Forms are explicitly excluded from frozen semantic
graph v5 and CLI ASM inspection v8 until a later roadmap-owned versioned slice.
The H21 authority/schema/no-runtime-discovery audit remains unchanged.

Verified: 301 core tests; 8 parser unit and 26 parser integration tests; six
focused I2 Form tests covering valid declarations, ownership, type authority,
provenance, reversed input order, invalid retention, identity capability,
duplicate behavior, repeated decorators, inheritance, and frozen graph
projection; all 126 CLI explain/ASM/build tests; the H21 freeze audit; strict
workspace clippy; formatting; and diff check. No browser gate was run because
I2 changes no runtime or browser surface.

Commit: this entry's commit (`compiler: lower canonical form declarations`).

Next is I3. The attached roadmap states only "Lower `@field()`" and does not
freeze Field declaration ownership, naming, typing/value, arity, duplicate, or
invalid-candidate identity rules. Those semantics require an authoritative I3
contract before implementation.

## 324. I3 Field syntax contract blocker

Stopped before implementing Phase I3 after committing the complete I2 slice as
`652d256`. The authoritative roadmap defines I3 only as "Lower `@field()`".
That heading does not supply enough semantic inputs to construct the canonical
`FieldId` introduced in I1 or connect a Field to the `FormEntity` authority
frozen in I2.

The missing choices are language contracts, not parser mechanics. I3 must state
which declaration target `@field()` decorates; how it names and references its
exact owning `FormId`; whether the decorated declaration itself supplies a
value/type or is marker-only; whether `@field()` accepts arguments and their
allowed forms; per-Form multiplicity and duplicate behavior; conflicting
decorator rules; and which invalid candidates retain owner, Field identity,
type, and provenance. It must also say whether inherited declarations are
unsupported and whether any field association may come from templates. I2
explicitly forbids inferring these facts from HTML, controls, actions, runtime
registration, or DOM ancestry.

No I3 implementation, parser extension, Field entity, binding, diagnostic,
schema, runtime, or browser behavior was attempted. The frozen A-H products
remain unchanged, I2's canonical Forms remain the only valid future Field
owners, and the worktree is clean after this documentation commit.

Required unblock contract: freeze the I3 declaration target, exact Form owner
reference, authored name source, type/value contract, invocation and arity,
multiplicity and duplicate policy, conflicts, inheritance policy, provenance,
and invalid-candidate identity/retention rules. Then resume at I3 without
revisiting I0-I2 or inferring ownership from templates or runtime behavior.

Commit: this blocker entry's commit (`docs(forms): record I3 field contract blocker`).

## 325. Canonical Form Field declarations

Completed Phase I3 from the supplied authoritative Canonical Form Field
Declarations contract. The parser now retains exact direct
`this.<identifier>` decorator designators, invocation/arity distinctions,
identifier/private/computed targets, declared types, initializers, existing
folded constant expressions, conflicting decorators, and complete source
provenance for every recognized `@field` placement.

Canonical lowering accepts only one invoked `@field(this.<formName>)` on a
directly authored, non-static canonical component field. The designator must
resolve to one valid I2 Form authored directly on the same component. The
authored field identifier supplies the name, and the existing Form-owned
`FieldId` derives only from that exact `FormId` plus name. No default Form,
nearest Form, inherited Form, cross-component Form, template ancestry, or
runtime lookup participates in resolution.

Every recognized placement receives a distinct source-qualified
`FormFieldDeclarationCandidateId`. Candidates retain independently true
target, decorator, designator, owner, initializer, type, serialization,
duplicate, conflict, and inheritance violation facts plus detailed provenance.
Invalid candidates never receive a `FieldId`, even when a plausible Form/name
path exists. Duplicate `(FormId, name)` groups retain all candidates, publish
no valid Field, and choose no source-order winner; unrelated valid Fields
remain unaffected.

Valid declarations lower to immutable `FormFieldEntity` records with exact
Component and Form ownership, authored-field identity, semantic type and type
assignment, compiler-known serializable initial value, full declaration and
subspan provenance, client boundary, and explicit per-Form authored order. The
type/value path reuses Phase C canonical declared-type and alias resolution,
normalization, inference, `is_assignable`, serialization compatibility, and
the existing immutable constant folder. I3 adds no Form-specific parser,
evaluator, assignability engine, or serializer authority.

The internal ASM now exposes canonical `Component -> Form -> Field` ownership,
provenance, entity lookup, deterministic queries, and semantic type facts.
Frozen semantic graph v5 and CLI ASM inspection v8 explicitly omit the new
Field entity kind, so no public schema changed. No template binding,
validation, submission, serialization/reset plan, dirty/touched state, IR,
runtime artifact, browser behavior, public diagnostic, or resumability product
was introduced.

Verified: all 306 core tests; 9 parser unit and 26 parser integration tests;
five focused I3 tests covering valid primitive, nullable, array, tuple, object,
constant, alias, ownership, provenance, ordering, reversed-input, invalid
decorator/target/designator/Form/type/value, inheritance, duplicate, conflict,
candidate-only identity, and frozen-schema behavior; all 126 CLI
explain/ASM/build tests; the H21 authority/schema/no-runtime-discovery audit;
strict workspace clippy; formatting; and diff check. No browser gate was run
because I3 changes no runtime or browser surface.

Commit: this entry's commit (`compiler: lower canonical form fields`).

Phase I is complete through I3. I4 requires an authoritative template-control
binding contract before implementation; its one-line roadmap heading does not
define binding syntax, supported controls, exact Form/Field resolution,
multiplicity, compatibility, ambiguity, provenance, or invalid-candidate
retention, and those semantics must not be inferred from HTML or the DOM.

## 326. Canonical Form control binding

Completed Phase I4 from the supplied authoritative Canonical Form Control
Binding contract. The normalized parser/render/template boundary now retains
the complete authored attribute set for compiler analysis: exact direct
`this.<identifier>` facts, literal values, name/value/expression spans, dynamic
expressions, spreads, event conflicts, input/select modifiers, and control
children. These immutable facts remain parallel to backend-facing Template
attributes; the compiler-only `field` marker is not emitted into HTML,
manifests, or runtime binding metadata.

Every JSX `field` attribute receives a source-qualified
`FormFieldBindingCandidateId`, including bare, static, unsupported-expression,
duplicate, spread-conflicted, unsupported-control, incompatible, unresolved,
ambiguous, cross-component, invalid-I3, and conflicting candidates. Candidates
retain control, attribute, expression, field-name, control-kind, radio-value,
and related conflict provenance plus any successfully resolved Field/Form/type
evidence. Invalid candidates never receive a `FieldBindingId`.

Canonical lowering accepts only intrinsic `input`, `textarea`, and `select`
controls using exact `field={this.<fieldName>}`. Resolution uses the template's
canonical Component and valid I3 `FormFieldEntity` products; the Form comes
only from `owner_form`. State, computed, method, inherited, imported, Context,
component, Slot, local-variable, HTML-name, `<form>`-ancestry, runtime-instance,
and DOM lookup paths never participate.

Valid occurrences receive control-qualified `FieldBindingId` identities,
Template ownership, exact control/Field/Form/Component identities, authored
order, complete provenance, field type, supported static input kind, and one
compiler-selected channel: `Value`, `NumericValue`, `Checked`, `RadioValue`,
`SelectedValue`, or `SelectedValues`. Dedicated `FieldBindingField` and
`FieldBindingForm` references preserve use-site relationships without changing
the existing `Component -> Form -> Field` declaration ownership chain.

Compatibility reuses canonical semantic types, literal typing, and
`is_assignable`. Immutable normalization metadata freezes nullable text and
numeric policies without implementing them. Text, numeric, checkbox, radio,
single-select, and array-only multiple-select shapes are checked; tuples,
objects on scalar controls, nullable checkboxes, unsupported/dynamic input
types, file/color inputs, dynamic `multiple`, missing/incompatible/duplicate
radio values, textarea children, value/checked/default/selected conflicts,
`onInput`/`onChange`, and separate `form` attributes remain invalid facts.

Non-radio Fields allow at most one otherwise-valid control occurrence. Radio
groups are keyed only by `(ComponentId, FieldId)`, retain authored order, and
require unique compiler-known static scalar values. No HTML `name`, source
winner, type heuristic, focus rule, event precedence, or runtime grouping is
introduced. Malformed candidates do not poison otherwise-valid bindings merely
because partial Field resolution succeeded.

The internal ASM exposes binding entity lookup, Template ownership,
deterministic queries, provenance, and exact Field/Form semantic references.
`EZASM1202` verifies that retained products equal canonical recomputation.
Frozen semantic graph v5 and CLI ASM inspection v8 explicitly filter bindings
and both new reference kinds, so no public schema changed. No I5 ownership
graph, validation, submission, serialization/reset plan, dirty/touched state,
IR, runtime artifact, browser execution, public diagnostic, or resumability
product was introduced.

Verified: all 312 core tests; 10 parser unit and 26 parser integration tests;
six focused I4 tests covering every supported static input kind, valid channels
and normalization, nullable values, radio groups, select multiplicity,
declaration/Form separation, nested controls, ownership/references/provenance,
invalid syntax/controls/resolution/types/conflicts/multiplicity, candidate-only
identity, malformed-candidate isolation, and reversed multi-file stability;
all 126 CLI explain/ASM/build tests; the complete seven-test Phase H
fixture/freeze matrix; strict workspace clippy; formatting; and diff check. No
browser gate was run because I4 emits no runtime or browser behavior.

Commit: this entry's commit (`compiler: bind canonical form controls`).

Phase I is complete through I4. I5 requires an authoritative Form
ownership-graph contract: its one-line roadmap entry does not define a graph
identity, node/edge domains or directions, containment versus binding-use
relations, Form-instance participation, invariants, queries, ordering,
validation, or schema boundary. Those semantics must not be inferred from
direct ASM ownership, template/HTML ancestry, the DOM, or runtime registration.

## 327. Canonical Form ownership graph

Completed Phase I5 from the supplied authoritative Canonical Form Ownership
Graph contract. `FormOwnershipGraphId` is a typed immutable product identity
derived from the sorted and deduplicated Phase H build-root identity set. Graph
nodes retain the existing canonical Component, Form, Field, template-control,
and Field-binding identities through a typed sum key; I5 introduces no wrapper
semantic identity for nodes or index-derived edge IDs.

The graph is a form-focused projection over canonical ASM ownership and
references rather than a second ownership authority. Declaration containment
is exactly `Component -> Form -> Field`. Each Field binding is now owned by its
exact intrinsic template-control semantic entity, the narrowest authored use
site in the existing Template tree. Binding-to-Field and binding-to-Form remain
the frozen I4 `FieldBindingField` and `FieldBindingForm` references. Neither a
Form nor a Field owns a template use site.

Construction consumes only valid I2 Forms, I3 Fields, I4 bindings, Phase H
build roots, canonical template entities, ASM ownership, ASM references, and
retained provenance. It does not walk or re-resolve parser/JSX syntax. Invalid
Form, Field, and binding candidates remain in their dedicated registries and
cannot become graph nodes; unrelated valid products remain intact.

Canonical node order is node kind then semantic identity. Roots use Component
identity order, ownership edges use owner/child identity order, and references
use source/kind/target order. I3 authored Field order remains separate metadata
and is not inferred from graph order. Repeated builds and reversed multi-file
inputs produce byte-equivalent in-memory graph products.

Read-only queries expose exact nodes, direct owners/children, Forms of a
Component, Fields of a Form, binding owners, bindings by Field/Form, inverse
Field/Form references, component containment, and roots. No query constructs
missing ownership, default Forms, runtime instances, or DOM facts.

Immutable validation reserves internal `EZASM1203` through `EZASM1219` for
duplicate nodes, missing/unknown/multiple owners, cycles, reachability,
unknown references, Component/Field/Form/binding reciprocity, invalid-candidate
promotion, instance leakage, provenance, ordering, and graph identity drift.
`EZASM1220` detects stale retained validation in the containing ASM. Validation
is deterministic and does not repair malformed products.

I5 explicitly excludes `ComponentInstanceId` and `FormInstanceId`. Repeated
component occurrences do not duplicate declaration Form nodes. The already
reserved `FormInstanceId::for_component_instance(ComponentInstanceId, FormId)`
identity remains deterministic and distinct in focused tests, but no instance
is created or added to the graph.

No public schema changed. Semantic graph v5, CLI ASM inspection v8, check JSON
v4, template manifest v2, runtime artifacts, and resume manifest v4 remain
unchanged; generic serializers continue to filter Phase I entities. The graph
has no serialized snapshot, no internal schema document, and no
`forms.runtime.json`. No validation language, submission, serialization/reset
plan, tracking, IR, runtime registry, browser execution, public diagnostic, or
resumability product was introduced.

Verified: seven focused I5 tests covering basic and multiple-Form ownership,
exact control-use binding ownership and references, radio bindings,
cross-component identity isolation, invalid-candidate exclusion, malformed
owners/endpoints/cycles/reciprocity/provenance/ordering/instance leakage,
read-only queries, repeated component instances, reserved Form-instance
identity, reversed inputs, repeated construction, and frozen public semantic
graph output; all 319 core tests; 10 parser unit and 26 parser integration
tests; strict workspace clippy; formatting; all seven Component fixture/freeze
tests; all 12 Context fixture/freeze tests; all 126 CLI inspection/build tests;
and all 26 real-browser tests through `just check`.

Commit: this entry's commit (`compiler: project canonical form ownership`).

Phase I is complete through I5. I6 requires an authoritative Validation Graph
contract: the roadmap's one-line entry does not define validation declaration
syntax, rule identity or catalog, arguments, exact Field/Form targets,
dependency semantics, execution boundaries, invalid-candidate retention,
ordering, graph invariants, or schema/diagnostic policy. I5 ASM integrity
validation must not be reinterpreted as Form value-validation semantics.

## 328. Canonical Form validation graph

Completed Phase I6 from the supplied authoritative Canonical Form Validation
Graph contract. The parser now retains each outer `@validate` invocation shape
and its normalized nested rule expression, including direct call identity,
ordered argument facts, exact `this.<identifier>` designators, literal strings,
existing compiler-owned constant expressions, unsupported expression shapes,
module/import value shadows, and complete decorator/rule/argument/target
provenance. Semantic target, dependency, rule, type, and validity decisions
remain outside the parser.

Every recognized placement receives one source-qualified
`ValidationRuleCandidateId`, including decorators on classes, methods,
accessors, parameters, static fields, fields outside canonical components, and
invalid I3 declarations. Candidates retain partial Component, declaration,
Field, Form, dependency, normalized argument, compatibility, conflict, and
source evidence plus all independently true violations. Invalid candidates
never receive a `ValidationRuleId`.

Canonical lowering accepts one invoked `@validate(<rule-expression>)` per rule
on a valid direct I3 Field declaration. The closed catalog is `required`,
`min`, `max`, `minLength`, `maxLength`, `pattern`, `email`, `equals`, and
`notEquals`. Valid identity derives only from the canonical target `FieldId`
plus the zero-based authored validation-decorator ordinal. All valid rules use
the Client boundary. User/import-authored shadows of compiler rule names,
unknown/member/bare rule expressions, invalid arity, unsupported constants,
non-finite numbers, invalid nonnegative lengths, and invalid flags-free
ECMAScript patterns remain candidate violations.

Type compatibility consumes the canonical I3/Phase C `SemanticType`,
normalization, serialization compatibility, and `is_assignable` authorities.
Numeric and length rules use compiler-folded normalized constants; pattern uses
the pinned OXC ECMAScript regex grammar; string, numeric, length-bearing,
required-serializable, and equality-comparable domains follow the frozen I6
contract. I6 introduces no validation-specific type parser, alias resolver,
constant evaluator, assignability engine, serializer checker, or JavaScript
execution.

Cross-Field rules accept only exact direct `this.<fieldName>` designators that
resolve to a valid Field on the same canonical Component and exact same
`FormId`. Missing, invalid, cross-Component, cross-Form, inherited, and self
dependencies do not fall back to any nearby/default Form or Field. Valid direct
dependencies become canonical `ValidationRuleField` references; they do not
create scheduling, invalidation, execution, state, or runtime behavior.

Normalized duplicate groups are invalidated wholesale with no winner.
Contradictory min/max ranges, minLength/maxLength ranges, and equals/notEquals
pairs against the same dependency retain every candidate while excluding the
conflicting group. Deterministic strongly connected analysis retains exact
cycle Field/candidate sets and excludes every cycle-participating rule from
valid executable membership. Rule execution order is explicit Field authored
order followed by validation decorator authored order and is not inferred from
map or graph identity order.

Valid candidates lower to immutable `ValidationRule` records and canonical ASM
ownership `Field -> ValidationRule`. `ValidationGraphId` derives from the same
sorted Phase H build-root authority used by I5. Its Form, Field, and Rule nodes
retain existing canonical identities. `FormOwnsField` projects I5 ownership,
`FieldOwnsRule` projects ASM ownership, and `RuleDependsOnField` projects ASM
references, so I5 remains the sole declaration ownership authority. Read-only
queries expose rules by Form/Field, exact rule targets, dependencies,
dependents, candidate associations, and cycles without reconstructing syntax or
runtime state.

Immutable integrity validation reserves `EZASM1221` through `EZASM1239` for
node/endpoint/owner/target/dependency/component/Form/cycle/provenance/order and
identity violations. `EZASM1240` detects stale retained graph validation and
`EZASM1241` detects stale candidate/rule lowering. Validation is deterministic
and never repairs malformed products.

No public schema changed. Semantic graph v5, CLI ASM inspection v8, check JSON
v4, template manifest v2, runtime artifacts, and resume manifest v4 explicitly
filter Validation Rules and references. No `forms.runtime.json`,
`validation.runtime.json`, Form instance, runtime validation registry/state,
validation execution, DOM discovery, browser behavior, public Form diagnostic,
IR, or resumability product was introduced.

Verified: seven focused I6 core tests covering valid unary/cross-Field rules,
canonical identity/ownership/references/order/provenance, canonical type domains,
same-Form resolution, invalid targets/rules/arguments/dependencies/types,
authored rule-name shadows, duplicate/contradiction/cycle exclusion, graph
integrity/staleness, candidate-only invalid identity, reversed multi-file
stability, and frozen semantic-graph output; two focused parser retention tests;
all 326 core tests; 12 parser unit and 26 parser integration tests; 2 CLI unit
tests including ASM inspection v8 leak prevention; all 7 Component fixture/freeze
tests; all 12 Context fixture/freeze tests; all 126 CLI inspection/build tests;
strict workspace clippy; formatting; and all 26 real-browser tests through
`just check`.

Commit: this entry's commit (`compiler: build canonical form validation graph`).

Phase I is complete through I6. Stop before I7: no complete authoritative I7
contract is present for cross-Field invalidation propagation, dependency
scheduling, update triggers, derived planning products, execution/runtime
state, or schema policy. I6 direct dependencies and cycle facts must not be
reinterpreted as an implicit I7 plan.

## 329. Canonical cross-Field validation dependency planning

Completed Phase I7 from the supplied authoritative Canonical Cross-Field
Validation Dependency Planning contract. I7 consumes only immutable I3
Form-Field records, I5 Form ownership, and I6 valid Rules/ValidationGraph
direct `RuleDependsOnField` edges. It does not parse or revisit decorators,
resolve names, reclassify Rules, recompute compatibility, inspect JSX/DOM,
derive Form instances, or execute validation.

Every valid Form now receives exactly one declaration-level
`ValidationPlanId::for_form(FormId)`, including empty Forms and Forms without
cross-Field Rules. Every eligible I6 direct read edge receives one
`FieldDependencyId::for_rule_and_source(ValidationRuleId, source FieldId)`.
Each immutable dependency retains its exact source Field, target Field,
dependent Rule, Form, Component, I3 Field/rule authored ordering, Client
boundary, dependency-designator provenance, and source/target declaration
provenance. No identity derives from field names alone, positions, insertion
order, Component instances, runtime registration, or DOM identity.

Each Form plan retains every Field in I3 authored order, one source entry and
one target entry per Field, and explicit deferred initial/submission statuses.
Source entries provide the invalidation direction (source Field to directly
dependent Rule) while target entries retain the read-side declaration query.
They remain distinct from the I6 Rule-to-source-Field edge. Unary Rules never
enter dependency entries; a target-Field write does not schedule its own
cross-Field Rule unless that Field is independently a source for another Rule.

Pure one-Field and change-set queries accept canonical Field identities only.
They normalize duplicate changed Fields by I3 authored Field order, combine
only direct source entries, schedule each Rule once by target Field authored
order then Rule authored order then Rule ID, deduplicate scheduled target
Fields in authored order, and retain every triggering FieldDependencyId.
They do not inspect values, compare old/new values, infer browser events, run
validators, schedule unary Rules, create Rule-to-Rule edges, or compute
transitive invalidation. Validation execution remains distinct from a Field
write, so a C-to-B-to-A read chain schedules B's Rule from C and A's Rule only
from B.

Malformed/stale canonical I5/I6 inputs retain deterministic blocked planning
records without a dependency identity or fallback Field. Immutable integrity
validation reserves `EZASM1242` through `EZASM1270` for plan/dependency
identity, exact I5/I6 correspondence, Form/Component/boundary/provenance,
index reciprocity, direct-only propagation, ordering, and instance-boundary
facts. `EZASM1271` detects stale retained plan validation and `EZASM1272`
detects stale whole planning products in shared ASM validation. Validation is
deterministic and never repairs drift.

No public schema changed. Semantic graph v5, CLI ASM inspection v8, check JSON
v4, template manifest v2, resume manifest v4, all runtime artifacts, and
browser output remain unchanged and exclude I7 plans, dependencies, invalidation
edges, and schedules. No Form instance, validation state/execution, dirty or
touched tracking, submission/reset/serialization plan, IR, runtime registry or
artifact, public diagnostic, or resumability product was introduced.

Verified: five focused I7 core tests covering exact dependency identity and
provenance, empty plans, direct-only propagation, unary and target-write
exclusion, deterministic source/target/rule ordering, change-set deduplication
and evidence retention, malformed/stale plan blocking, missing-projection and
index integrity, instance isolation, repeated/reversed multi-file construction,
and frozen public semantic-graph output; the focused Form/instance identity
test; strict `ezc_core` clippy; formatting; and `just check` with all 331 core
tests, 12 parser unit tests, 26 parser integration tests, 2 CLI unit tests, 7
Component fixtures, 12 Context fixtures, 126 CLI inspection/build tests, and
26 real-browser tests.

Commit: this entry's commit (`compiler: plan canonical form validation dependencies`).

Phase I is complete through I7. Stop before I8: no complete authoritative I8
contract is present for dirty/touched identity, transitions, focus/blur/change
mapping, reset interaction, state, runtime execution, or schema policy. I7
plans must not be reinterpreted as dirty/touched, event, or validator-execution
semantics.

## 330. Canonical Form dirty and touched tracking

Completed Phase I8 from the authoritative I8 contract. I8 consumes only
immutable I3 Field declarations, I4 valid control bindings, and I5 ownership.
It introduces declaration-level `DirtyTrackingPlanId::for_form`,
`TouchedTrackingPlanId::for_form`, and `FieldTrackingId::for_field` identities:
one dirty/touched plan for every valid Form and one shared tracking identity
for every valid Field, including unbound Fields. None are Form-instance or
runtime-storage identities.

Dirty tracking is an immutable transition specification: the initial state is
false; a committed Field value is dirty exactly when canonical recursive
`SerializableValue` equality differs from the I3 initial value; returning to
the initial value clears dirty; field/form reset facts clear dirty. Touched
also starts false and is set only by a retained bound-control blur source;
focus, input, change, and submit are intentionally absent. All radio controls
for a Field retain one shared Field touched record, and unbound Fields retain
an empty blur-source set. I8 records later direct-validation and reset handoff
facts only; it neither executes tracking nor schedules/runs validation.

`DirtyTrackingGraph` and `TouchedTrackingGraph` are immutable internal
products, with deterministic I3 Field order, exact Form/Field/Component and
initial-value reciprocity validation, binding/blur-set validation, provenance,
and stale-product detection (`EZASM1273` through `EZASM1285`). They are not a
new ASM ownership authority and exclude Component/Form instances, invalid
candidates, DOM discovery, runtime registration, and browser behavior.

No public schema changed. Semantic graph v5, ASM inspection v8, check JSON v4,
template manifest v2, resume manifest v4, runtime artifacts, and browser output
remain unchanged and exclude I8 plans and records.

Verified: four focused I8 core tests for per-Form/per-Field plans, composite
structural equality and dirty return-to-initial behavior, blur-only/radio and
unbound touched participation, malformed-product detection, and frozen public
semantic-graph output; all 334 `ezc_core` tests; strict `ezc_core` clippy;
formatting; and `git diff --check`.

Commit: this entry's commit (`compiler: plan canonical form tracking`).

Phase I is complete through I8. Proceed to I9 only under its authoritative
submission-planning contract; I8 tracking facts must not be treated as runtime
state, events, validation execution, or reset execution.

## 331. Canonical Form submission planning

Completed Phase I9 from the authoritative submission-planning contract. The
parser now retains submit decorator invocation, exact direct `this.<form>`
designator, method static/async/parameter/return facts, and action-decorator
facts. I9 converts every recognized placement into a source-qualified
`SubmissionDeclarationCandidateId`; malformed declarations retain evidence and
never receive a plan identity.

Only direct Component methods with valid zero-argument `@action()`, one-argument
`@submit(this.<form>)`, an exact same-Component valid Form, non-static,
non-async, zero-parameter, exact `void` signature lower. Duplicate Form
submissions and attempts to submit multiple Forms from one method invalidate
their entire valid-looking group without a winner. A valid
`FormSubmissionPlan` receives `SubmissionPlanId::for_form(FormId)`, references
the existing exact compiler Action Batch, lists every valid I6 Rule once in I3
Field order then I6 decorator order, blocks action execution on an invalid
rule, and freezes automatic reset as `SubmitResetPolicy::Never`.

I9 is internal declaration planning only. It neither invokes validation nor
actions, creates Form instances, scans HTML/DOM, derives an action by method
name at runtime, serializes values, resets Fields, adds runtime state/artifacts,
or changes public schemas.

Verified: parser retention for direct designators, bare invocation, signature,
and static-method facts; focused I9 valid-plan/action-batch/rule-order and
invalid-candidate/no-winner tests; all 13 parser unit and 26 parser integration
tests; all 337 core tests; strict parser/core clippy; formatting; and
`git diff --check`.

Commit: this entry's commit (`compiler: plan canonical form submissions`).

Phase I is complete through I9. Proceed to I10 only under its authoritative
serialization-planning contract; submission plans must not be interpreted as
execution, serialization, reset, or runtime registration.

## 332. Canonical Form serialization planning

Completed Phase I10 from the authoritative serialization contract. I10 retains
normalized `@serialize(...)` decorator facts on Form declarations and creates
one immutable `SerializationPlanId::for_form(FormId)` for every valid Form.
Absent decorators select JSON; the only explicit static formats are `json`,
`form-data`, and `url-encoded`. Invalid invocation/arity/format or duplicate
decorator groups produce a non-executable plan status without silently choosing
a winner.

Each plan contains every I3 Field in authored order, keyed solely by the
authored Field identifier. It records a compiler-selected JSON, FormData, or
URL-encoded conversion kind with the canonical initial value and provenance.
Eligibility reuses the Phase C serialization authority; a nonserializable Field
keeps the Form out of executable submission/runtime membership rather than
being omitted. Valid I9 submissions link to plans by exact
`SubmissionPlanId`, never by method name. Runtime conversion, percent encoding,
and artifact emission remain deferred.

No public schema changed. Semantic graph v5, ASM inspection v8, check JSON v4,
template manifest v2, resume manifest v4, and existing runtime artifacts remain
unchanged and do not expose I10 plans.

Verified: focused implicit/explicit format, Field order, conversion selection,
invalid format, and plan-status tests; all 13 parser unit, 26 parser
integration, and 339 core tests; strict parser/core clippy; formatting; and
`git diff --check`.

Commit: this entry's commit (`compiler: plan canonical form serialization`).

Phase I is complete through I10. Proceed to I11 only under its authoritative
reset-planning contract; serialization plans are not runtime serializers.

## 333. Canonical Form reset planning

Completed Phase I11 from the authoritative reset contract. Every valid Form
now receives one immutable `ResetPlanId`, and every valid I3 Field receives one
`FieldResetOperationId` derived only from that Form reset plan and exact Field.
The operations preserve I3 authored Field order and retain canonical initial
values, source provenance, and the complete ordered reset sequence: restore
value; update all I4-bound controls; clear dirty; clear touched; clear Field
validation. Unbound Fields retain the same operation with no control writes.

After Field operations, the Form plan clears aggregate validity and submission
state. `schedule_validation` is explicitly false: no reset operation invokes a
submit action, revalidates, creates an I7 change schedule, infers a reset
button, or introduces authored reset syntax. I11 validates the mandatory I8
tracking correspondence before producing plans and keeps reset as an immutable
declaration product only.

No public schema changed. Existing semantic graph, ASM inspection, check JSON,
template manifest, resume manifest, runtime artifacts, and browser behavior
remain unchanged and exclude I11 reset programs.

Verified: focused multi-Field/bound-unbound ordered reset and no-revalidation
test; all 340 core tests; strict core clippy; formatting; and `git diff --check`.

Commit: this entry's commit (`compiler: plan canonical form reset`).

Phase I is complete through I11. I12 is the first authorized instance
projection slice; declaration-level reset identities must not be used as
mutable runtime storage.

## 334. Canonical Form IR

Completed Phase I12. The compiler projects each valid declaration Form through
the frozen Phase H `ComponentInstancePlan`; every resulting `FormInstanceId`
combines the exact Form and Component instance. All mutable Field value, dirty,
touched, validation, aggregate-validity, and submission slots are derived from
that Form instance, never a declaration Form/Field ID alone. Repeated Component
instances therefore retain separate Form IR and storage.

I12 adds internal typed initialize, Field input, blur, and reset operation
sequences. Input records compiler-owned channel read/normalization/write/dirty/
validation/aggregate/control synchronization stages; blur records touched then
target validation/aggregate; reset remains reset/clear-validation-only. This is
an immutable IR projection without execution, public schema, runtime artifact,
or DOM discovery.

Verified: focused repeated-instance storage test; all 341 core tests; strict
core clippy; formatting; and `git diff --check`.

Commit: this entry's commit (`compiler: lower canonical form IR`).

## 335. Form IR optimization

Completed Phase I13. `OptimizedFormIrReport` retains both the source I12 report
and a separate optimized report plus immutable-input proof and pass metrics.
The current optimizer makes no observable-changing rewrite: every Form instance,
instance-qualified slot, control/Field write, dirty/touched/validation write,
aggregate calculation, serialization/submission transition, action invocation,
and reset operation retains its exact identity and order. This freezes the
optimization boundary without permitting runtime discovery or equality
suppression.

No public schema changed. Verified: focused source/optimized equality coverage,
all 342 core tests, strict core clippy, formatting, and `git diff --check`.

Commit: this entry's commit (`compiler: optimize canonical form IR`).

## 336. Runtime Form Registry

Completed Phase I14. The compiler now projects I8–I13 into internal
`RuntimeFormRegistry` contract version 1. Registry records retain exact Form
definitions, I3 Field order, canonical bindings/rules/plans/reset facts, and
I12/I13 instance-qualified programs and storage. Every instance record is
keyed by `FormInstanceId` and its exact Component instance; raw authored names,
DOM scanning, validator discovery, field registration, and runtime semantic-ID
generation remain absent.

No public schema or artifact changed. Verified: focused registry version and
instance qualification coverage, all 343 core tests, strict core clippy,
formatting, and `git diff --check`.

Commit: this entry's commit (`compiler: build runtime form registry`).

## 337. Runtime Form Artifact

Completed Phase I15. The compiler emits one deterministic `forms.runtime.json`
schema-v1 artifact from canonical Forms products and embeds that exact document
as `ez-forms-runtime`. Artifact records use canonical Forms, Field, Binding,
Rule, dependency, plan, instance, slot, and program identities; authored names
are explicit non-authoritative debug values only. No DOM discovery, validator
registration, or runtime semantic reconstruction was introduced.

Template manifests advance to v3 only when Forms bridges exist, carrying exact
control anchors, FieldBindingIds, FormInstanceIds, input/blur program addresses,
and compiler-selected channels. Resume manifests advance to v5 with planning
metadata only for instance-qualified Form slots; no live-state restoration is
implemented. Semantic graph v5, ASM inspection v8, and check JSON v4 remain
unchanged.

Verified: 345 core tests; all CLI suites including the Forms build/embed test
and browser suite; strict core/CLI clippy; formatting; and `git diff --check`.

Commit: this entry's commit (`compiler: emit forms runtime artifact`).

## 338. I16 architectural blocker

I16 cannot be completed under the frozen Phase I contracts. The required
compiler-associated submission host/listener has no canonical semantic input:
I4 explicitly defers HTML `<form>` element semantics and I9's
`FormSubmissionPlan` records only a Form, action method, and action batch.
I15's v3 bridge covers controls only; it has no `SubmissionHostId`, host anchor,
or Form-to-host binding. Choosing a DOM ancestor, HTML name, first control, or
default host would introduce forbidden runtime/DOM discovery or new language
semantics.

Required authoritative addition before resuming I16: a declaration-level
submission-host identity plus an exact compiler-owned Form-to-host binding and
runtime/template anchor policy. No I16 runtime execution is committed.

## 339. I15-S Canonical Form Submission Host Binding

Corrected the not-yet-frozen Phase I template-manifest-v3 and
`forms.runtime.json`-v1 Forms contracts before I16. Explicit intrinsic host
syntax is only `<form form={this.<formName>}>`; the compiler-only attribute is
retained as normalized authored-template evidence and excluded from emitted
HTML. Every recognized placement receives `SubmissionHostCandidateId`; only a
valid same-Component Form with an exact SubmissionPlan, FormSubmitAction,
SerializationPlan, and Form IR product receives `SubmissionHostId`.

The owning template form element is the host use site. Candidate validation
rejects duplicate hosts for one Form in a Component template, nested hosts, and
contained valid Field controls for another Form. Controls outside a host remain
valid and Field ownership remains declaration/binding-owned rather than host
ancestry-derived. Manifest v3 and Forms artifact v1 project the resulting host
through exact Component/Form instances with compiler anchor, submit event,
action function, action batch, serialization plan, and prevent-default policy.
No implicit/default/nearest/button/DOM-ancestry host was introduced.

Verified focused core host tests, artifact/build coverage, and a real-browser
native-submit probe that checks only the compiler-emitted host anchor and
records.

## 340. Runtime Form Execution

Completed I16 using only I15-S compiler-emitted records. The runtime creates
instance-qualified Field state from `forms.runtime.json`, maps exact control
anchors from template-manifest-v3, and installs one native listener only on
each exact host anchor for its emitted `submit` event. Submission revalidates
the compiler-owned Field state, serializes those state records, invokes the
matching compiler action batch, and records its terminal status.

`preventDefault()` is performed only when the matched host record says so. The
runtime has no nearest-form lookup, DOM control scanning, authored-name
authority, implicit host policy, or `FormData(formElement)` semantic path.
Focused runtime-codegen, Forms artifact/build, and real-browser native-submit
tests pass.

## 341. Forms Inspection

Completed I17 with one core-owned `FormInspectionRegistry` consumed unchanged
by full ASM JSON, selected ASM JSON, entity-mode `explain`, and Forms graph
coverage. Form, Field, FormFieldBinding, and ValidationRule are now first-class
semantic graph nodes. Graph schema v6 freezes typed `ComponentOwnsForm`,
`FormOwnsField`, `FieldOwnsValidationRule`, `ControlOwnsFieldBinding`,
`FieldBindingBindsField`, `FieldBindingBelongsToForm`, and
`ValidationRuleDependsOnField` edges.

ASM inspection schema v9 freezes the shared `form` record. It exposes the
canonical Form owner, Field order, control bindings/channels, validation and
cross-field dependency facts, dirty/touched tracking, submission,
serialization, and reset plans, exact Form instances with compiler-owned
slots/programs, runtime artifact/registry membership, resume planning, and
blocked reasons. Field records additionally expose canonical semantic type,
initial value, per-Field tracking identities, and inbound/outbound rule facts.
Runtime slots, IR, manifests, and artifacts remain inspection facts rather
than semantic ownership.

Verified `cargo test -p ezc_core --lib` (348), `cargo test -p ezc_cli --test
explain --test component_fixtures --test context_fixtures` (128/7/12), strict
core/CLI clippy, formatting, and `git diff --check`.

## 342. Forms Diagnostics

Completed I18 by projecting the frozen `EZC1084` through `EZC1095` Forms
diagnostic range from canonical retained compiler facts only. The projection
uses declaration candidates, validation cycles/rules, submission declarations,
serialization declarations, Field bindings, explicit submission-host demand,
and reset products; it neither reparses authored source nor observes runtime
state. Candidate-only failures retain their candidate semantic identities.

Check JSON schema is now v5. ASM inspection remains v9 because its shared
diagnostic envelope did not require a serialized-shape change. Core and CLI
inspection/component/context regression suites, strict clippy, formatting, and
`git diff --check` pass.

## 343. Fixture Expansion

Completed the I19 Forms determinism expansion with a two-component Form
fixture that reverses compilation-unit input order. It proves byte-identical
`forms.runtime.json`, template-manifest-v3, and resume-manifest-v5 output
without adding compiler, artifact, or runtime semantics.

## 344. Stability Audit and Freeze

Completed I20 and froze Phase I Forms authority in `docs/forms-contract.md`.
It records exact compiler-owned syntax/identity/ownership, runtime
no-discovery rules, diagnostics, schemas, unsupported semantics, and the
Phase J boundary. Frozen versions are graph v6, ASM v9, check v5, template
manifest v3, Forms artifact v1, resume manifest v5, and runtime registry v1.

## 345. Phase I Freeze Gate Repair

The required pre-Phase-J full gate exposed one stale CLI unit assertion from the
pre-I17 inspection contract: it still prohibited `validation-rule` from ASM
output even though I17 deliberately freezes that Forms projection in ASM v9.
The assertion is now a positive ASM-v9 contract test. This corrects Phase I
verification only; it adds no Phase J IDs, products, schemas, runtime behavior,
or executable resumability semantics.

## 346. Phase J Resumability Entry Audit

Confirmed the committed Phase I baseline after repairing its stale fixture
assertions and rerunning the full sequential gate. The sole existing resume
authority remains `resume_manifest.rs` at schema v5; graph v6, template
manifest v3, ASM v9, and check JSON v5 remain frozen.

J0 reserves the public `EZC1096` through `EZC1111` resumability catalog and
the 96-code internal integrity range `EZASM1289` through `EZASM1384`. Its
focused freeze test proves those reservations while preserving every prior
schema. No Phase J identity, executable manifest, snapshot, chunk, anchor,
loader, or runtime behavior exists yet. Next: J1 canonical resume identities.

## 347. Canonical Resume Identities

J1 adds typed compiler-only IDs for boundaries, slots, schemas, capture and
restore programs, chunks, activations, anchors, events, snapshots, builds, and
value records. Constructors consume existing instance/event/storage identities;
there is no source-name, DOM-position, or runtime-counter constructor. No
public serialization or schema changed. Next: J2 liveness and retained slots.

## 348. J2 Resumability Liveness Blocker

J2 cannot classify retained State slots without violating the Phase J identity
contract. The committed runtime writes State into `storageValues` under the
declaration-level `state.storage` identity. Although component runtime records
also expose an `instance_storage_prefix`, no frozen compiler product maps an
exact Component instance plus State declaration to a canonical existing slot,
and the runtime does not consume such a mapping.

Inventing `prefix + state` would create a new frozen storage identity; keeping
the declaration ID would collapse repeated component instances; DOM/runtime
lookup would violate no-discovery; and cold initialization would violate J2's
mandatory State retention. No J2 product was added. Continuation requires an
authoritative instance-qualified State slot identity, artifact projection, and
runtime-map contract.

## 349. J1-A Amendment Blocker

The supplied State-instance-storage amendment resolves the earlier missing
State-slot identity and authorizes a component artifact v3 projection. Its
required action-isolation proof nevertheless needs an exact execution context
for ordinary template events and bindings. The committed template manifest
retains normal events only by node/event/handler/method/action-batch; it has no
ComponentInstanceId. Forms alone have an explicit instance-qualified bridge.

Choosing an instance from DOM ancestry, component name, node order, or a
runtime ordinal would be forbidden discovery. Adding an ordinary mapping or
starting J10 anchor emission early would invent an unamended product and merge
slices. No J1-A code was retained. Continuation requires an authoritative
normal event/binding-to-ComponentInstanceId compiler and runtime contract.

## 350. J1-P Ordinary Template Instance Context Projection

The authoritative ordinary-template amendment supplies the missing bridge:
typed `TemplateInstanceTargetId` and `TemplateInstanceBindingId` project exact
Phase H component instances and canonical template records into an immutable
ordinary registry. Component artifact v3 and template manifest v4 serialize
one closed ordinary target/binding/event authority; Forms control and submit
records reciprocally use the same target identity. Compiler-produced HTML uses
`data-ez-ti`, paired text-binding comments, and instance-projected
conditional/list payloads only; no J10 resume markers are emitted.

The runtime validates the v4/v3 pair, builds exact target/binding/event
indexes, and creates `RuntimeExecutionContext` for ordinary action and binding
execution. J1-A remains unstarted until J1-P's full gate and atomic commit;
its State addressing must consume this context rather than invent another
ownership path.

## 351. J1-C Instance-Qualified Computed Runtime Slots

The authoritative repair inserts J1-C between J1-P and J1-A. It retains every
declaration-level E12 computed cache and dirty identity as program input, then
projects each executable Phase H component instance into exact typed cache and
dirty slot IDs. The immutable registry and v3 component artifact carry those
complete runtime addresses. The ordinary v4/v3 runtime registers them before
cold evaluation, starts every dirty slot from E12's existing initial value,
and reads/writes/invalidates only the slot selected by the active J1-P
execution context. Cache slots do not receive fabricated values before their
compiler-planned evaluation.

J1-C deliberately adds no retained-slot, snapshot, restore, resume-boundary,
or lazy-activation policy. That is J2 work. Internal integrity allocation is
`EZASM1305` through `EZASM1311`; no public Phase J diagnostic is emitted.

Verified: `cargo test -p ezc_core --lib` (359); `cargo clippy -p ezc_core
--all-targets -- -D warnings`; `cargo fmt --all --check`; `git diff --check`;
and `cargo test -p ezc_cli --test runtime_browser` (27 real-browser probes).
The component-runtime probe had not been reaching a product failure: Chrome
blocked while dumping the artifact-heavy DOM because the test runner deferred
reading its piped output until process exit. The browser harness now drains
stdout and stderr concurrently; the component probe also removes parsed JSON
metadata before DOM dumping. The probe keeps the 20-second watchdog and fails
immediately when the runtime reports its terminal error status.

Next: commit J1-C atomically, then resume J1-A using the complete ordinary
execution-context and computed-address bridges.

## 352. J1-A Instance-Qualified State Storage

Completed the authoritative State-storage amendment. The typed
`StateInstanceSlotId` is the exact `(ComponentInstanceId, IrStorageId)` pair,
and the immutable schema-v1 registry projects every executable State storage
in canonical instance/storage order. Component artifact v3 now carries full
`state_slots` records and removes the noncanonical storage prefix.

The v4/v3 browser path registers and cold-initializes only exact State slot
keys. Action records carry canonical storage operands; ordinary bindings use
artifact storage dependencies; State reads/writes and computed invalidation
resolve through the active J1-P execution context. Repeated-instance browser
proofs show that updating A changes only A's slot, DOM binding, computed cache,
and dirty flag. Phase J rejects component artifact v2, while the existing
manifest-v3/component-v2 cold path remains accepted without a resume product.

The full gate also repaired deferred J1-P fixture drift: manifest goldens now
freeze schema v4 ordinary-instance records and action storage operands, and
Phase G/H audit assertions recognize the authorized template-v4/component-v3
boundary.

Verified: `just check` passes formatting, strict workspace clippy, 2 CLI units,
7 Component fixtures, 12 Context fixtures, 128 CLI inspection/build tests, 29
real-browser probes, 364 core tests, 13 parser units, and 26 parser integration
tests. Next: commit J1-A atomically, then implement J2 resumability liveness.

## 353. J2 Canonical Resumability Liveness

Completed J2 with the compiler-only `ResumeLivenessPlan` and its required
retained, recomputable, excluded, and blocked products. The plan classifies
every exact existing State, Computed cache/dirty, instance-selected Context,
Form v5, and Effect activation-metadata slot once, with deterministic queries
by owner, boundary candidate, reason, and existing slot.

Computed recomputation requires complete canonical purity, determinism, eager
evaluation, fixed post-restore ordering, and exact retained/recomputable
dependency evidence. Context classification combines the exact Phase H
instance binding with its canonical Context evaluation source entry, so one
shared provider remains one runtime slot while retaining exact transitive
State/Computed dependencies. Unsupported required values block rather than
cold-initialize, and Effect activation metadata is explicitly excluded from
snapshot values.

Reserved and exercised `EZASM1320` through `EZASM1327` for duplicate
classification, missing storage ownership, unknown dependencies, invalid
retention policy, incomplete recomputation proof, unsupported required values,
invalid boundary promotion, and canonical provenance/order/index drift. No
public schema or runtime behavior changed.

Verified `cargo test -p ezc_core --lib` (368), strict all-target core clippy,
formatting, and `git diff --check`. Next: atomic J2 commit, then J3 Resume
Boundary Graph.

## 354. J3 Canonical Resume Boundary Graph

Completed J3 with `ResumeBoundary`, `ResumeBoundaryGraph`, ownership edges,
activation references, and retained block records. The graph projects exact
Phase H/I products into distinct application-root, Component instance,
structural-region, Form instance, ordinary-event interaction, and enhanced
Form-submit interaction boundaries without merging identities.

Ownership edges are parent-before-child and use only canonical instance
parentage. Structural-template Component instances remain distinct boundaries
under their structural-region boundary, which lets their exact ordinary events
reference a real owner without DOM discovery. Interaction references stay
outside ownership parentage and carry exact existing programs, required owner
boundaries, and J2 retained slots. Blocked Component instances and J2 liveness
slots remain explicit boundary blocks.

Reserved and exercised `EZASM1328` through `EZASM1336` for unique identity,
owner, parentage, cycle, reachability, reciprocity, Phase H/I correspondence,
provenance, and ordering/index integrity. No public schema or runtime behavior
changed.

Verified `cargo test -p ezc_core --lib` (372), strict all-target core clippy,
formatting, and `git diff --check`. Next: atomic J3 commit, then J4 Activation
Policy Planning.

## 355. J4 Canonical Activation Policy Planning

Completed J4 with one `ResumeActivationPolicyDecision` or explicit block for
every J3 boundary. Application bootstrap/registry/delegation, immediate Forms
runtime, and post-restore recomputation are Eager; exact ordinary and Form
submit roots are Interaction; boundaries with no independent executable work
are None. The fixed precedence is encoded directly, with zero heuristic
Visible or Manual policies.

Prerequisites retain exact boundaries, events, retained slots, Form instances,
and recomputable slots. Missing references or blocked required boundaries fail
closed, while unsupported lazy payloads have an explicit eager-fallback path.
Reserved `EZASM1337` through `EZASM1342`. No public schema or runtime changed.

Verified `cargo test -p ezc_core --lib` (376), strict all-target core clippy,
formatting, and `git diff --check`. Next: atomic J4 commit, then J5 deterministic
interaction chunk graph.

## 356. J5 Deterministic Resume Chunk Graph

Completed J5 with one eager chunk and one isolated lazy chunk for each exact
Interaction activation root. Closures consume only canonical J3/J4 program and
prerequisite identities; lazy chunks have no lazy dependencies or unrelated
actions, and module stems precede deterministic content hashes. Visible/Manual
roots remain empty. Reserved `EZASM1343` through `EZASM1348`.

Verified `cargo test -p ezc_core --lib` (379), strict all-target core clippy,
formatting, and `git diff --check`. Next: atomic J5 commit, then J6 resume slot
schemas and closed codecs.

## 357. J6 Canonical Resume Slot Schemas and Codecs

Completed J6 with one `ResumeBoundarySchema` for every J3 boundary and exact
slot reciprocity for every J2 retained or recomputable value. J2 blocked slots
remain explicit `ResumeSchemaBlock` records, while excluded Effect activation
metadata is omitted from serialization.

Authored State, Computed cache, Context, and Form Field values derive codecs
from canonical semantic types. The closed codec vocabulary is null, boolean,
number, string, homogeneous array, canonically ordered object properties, and
an explicit nullable wrapper. Compiler-owned Form dirty/touched/aggregate,
validation-result, and submission slots use the already-frozen boolean,
string-array, and string runtime representations. Unsupported tuples,
resources, and non-null unions block; there is no reflective codec or runtime
type guessing.

Reserved `EZASM1349` through `EZASM1354` for malformed types, duplicate
properties, unsupported values, missing slots, identity collisions, and
ordering/index drift. Verified `cargo test -p ezc_core --lib` (385),
`cargo clippy -p ezc_core --all-targets -- -D warnings`, focused J6 tests,
formatting, and `git diff --check`. Next: atomic J6 commit, then J7 generated
capture programs and internal snapshot model v1.

## 358. J6 Boundary-Order Correction

The post-commit J7 audit found that J6 preserved deterministic schema order but
sorted schemas globally by boundary ID. Phase J's canonical ordering instead
requires the J3 parent-before-child boundary sequence. The registry now retains
the exact J3 order, validation compares the full ordered boundary vector, and a
nested Component fixture proves parent-before-child schema construction.
Verified all 7 focused schema tests, strict all-target core clippy, formatting,
and `git diff --check`.

## 359. J7 Generated Capture Programs and Snapshot Model v1

Completed J7 with one parent-before-child `ResumeCaptureProgram` per J6
boundary. Programs contain only exact `ReadSlot`, `EncodeSlot`, and
`AppendValueRecord` triples for J2 retained slots; recomputable slots are
omitted from snapshot values.

The internal snapshot-v1 envelope writer fixes manifest version 6,
build-derived snapshot identity, `capturedAt: null`, canonical compact JSON,
and standalone trailing-newline policy. Closed encoders cover every J6 value
variant without reflection or runtime object walking, order object fields from
the generated schema, and reject missing/extra properties, runtime shape
guessing, non-finite numbers, and negative zero.

Capture requires the complete Phase J quiescence vector and never waits,
mutates application values, or serializes pending work. Form submission state
is capturable only as `Idle`, `Invalid`, `Failed`, or `Completed`. Reserved
`EZASM1355` through `EZASM1358`. Verified 9 focused capture tests,
`cargo test -p ezc_core --lib` (395), strict all-target core clippy, formatting,
and `git diff --check`. Next: atomic J7 commit, then J8 generated restore
programs and the fixed R0-R20 schedule.

## 360. J8 Generated Restore Programs and Fixed Schedule

Completed J8 with one parent-before-child `ResumeRestoreProgram` per boundary
and the complete R0-R20 application schedule. Every retained/recomputable J2
slot has an exact restore phase. Retained values decode through J6 codecs;
recomputable Computed cache/dirty pairs are assigned to R5 and invoke the exact
compiler-planned evaluator only once per cache.

State writes occur at R3, retained Computed state at R4, Context values at R6,
exact Consumer binding at R7, and Form value/dirty-touched/validation/
submission classes at R11-R14. Every program allocates at R2 and ends with
`MarkBoundaryRestored` at R19. No instruction can execute authored actions,
initializers, render methods, validators, submit handlers, or Effect bodies.
Anchor- and Effect-establishment phases remain present but empty until their
authoritative later slices provide exact identities.

Reserved `EZASM1359` through `EZASM1362`. Verified 5 focused restore tests,
`cargo test -p ezc_core --lib` (400), strict all-target core clippy, formatting,
and `git diff --check`. Negative tests cover dangling references, wrong phases,
duplicate writes, missing completion, parent-order reversal, and deterministic
source reversal. Next: atomic J8 commit, then J9 executable resume manifest v6.

## 361. J9 Executable Resume Manifest v6

Completed J9 with one sole-authority resume manifest v6 publishing every J1-J8
executable product plus normalized Phase I Component/Form resume records.
Snapshot schema v1 and runtime protocol v1 are public. V5, unknown fields,
malformed shapes, version drift, duplicate IDs, and unresolved endpoints are
rejected without a compatibility adapter.

Build now writes canonical `resume.runtime.json` and embeds the exact same
bytes in `#ez-resume-runtime`. `ResumeBuildId` uses SHA-256 lowercase
hexadecimal over framed executable artifacts, zero-sentinel manifest bytes,
normalized chunk bytes, marker plan, and version inputs. Absolute source-root
prefixes, provenance/spans, time, diagnostics, output directory, and machine
information are excluded; executable changes alter the ID.

Verified 8 focused J9 manifest tests, all 405 core tests, 7 Component fixtures,
12 Context fixtures, all 128 CLI inspection/build tests, strict all-target
core/CLI clippy, all 29 sequential real-browser probes, formatting,
`git diff --check`, manual build emission, exact artifact/page byte
equivalence, repeated/reversed determinism, and two-root absolute-path
independence. Next: atomic J9 commit, then J10 exact resume anchors and event
markers.

## 362. J10 Exact Resume Anchors and Event Markers

Completed J10 with a canonical marker plan for every required ordinary
instance-qualified resume target. Element, Form-control, and event targets use
exact ID-only `data-ez-r` and `data-ez-e` attributes. Dynamic text uses
zero-layout `<template data-ez-r>` markers, while conditional and keyed-list
regions extend the existing stable comment machinery with exact
`ez-r-start`/`ez-r-end` pairs.

Resume manifest v6 now publishes the matching anchor and event records and
uses the marker plan in its normalized build fingerprint. The template
manifest remains unchanged. Static-only output emits no resume markers.
Validation covers missing and unstable targets, duplicate anchors, wrong
kinds, structural-pair mismatch, exact page/manifest agreement, and byte
determinism. Reserved `EZASM1363` through `EZASM1368`.

Verified all 5 focused J10 marker tests, all 410 core tests, 7 Component
fixtures, 12 Context fixtures, all 128 CLI inspection/build tests, strict
all-target core/CLI clippy, all 29 sequential real-browser probes, formatting,
and `git diff --check`. Next: atomic J10 commit, then J11 resume runtime
registry and bootstrap loader.

## 363. J11 Resume Runtime Registry and Bootstrap Loader

Completed J11 with runtime Resume registry contract v1 and the internal
`bootstrapResume`, `captureSnapshot`, `activateByEvent`, and
`activateBoundary` API surface. Bootstrap validates the existing runtime
artifacts, resume manifest v6, optional snapshot v1, all closed top-level
fields, versions, unique identities, and manifest cross-references before
selecting cold or resume.

No snapshot preserves the existing cold path. A valid same-build snapshot
allocates empty closed ID-keyed runtime registries without executing cold
authored initialization. Snapshot/artifact rejection discards all partial
registry state and invokes one clean cold boot. Double bootstrap rejects, and
development evidence is deterministic and identity-only.

The readiness audit found four preexisting cold-path failures that literal
success markers had hidden: invalid strict-mode `const arguments` syntax,
class-name/semantic-ID State correlation mismatch, missing text targets in the
ordinary target index, and numeric instance slots retained as strings. Those
are corrected, generated runtime syntax is frozen by a negative assertion,
and the J11 marker is split in source so only executed assertions can assemble
the expected pass token. The cold case now also performs an action and proves
the exact numeric State transition.

Verified the focused five-page real-browser matrix for no-snapshot cold,
accepted resume, build mismatch, runtime-protocol artifact rejection, and
malformed snapshot. It also proves no retained partial registries, exact
event/boundary lookup, double-bootstrap rejection, and unchanged cold state.
All 410 core tests, strict all-target core/CLI clippy, 2 CLI units, 7 Component
fixtures, 12 Context fixtures, 128 CLI inspection/build tests, and all 30
sequential browser probes pass. Formatting and `git diff --check` pass. Next:
atomic J11 commit, then J12 State, Resource, and Computed restoration.

## 364. J12 State, Resource, and Computed Restoration

Completed J12 with exact R3-R5 execution from the compiler-authored restore
programs. Mutable State and authorized stable Resource values decode through
their J6 codecs at R3, retained Computed cache/dirty pairs install at R4, and
only J2-approved recomputable Computed programs execute at R5 in canonical
topological order. Restored slots suppress authored initialization/evaluation,
and no Effect body runs.

The accepted resume path now allocates instance-qualified runtime State and
Computed records directly from closed manifest identities. Snapshot value
absence, duplication, or codec mismatch rejects the candidate atomically and
selects one clean cold boot. Debug evidence qualifies Computed records by
instance so repeated definitions cannot collapse.

A hardened browser probe restores distinct State values 7 and 11 into repeated
instances and proves exact Computed results 14 and 22, one recomputation per
instance, clean caches, isolation, and zero initial Effects. The six-page J11
fallback matrix now also proves an invalid number codec produces
`ValueTypeMismatch` without retained partial state. Verified all 410 core
tests, 2 CLI units, 7 Component fixtures, 12 Context fixtures, 128 CLI
inspection/build tests, all 31 sequential browser probes, strict all-target
core/CLI clippy, generated-JavaScript syntax, formatting, and
`git diff --check`. Next: commit J12 atomically, then implement J13 Context
restoration.

## 365. J13 Context Restoration

Completed J13 R6-R7 execution from the compiler-authored restore programs.
Retained Provider and Context-default values decode through their exact J6
codecs into instance-qualified Context slots. Consumer bindings install only
when `ConsumerInstanceId`, selected source, optional `ProviderInstanceId`, and
value slot exactly match the frozen Component runtime artifact.

No initial Context evaluator, ancestry traversal, Provider selection,
Context-name lookup, or Effect body runs. Missing, duplicate, or mismatched
bindings abort the candidate registry atomically. The frozen Phase G
action-update batches remain the sole future propagation order; resumed event
delivery stays within J17.

The hardened nested browser proof restores three distinct values across a
Context default, outer Provider, and nearest override, then verifies every
Consumer against its exact compiler-emitted slot and source. A separate
minimal hardened probe mutates one R7 source and proves one clean cold fallback
with no retained resume registry or runtime reselection. Focused J13 browser
proofs, strict all-target core/CLI clippy, formatting, and generated-runtime
syntax pass. All 410 core tests pass; the full CLI/browser gate is running.
The completed gate also passes 2 CLI units, 7 Component fixtures, 12 Context
fixtures, 128 CLI inspection/build tests, and all 33 sequential browser
probes. Next: commit J13 atomically, then implement J14 Component, Slot, and
structural restoration.

## 366. J14 Component, Slot, and Structural Restoration

Completed J14 R8-R10 from the compiler-authored restore plan. Component
runtime records and caller-owned Slot bindings now install only through exact
Phase I instance/binding records; resume invokes neither constructors nor
render methods. The restore-plan authority now maps each conditional/keyed-list
structural region to its exact retained instance-qualified State slot, so R9
restores selection/key state without introducing a new storage or identity
domain.

Exact `data-ez-r` and structural comment anchors are verified before Ready.
Resume leaves the server DOM untouched; a structural State mismatch or missing
anchor rejects the whole candidate and cold boots with no partial registry.
The audit also fixed two cold-runtime v4 defects exposed by this fallback:
definition-level anchor validation no longer mistakes uninstantiated Components
for missing DOM, and the ordinary target index recognizes compiler-emitted
conditional/list comment ranges.

Focused real-browser probes prove structural resume preserves DOM identity,
installs exact Component/Slot records, rejects both state/anchor failures into
one clean cold boot, and never runs Component initialization on the resume
path. The existing Phase H structural browser probe remains the authoritative
proof of subsequent create/destroy execution; resumed event delivery is owned
by J17. Next: verify and commit J14 atomically, then implement J15 Form
restoration.
