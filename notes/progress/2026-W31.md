# 2026-W31

## Decorator-free Forms source authority

Recovered the exact V2 Forms syntax from the supplied specification archive and
implemented the first canonical slice. `defineForm` and its typed `field`
authoring surface are now public. V2 authority request/response schema v5
classifies `defineForm` by resolved TypeScript identity, lowers it into the
canonical authored model, and projects it into the existing Form entity.

No Form controller, source translator, alternate runtime, or decorator fallback
was added. Field schema, validation, submission, bindings, and cold/resume
browser proof remain the next slices.

Focused compiler, CLI compilation, framework package, TypeScript-authority, and
public TypeScript checks passed. Beta readiness moved from 76% to 79%.

## Decorator-free Form Fields and serialization

Added a source-faithful static Form-definition view without assigning framework
meaning in the parser. TypeScript authority schema v6 now independently proves
each nested `field(...)` call and the compiler joins those declarations only
under an authority-proven `defineForm(...)`.

Canonical Form Fields now project into the existing Field, semantic type,
serialization, and nested-path products. Dynamic field structures, missing
initial values, unsupported options, invalid arity, and non-canonical
lookalikes fail closed. Validation expressions and inline submit shape are
retained for the next authority-owned slices; no rule-name recognition or
decorator compatibility bridge was introduced.

Focused parser, canonical-lowering, V2 Form ASM, TypeScript-authority, CLI
compilation, and public TypeScript checks passed. Beta readiness moved from 79%
to 82%.

## Decorator-free built-in validation

V2 authority schema v7 now resolves imported built-in validation helpers by
TypeScript identity and joins each proven rule to its canonical Form Field.
Aliased helpers are normalized to the canonical rule identity before entering
the frozen validation graph; locally declared lookalikes remain inert.

Validation arguments, ordering, nested Field ownership, diagnostics, plans, and
provenance continue through the existing compiler products. No parser spelling
or decorated-method bridge was granted semantic authority. Focused authority,
V2 Form ASM, TypeScript-authority, and CLI compilation checks passed. Beta
readiness moved from 82% to 84%.

## Decorator-free Form bindings and resume

Canonical V2 Form Field paths now bind through `bind:value` and
`bind:checked`. Resolution requires the exact local Form identity and nested
Field path, so similarly named Fields in other Forms or components cannot be
selected accidentally.

A decorator-free project now compiles and runs in a real browser. The probe
proves cold initialization, text and checkbox updates, validation, emitted Form
artifacts, stable snapshot resume, and DOM control synchronization. The same
source contains no decorated compatibility bridge. Beta readiness moved from
84% to 87%.

## Native decorator-free submission

The `submit` callback in `defineForm` now creates a native V2 action endpoint
and enters the existing submission, serialization, host, action-batch, and
runtime products. The first admitted callback body supports canonical State
updates and accepts the specified one-parameter sync or async form. Dynamic or
external statements are rejected rather than discarded.

The real-browser V2 Form project now submits through its generated host,
prevents native navigation, validates and serializes compiler-owned values,
updates State, and reaches `Completed`. No decorated method is synthesized.
Beta readiness moved from 87% to 89%.

## Authority-backed file Fields

Added the V2 platform-value contract for `field<File[]>` and `bind:files`.
TypeScript authority schema v8 proves both the canonical `field` call and the
resolved DOM `File` element type; JSX spelling alone cannot opt a Field into
file semantics.

FormData planning now admits file arrays while JSON, URL encoding, network,
HTML, and resume classifications remain rejected. The browser fixture proves
native `change` capture, required validation, reset through the permitted empty
control value, submission, and partial Form resume: serializable Fields restore,
file Fields cold-reset, and validation recomputes. Beta readiness moved from
89% to 91%.

## Executable built-in validation

Replaced debug-formatted Form rule arguments with the closed schema-v4 runtime
vocabulary. The browser now executes numeric, length, pattern, email, required,
and compiler-bound cross-Field rules and rejects unknown or mismatched records
instead of silently treating them as valid.

The decorator-free browser project proves every unary built-in, prevents IME
composition text from committing early, and confirms that Form inputs and the
submission host remain live after snapshot resume. Beta readiness moved from
91% to 92%.

## Standard Schema compile-time authority

Added a fail-closed Standard Schema v1 boundary without introducing compiler
evaluation or browser source reconstruction. Core TypeScript authority schema
v3 proves the protocol shape and Field compatibility. V2 authority schema v9
then joins the parser-selected validator site to its exact named import,
resolved declaration modules, and input/output type displays.

Canonical authored semantics schema v5 and the Form graph retain that evidence.
Until the original export is bundled into an integrity-bound runtime module,
the candidate remains visible and emits `PSC1087`; it is never silently
discarded or mistaken for a built-in helper. Focused TypeScript, compiler,
public API, and CLI compilation checks passed. Beta readiness moved from 92%
to 93%.

## Executable Standard Schema validation

Closed the runtime half of the Standard Schema boundary. Ergonomic builds now
generate an internal entry that imports only TypeScript-authority-proven named
exports and bundle it with the project's direct Vite dependency. The preserved
registry export is published as `presolve.validators.js` through the normal
digest-inventoried file-route product. Forms runtime artifact schema v5 carries
its absolute path and exact validator IDs; bundle, export, registry, and
protocol failures remain closed.

The browser runtime admits synchronous or Promise results, normalizes issues,
ignores transformed output as implicit Field coercion, suppresses stale
generations, and awaits validation before submit and runtime readiness. The
decorator-free browser fixture proves cold and resumed behavior, file-field
cold reset, post-resume input, and a deliberately out-of-order Promise pair.
The gate also replaced a source-text-satisfiable browser marker and fixed
duplicate ordinary-binding registration for compiler-owned Form `bind:*`
channels. Beta readiness moved from 93% to 95%.

## Imported Form submission capability contract

Defined the compiler-authoritative boundary for asynchronous decorator-free
Form persistence without interpreting handler source. The parser now retains
one direct identifier call and its identifier arguments as syntax-only facts.
The admitted source form is an async, single-parameter submit callback whose
entire body invokes one named import with exact `value, signal` arguments.

Semantic-package kind `capability` now requires the exact
`(FormValue, AbortSignal) -> Promise<void>` signature plus client execution,
abort cancellation, Form-value input, void result, and cold fallback metadata.
This closes the previously generic capability kind rather than admitting an
opaque escape hatch. Focused parser and semantic-package tests passed. Runtime
artifact publication, exact export bundling, lifecycle execution, and browser
proof are deliberately the next implementation gate. Beta readiness moved from
95% to 96%.

## Executable imported Form submissions

Projected the closed client persistence boundary through compiler submission
products and Forms artifact schema v6. Each admitted plan retains its exact
package, version, integrity, export, runtime module, and cold-fallback policy.
Ergonomic publication builds a deterministic `presolve.form-submissions.js`
registry from the package runtime module; missing modules, registry drift, and
non-callable exports fail closed.

The runtime validates before invoking, constructs the canonical nested value
from compiler-issued Field paths, suppresses duplicate submits, and owns a
fresh AbortController per accepted call. Fulfillment, rejection, abort,
reset, page lifecycle, and structural component disposal have explicit
lifecycle handling. Active submission remains non-resumable.

The browser fixture uses a real discovered semantic package and proves
deterministic double builds, exact values including `File[]`, fulfillment,
rejection, duplicate suppression, reset-driven abort, cold boot, and resumed
submission. Public TypeScript, full compiler/CLI/package/docs checks, all 57
browser tests, production budgets, and the updated 290343-byte deterministic
runtime baseline pass. Beta readiness moved from 96% to 98%.

## Package usage classification amendment

Authored `docs/v2/package-usage-classification-contract.md` to remove package-
wide decorator admission as the long-term third-party model. External package
diagnostic depth is now classified per authority-proven use site: erased or
Vite-owned inputs create no Presolve runtime semantic; discarded terminal
client calls are bounded capabilities; values entering State, Computed,
rendering, Forms, Context, serialization, or resume require a closed semantic
contract; and framework/server integrations retain dedicated products.

The first executable slice is deliberately narrow and testable: a synchronous
decorator-free V2 Action whose complete body calls one named import with no
arguments and discards its result. TypeScript authority must prove the exact
export, Vite must publish a digest-inventoried callable registry, and real-
browser evidence must prove execution, compatible resume, deterministic
publication, and failure diagnostics. This amendment does not synthesize
`@opaque`, inspect package source, or claim compatibility with packages built
for another host runtime.

## 0.2.0-beta.14 release hardening

Prepared the complete release train at 0.2.0-beta.14 and VS Code prerelease
0.2.14. The version-derived compiler session, workspace, service, persistence,
and query-snapshot identities were regenerated from canonical compiler output;
the query product and language-service invalid-identity test now share the
exact release identity.

The full `pnpm release:check` gate passes: 654 compiler tests, all 57
real-browser tests, TypeScript 7.0.2 compatibility, public TypeScript, 88
documentation files, a newly packed scaffold, Rust crate packaging, VSIX
packaging, and deterministic hashes for all ten release packages. Local release
readiness is complete. Beta readiness moved from 98% to 99% pending hosted
publication and public registry verification.

The first hosted beta.14 CI run exposed a clean-runner dependency gap in the
isolated browser job: the Form acceptance project deliberately uses the
application's direct Vite dependency, but that job had not installed the locked
Node workspace. The browser gate now installs the same pinned pnpm and Node
versions plus the frozen dependency graph before executing the Rust-owned
Chrome suite. This preserves the production bundling path instead of replacing
it with a test-only module.

The next hosted package run exposed a second host boundary: TypeScript's native
authority returned lowercased declaration paths on case-insensitive macOS but
source-cased paths on Linux. Declaration modules are semantic identity
coordinates rather than lookup paths, so the authority now case-folds those
project-relative identities on every host while keeping authored runtime module
specifiers source-faithful. The same Standard Schema export therefore has one
canonical identity across the release matrix.

After both portability fixes, hosted `main` CI and the independent release dry
run passed. Tag `v0.2.0-beta.14` then completed the full Publish beta workflow:
input verification, crates.io publication, four native CLI builds, npm
publication, VS Code prerelease publication, and GitHub prerelease creation.

Public verification confirmed:

- all 13 npm packages exist at `0.2.0-beta.14`, with every `beta` dist-tag
  selecting that version;
- `presolve-parser`, `presolve-compiler`, and `presolve-cli` are indexed at
  `0.2.0-beta.14` by Cargo;
- Marketplace lists `fierstdev.presolve-vscode` prerelease `0.2.14`; and
- GitHub publishes `Presolve 0.2.0-beta.14` as a non-draft prerelease.

Beta completion moved from 99% to 100%.

## 0.2.0-beta.15 route-local runtime publication

The first interactive production site built with beta.14 exposed a
file-route isolation defect: conventional publication selected the requested
route for HTML composition, but retained sibling route roots while deriving
runtime component and resume products. An action on the selected page could
therefore boot another route against targets that were never rendered and
raise `PSR_INVALID_ORDINARY_TARGET`.

File-route assembly now retains only the compiler-planned owner root that
contains the selected route before instance scope, runtime, and resume products
are derived. A regression fixture proves that a selected interactive route
keeps its layout and local instances while excluding a sibling route's
instances, targets, and events. The real presolve.dev application then proved
that its docs publication contains no homepage targets or resume events, its
homepage counter still updates, cross-route navigation remains clean, and the
state-backed mobile menu closes after navigation without a runtime diagnostic.

The complete `pnpm release:check` gate passes at `0.2.0-beta.15`: formatting,
clippy, every Rust and package test, all 57 real-browser cases, TypeScript
7.0.2 compatibility, 88 documentation files, a newly packed scaffold, crate
packaging, VSIX packaging, and deterministic hashes for all ten release
packages. Beta remains complete; this patch closes a production route-isolation
defect rather than adding a workaround in the site.

Hosted CI, the independent release dry run, and every Publish beta job passed.
GitHub Actions published all 13 npm coordinates with the `beta` tag, all three
Rust crates, four native CLI packages, VS Code prerelease 0.2.15, and the
non-draft GitHub prerelease. Public registry queries independently returned
`0.2.0-beta.15` for every npm package and crate.

Presolve.dev now installs those public packages, documents beta.15, and is
deployed as Cloudflare Worker version
`7546ea4f-1fde-40ed-b2ae-a7a5942879a6`. All 26 routes return complete styled
HTML. Production browser proof at 390 CSS pixels shows the menu opening,
closing after same-page selection, restoring page scroll, keeping the viewport
at exactly 390 pixels, preserving counter interactivity, and navigating to the
docs without runtime errors.

## Safari returning-client asset coherence

An iOS Safari returning-client report showed current beta.15 HTML without its
external CSS or runtime behavior, while Chromium and fresh WebKit contexts
loaded the same production publication successfully. The old document link
used a digest query on the stable `/app.css` path and every route executed the
stable `runtime.js` path, leaving browser cache/revalidation behavior able to
separate current HTML from its matching external bytes.

Conventional publication now makes the browser coordinates immutable. The
document links `/app.<sha256>.css` and executes the route-local
`runtime.<sha256>.js`; `/app.css` and `runtime.js` remain byte-identical
compatibility artifacts. The compiler-issued file-route manifest inventories
and digest-verifies every immutable and compatibility path, and atomic
publication retains its existing validation.

All 18 ergonomic-project tests, the production runtime determinism fixtures,
formatter, and strict CLI clippy pass. Building the real Presolve.dev source
with the corrected CLI emits digest-bearing CSS and runtime paths across all 26
routes. A headless WebKit 26 iPhone 15-class probe loads one stylesheet,
computes the expected dark canvas, preserves a 393-pixel viewport with zero
horizontal overflow, reports no console or request failures, and updates the
homepage counter from `Count: 0` to `Count: 1`.

Beta completion remains 100%. The correction is a publication-coherence patch,
not a site-only fallback; the next action is the `0.2.0-beta.16` release train
and public-package adoption on Presolve.dev.

## 0.2.0-beta.16 release preparation

Prepared the complete package, crate, native CLI, and VS Code release train at
`0.2.0-beta.16` and `0.2.16`. The compiler-contract change regenerated the
canonical workspace and query-snapshot identities, and the browser resume
probes now discover the compiler-emitted content-addressed runtime coordinate
instead of assuming the retained compatibility filename.

The complete `pnpm release:check` dry run passes: formatting, strict workspace
clippy, every Rust and package test, all 57 real-browser cases, TypeScript
7.0.2 compatibility, public TypeScript declarations, all 88 documentation
files, a newly packed scaffold, Rust crate packaging, VSIX packaging, and
deterministic hashes for all ten release packages. Beta completion remains
100%; hosted CI, public publication, registry verification, and Presolve.dev
adoption are the remaining release operations.

## 0.2.0-beta.16 publication and returning-client proof

Hosted `main` CI run `30418471399` and release dry-run `30418471420` passed.
Tag `v0.2.0-beta.16` then completed Publish beta run `30419010174`, including
input verification, all three crates, all 13 npm packages and native CLIs, the
VS Code prerelease, and the non-draft GitHub prerelease. Independent public
queries confirmed every npm `beta` tag and Cargo release at
`0.2.0-beta.16`; Marketplace lists prerelease `0.2.16`.

Presolve.dev now installs the public beta.16 packages, documents the immutable
publication contract, and runs as Cloudflare Worker version
`35705c75-0053-4392-a1d5-0e6b6e052030`. All 26 public routes return HTTP 200.
The favicon and alternate PNG mark both return HTTP 200.

The production acceptance check reused a persistent WebKit profile that had
loaded beta.15 before deployment; no browser storage or cache was cleared. The
same `https://presolve.dev/` URL then loaded beta.16 with
`app.<sha256>.css` and `runtime.<sha256>.js`, computed the expected dark
canvas, preserved a 393-pixel viewport without overflow, reported no console or
request failures, and updated the homepage counter from `Count: 0` to
`Count: 1`. The docs architecture route remained styled and runtime-ready
after navigation and reload in that same returning profile.

Beta completion remains 100%. The reported iOS Safari stylesheet and
interactivity regression is closed at the compiler publication layer and
verified on the deployed product.

## Decorator-free package interoperability

Clarified that the prior 100% figures measured individual release-train
completion, not the full beta product gate list. The final beta remains gated
by broader V2 Actions, Effects, structural resume/Slot projection, Context,
completion-grade product families, and release hardening.

Implemented the first package-use slice from
`docs/v2/package-usage-classification-contract.md`. TypeScript authority schema
v10 proves exact named-import identity at both the import and call site.
Authored schema v6 joins that proof to a canonical decorator-free Action.
Ordinary V2 package imports are now classified as Vite inputs rather than
requiring a legacy semantic-package contract; Presolve diagnostics apply only
when the use site enters compiler-owned semantics.

Publication emits deterministic `package-invocations.runtime.json` records and
Vite bundles the exact proven exports into
`/presolve.package-invocations.js`. Runtime boot validates the registry,
executes once per accepted event, reports stable module/invocation diagnostics,
and restores the stateless Action event without replaying side effects.

`examples/package-interop` provides the canonical source. Focused compiler,
authority, public TypeScript, ergonomic publication, and application
publication suites pass. A real-browser test accepts a valid resume snapshot,
executes two events exactly twice, and rejects a missing registry with the
fatal `PSR_PACKAGE_INVOCATION_MODULE_FAILED` diagnostic. Deterministic double
builds retained identical registry, artifact, and document hashes.

Full-beta completion moved from the 30% contract foundation to 36% after the
executable package invocation gate.

## 0.2.0-beta.17 release preparation

Prepared the complete package, crate, native CLI, scaffold, and VS Code release
train at `0.2.0-beta.17` and Marketplace prerelease `0.2.17`. Authored
semantics schema v6 and the compiler-contract version produced new canonical
tooling identities; the reviewed fixtures now bind the exact beta.17 bytes.

The package-call runtime addition moved the audited representative
`runtime.js` baseline from 290,343 to 297,509 bytes without exceeding the
committed production budgets. Both baseline fixtures remain byte-deterministic.

The complete uninterrupted `pnpm release:check` passes: formatting, strict
workspace clippy, every Rust and package test, all 58 real-browser cases,
TypeScript 7.0.2 compatibility, public TypeScript declarations, all 89 public
documentation files, a clean application scaffolded from packed packages,
Rust crate packaging, VSIX packaging, and deterministic hashes for all ten
release packages.

Full-beta completion is 38% after the release-hardening gate. The remaining
release operations are the `fierstdev/presolve` main merge, beta.17 tag,
GitHub Actions publication, and independent registry verification. Broader
Action arguments and Promise completion remain the next authored product gate
after publication.

## 0.2.0-beta.17 publication and Presolve.dev adoption

Fast-forwarded the reviewed beta.17 commits to `fierstdev/presolve` `main`,
created and pushed `v0.2.0-beta.17`, and completed hosted CI
`30455449678`, release dry run `30455449652`, and Publish beta
`30456828739`. The publish run completed all Rust crates, npm packages, native
CLIs, VS Code prerelease `0.2.17`, and the non-draft GitHub prerelease.
Independent npm queries resolved the public `beta` tag for every Presolve
package to `0.2.0-beta.17`.

Presolve.dev now installs the public beta.17 framework, CLI, and TypeScript
authority packages. The package guide replaces the legacy decorator example
with the exact named-import Action form and documents use-site classification,
compiler/Vite/TypeScript diagnostic ownership, runtime artifacts, resume
behavior, current exclusions, and verification commands.

Cloudflare Worker version `758ba975-db0a-4772-9a7c-9225b76b20a1`, release
SHA-256
`e374169289c6402125b522df8a448d5ae474efd515df4493b7d621c660738a6e`,
serves the update. The first custom-domain read briefly observed the prior
root cache entry during propagation; the new Worker origin and a cache-busted
custom-domain request already served beta.17, and the canonical root converged
to beta.17 on the next read. Production mobile proof covers the
content-addressed stylesheet, returning reload, logo geometry, internal code
scrolling with no document overflow, opaque navigation, close-on-navigation,
and `Count: 0` to `Count: 1` with no console diagnostics.

Full-beta completion is 40% after public release and production documentation
adoption. Compiler-transported package Action arguments and Promise-aware
completion remain the next authored product gate.

## Typed and Promise-aware package Actions

Completed the package-Action amendment through one compiler-owned path.
TypeScript authority schema v11 now verifies exact ordered primitive
parameters, synchronous non-Promise completion, `Promise<void>`, and the DOM
`AbortSignal` identity. Authored semantics schema v7 and package-invocation
artifact schema v2 carry the argument codecs and lifecycle contract without
retaining handler source.

The runtime constructs arguments only from compiler-accepted event records,
injects one owned signal for Promise work, replaces prior work per component
instance, cancels on structural teardown and `pagehide`, suppresses stale
settlement, preserves rejection evidence, and restores events without replay.
The browser proof also exposed and closed a shared event transport bug: numeric
Action arguments are now emitted as JSON numbers instead of strings.

`examples/package-interop` proves string, number, boolean, and null forwarding;
Promise fulfillment, replacement, failure, and pagehide cancellation; no
resume replay; post-resume interaction; and missing-registry failure. Two
complete builds produced identical invocation-artifact and registry hashes.
The complete Rust workspace gate passes, including all 58 real-browser tests
and 662 compiler tests. The audited representative runtime baseline is now
301,905 bytes and remains within the committed production budgets.

Full-beta completion is 45%. The next required gate reconciles
`create-presolve` with the canonical Presolve.dev project model and publishes
exhaustive ownership, styling, Vite, and runnable-concept documentation.

## Canonical scaffold, publication retention, and Examples runtime correction

Reconciled the generated application with the canonical beta ownership model.
`create-presolve` now provides interactive destination selection, closed CLI
help/version behavior, a complete document template, shared application shell,
route-owned primary landmark, mobile-first accessible global CSS, favicon and
public inputs, and a live State/Action/Computed route. Its embedded README and
the public project-structure and styling/Vite guides explain why every path
exists, how byte-exact and content-addressed CSS publication works, how
Tailwind/PostCSS feed `app/app.css`, what project-local Vite bundles, how public
and imported assets differ, and where the compiler fails closed.

Atomic application and file-route publication now retains exactly the active
hidden release directory. Before and after pointer exchange it removes only
directories matching Presolve's exact numeric stage grammar, preserves the
current release through a failed build, and leaves caller-created lookalikes
untouched.

The production-shaped Presolve.dev Examples page then exposed a closed-runtime
defect. Its shared `app.tsx` shell and route-owned Computed values generated
valid instance-qualified slots, but cold boot evaluated every Computed program
for the shell instance first. `computedSlotForExecution()` correctly rejected
the route slot under the shell identity, producing
`PSR_INVALID_COMPONENT_ARTIFACT`, `undefined` totals, and a disabled event
runtime. `executeComputedPlan()` now filters evaluations by the component
definition of the active compiler-issued instance. No synthetic slot or
site-only fallback was added.

The generated starter is the regression fixture: real Chrome proves that the
shell and route boot together, CSS and document metadata apply, one primary
landmark exists, the Action updates State, the route Computed value recomputes,
and no runtime diagnostic is emitted. Application publication, all 18
ergonomic-project cases, runtime code generation, production determinism and
budgets, strict touched-crate clippy, TypeScript 7.0.2 compatibility, creator
tests, and all 90 public documentation files pass. The audited runtime baseline
moved from 301,905 to 302,247 bytes and remains inside its committed budget.

Full-beta completion remains 45% until this combined gate passes the
uninterrupted release dry run, is published as `0.2.0-beta.18`, and is adopted
and browser-proven on Presolve.dev.

## 0.2.0-beta.18 release preparation

Prepared all crates, npm packages, native CLI metadata, scaffold references,
and the VS Code Marketplace prerelease in lockstep at `0.2.0-beta.18` and
`0.2.18`. The compiler-contract version change regenerated the canonical
tooling query snapshot and its validation identity.

The complete beta.18 release dry run passes without interruption: strict
formatting and clippy, the Rust workspace, 59 real-browser cases, all package
tests, TypeScript 7.0.2 compatibility, 90 public documentation files, a
clean-room application installed from freshly packed packages, Rust crate
packaging, VSIX packaging, and deterministic SHA-256 hashes for all ten release
packages.

Full-beta completion remains 45% until hosted CI, the release dry run, GitHub
Actions publication, registry verification, Presolve.dev adoption, and
production browser proof are complete.

## Beta.18 publication and initial Computed identity correction

Hosted CI `30513133319`, release dry run `30513133285`, and Publish beta
`30513847524` all passed. `0.2.0-beta.18` is public across crates.io, npm,
native CLI artifacts, the VS Code Marketplace as `0.2.18`, and the non-draft
GitHub prerelease.

Presolve.dev installed those exact public packages and passed compiler checks
and production assembly, but the first 390px browser gate exposed a remaining
cold-boot mismatch. Runtime status was ready and Actions worked, yet route
Computed bindings initially rendered `undefined`. The component artifact keys
instances by semantic component ID while the Computed artifact keys ownership
by authored component name. Cold-boot and resume allocation incorrectly copied
the former into `component.name`, so the beta.18 evaluation filter skipped the
route's initial programs and binding registration observed empty caches.

Both allocation paths now retain `definition.name`. The semantic component ID
remains the definition lookup and instance identity; no alternate runtime
authority was introduced. The generator rejects the old spelling, the starter
browser fixture asserts authored route identity, and the full Presolve.dev
acceptance probe now passes with zero diagnostics: initial `$ 24` and `1000`,
updated `$ 48` and `3000`, conditional panels, boolean State, Preview/Code
switching, invalid Form blocking, valid Form submission, applied CSS, and no
390px document overflow.

The project-structure guide exposed a second publication defect by displaying
the three document placeholders in a code sample. Document assembly performed
sequential replacements after inserting application HTML, allowing that sample
to consume the runtime payload. The generated code block grew to 835,288 bytes,
lost its literal runtime token, and showed runtime whitespace/source inside the
body example. Assembly now records all validated placeholder positions before
inserting any payload, so inserted content cannot become replacement input.
The exact compiler test preserves literal head/app/runtime spellings and one
runtime manifest. The rebuilt guide retains all three literals in a compact
2,515-byte sample with no payload spill.

Full-beta completion is 48%. Both corrections must ship immediately as
beta.19, then Presolve.dev can adopt them, close the HMR/CSS-explanation
contract, and complete the expanded showcase/deployment gate.

The uninterrupted beta.19 release dry run passes. It covers strict formatting
and clippy, 663 compiler tests, all 59 browser cases, package suites,
TypeScript 7.0.2, 90 documentation files, clean-room packed-package scaffold
installation, Rust and VSIX packaging, production determinism/budgets, and
deterministic hashes for all ten release artifacts. The audited runtime
baseline is 302,241 bytes, six bytes below beta.18.

## Beta.19 publication and canonical development updates

Hosted CI `30516684036`, release dry run `30516684048`, and Publish beta
`30517196543` all completed successfully for `0.2.0-beta.19`. The release is
public across crates.io, npm, all native CLI packages, VS Code prerelease
`0.2.19`, and the non-draft GitHub prerelease. Registry inspection confirms
the framework, CLI, and creator `beta` tags resolve to beta.19.

Characterization of the canonical development command then found a product
gap: `presolve dev` built once and served a frozen file-route manifest. The
public Vite adapter transported validated HMR products for custom hosts, but
the scaffold command had no watcher, rebuild, stylesheet update, browser
reload, or recovery path.

The canonical CLI now snapshots authored inputs while excluding generated
output, dependencies, Git data, Rust output, and Presolve's atomic publication
stages. Every observed edit reruns the development-profile compiler in a child
process, atomically retains the last successful publication, and swaps in the
new compiler-issued file-route manifest. CSS-only edits publish a
`style-update` and replace the global stylesheet through
`/app.css?presolve-dev=<revision>` without replacing the document. Other edits
use the fail-closed full reload boundary until narrower compiler compatibility
evidence exists. Failed builds keep the last good page available and publish
the exact compiler stderr in an accessible browser alert; recovery reloads
automatically. All development responses, including redirects, are
`Cache-Control: no-store`, and the same-origin external development client
works with ordinary `script-src 'self'` CSP policy.

Focused integration tests prove CSS updates, manifest reload, route content
replacement, failed-build retention, diagnostics, and recovery. A real Chrome
probe additionally proves:

- the page begins red with a browser-owned marker;
- CSS changes to blue at `/app.css?presolve-dev=1` while the marker remains;
- a TSX edit reloads to new route content and clears the old page marker;
- an invalid edit leaves the prior content visible with
  `PSROUTE2002_FILE_ROUTE_SET_EMPTY`; and
- a valid correction removes the diagnostic and renders `Recovered`.

Public and scaffold documentation now traces how route/component classes match
global selectors in `app/app.css`, how Presolve links immutable production CSS
from every generated route head, how Tailwind/PostCSS output feeds that file,
and how development hot swap differs from fail-closed semantic reload.

`0.2.0-beta.20` and VS Code prerelease `0.2.20` are prepared in lockstep for
this development-server gate. The version-derived query snapshot has the new
canonical query ID
`aaabc81a9822494ed1303092503d35797301409ffa6d19efed21e5f604a01acc`
and snapshot ID
`06331d0a89ea719a4b773391b65b5651a634d5022eb274fe407d2dc8f7e2dcbf`.

The uninterrupted beta.20 release dry run passes after one resource-related
Chrome process SIGKILL was isolated and rerun successfully. The clean rerun
covers strict formatting and clippy, all 59 real-browser cases, 663 compiler
tests, the full Rust and package workspaces, TypeScript 7.0.2 compatibility,
90 public documentation files, a clean-room packed-package scaffold, Rust and
VSIX packaging, production determinism and budgets, and all ten deterministic
release artifacts. The framework, CLI, and creator package hashes are:

- `ca990ac4899649ac6288a77357b28e7e353084a8076119d8ea1f37f7468425a9`;
- `0212b4034b541240cde6bc70e7720f8d2a5a07e673a2f7842656535a4c6d0aa6`;
  and
- `b85d9d3a7a805a7f5a2ac6e727226930c37f354b329582d1d115f98b343f9c0a`.

Full-beta completion is 50%. Publication through the hosted main and tag gates
is the next required release step.

## Beta.21 creator publication correction

`0.2.0-beta.20` published successfully through hosted CI `30519039660`, release
dry run `30519039682`, and Publish beta `30519693658`. Presolve.dev adopted the
public packages, passed its local compiler/build/browser matrix, and deployed
Cloudflare Worker version `047539de-cf39-4d91-b509-72c6d34df135` with release
SHA-256
`a749cdb16950a7d4bb7b55852d9b32eadfdcd9b6395724463c26521fd405beff`.

An independent unqualified `pnpm create presolve my-app` then exposed a
publication defect. npm's `latest` tag still selected beta.1; after repairing
the tag to beta.20, the clean-room command proved that the immutable beta.20
creator tarball itself still emitted beta.1 framework, CLI, and
TypeScript-authority pins. A dist-tag-only change therefore cannot repair the
public scaffold.

Beta.21 fixes the source and the publication contract together. Every release
surface is locked to `0.2.0-beta.21` (VS Code `0.2.21`), `create-presolve` is
published directly under `latest`, the workflow waits for registry convergence,
and a clean-room unqualified creator invocation must emit the exact framework,
CLI, and TypeScript-authority release pins before the workflow can proceed.
The one-off metadata repair proved the renewed npm credential can mutate access
and dist-tags; its GitHub Action failed only because the immediate registry read
had not converged, and the durable retry loop closes that race.

The uninterrupted beta.21 release dry run passes after one discarded attempt
timed out downloading Cloudflare's `workerd` package. The clean run includes
strict formatting and clippy, all 59 real-browser cases, 663 compiler tests,
the complete Rust/package/TypeScript suites, 90 documentation files, a
clean-room packed-package scaffold install/check/build/deploy preparation,
Rust and VSIX packaging, production determinism and budgets, and ten
deterministic release artifacts. The framework, CLI, and creator hashes are:

- `93562c6cd75240f531a7e6fe42449be8668cb88c4225824a22b5d83031f5cba3`;
- `14d3344aee7a0179d80962f0fcf964d782e77804c0715405f2a2a894d05166d3`;
  and
- `a992e68ff4fea5c31292bf58e2a89816f0567cb172aa113f69cfed2894a1a3b3`.

Full-beta completion is 58%. Hosted main gates, beta.21 publication, a public
unqualified scaffold proof, and Presolve.dev adoption are next.
