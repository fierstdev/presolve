import { component, Component } from "presolve";

@component()
export class Packages extends Component {
  render() {
    return <article><h1>Third-party packages</h1><p>Use normal packages where the compiler admits their imported capability. For a terminal call outside the lowered model, declare an @opaque boundary so application ownership remains clear.</p><p>Opaque boundaries do not hide reactive state, routing, or rendering from the compiler.</p></article>;
  }
}
