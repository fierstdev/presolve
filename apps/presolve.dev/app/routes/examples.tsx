import { component, Component } from "presolve";

@component()
export class Examples extends Component {
  render() {
    return <article><h1>Examples</h1><p>Counter, composition, Context, forms, resumability, routes, and this documentation site are all compiled from the same authoring model.</p><p>Read the source in the Presolve repository to see the exact compiler contracts behind each feature.</p></article>;
  }
}
